# notron-sdk — Implementation Log

Log berjalan per fase sesuai `NOTRON_SDK_06_ROADMAP_PHASING.md` bagian Format Entry. Bersifat append-only.

---

## Fase 0 — Eksplorasi & Pemetaan (selesai: 2026-09-12)

### Selesai
- Membaca `NOTRON_ARCHITECTURE_RECONSTRUCTION.md:1` (prinsip command-based: setiap fitur → command `namespace.aksi` + `executeCommand(id)` terpusat) dan `NOTRON_MODULARITY_MAP.md:1` (17 titik ekstensi modular vs 8 batasan tetap core).
- Membaca `NOTRON_SDK_00_INDEX.md:1`, `NOTRON_SDK_03_HOST_RUNTIME.md:1`, `NOTRON_SDK_06_ROADMAP_PHASING.md:14` — mengonfirmasi Fase 0 adalah eksplorasi murni tanpa menulis kode SDK.
- Eksplorasi `src/lib/commands/registry.ts:48` — `CommandRegistry` (`register`, `registerAll`, `get`, `execute`, `executeCommand`, `onDidRegister`) + `CommandCapability` (`safe`|`trusted`|`dangerous`) + `trustGate` dan `isAllowed`. Helper global `executeCommand(id)` di `registry.ts:150`.
- Inventarisasi **87 command ID unik** (deduplicated) tersebar di 12 modul: `coreCommands.ts:34`, `fileCommands.ts:19`, `workbenchCommands.ts:56`, `editorCommands.ts:23`, `editorActionCommands.ts:11`, `explorerCommands.ts:27`, `tabCommands.ts:22`, `viewCommands.ts:9`, `terminalCommands.ts:12`, `gitCommands.ts:13`, `runCommands.ts:10`, `searchCommands.ts:8`, `windowCommands.ts:11`, `previewRegistry.ts:130`.
- Verifikasi format ID konsisten `namespace.aksi` — mayoritas `workbench.action.*` (44), `editor.action.*` (12), `explorer.*` (14), `git.*` (12), `window.*`/`terminal.*`/`run.*` — sudah dogfooding: UI tidak memanggil handler langsung, semua lewat `commandRegistry.execute(id)` (contoh `keybindingService.ts:163`, `menuRegistry.ts:138`).
- Eksplorasi keybinding terpusat `src/lib/commands/keybindings.ts:30` — `DEFAULT_KEYBINDINGS: Keybinding[]` (104 entri) memetakan `key+modifiers+chord → command ID` (bukan handler). Resolver `resolveKeybinding`/`executeKeybinding` di `keybindings.ts:137`. Capability `commandCapabilities.ts:43` menandai `trusted`/`dangerous` untuk trust-boundary (14 command sensitif).
- Eksplorasi menu registry `src/lib/workbench/menuRegistry.ts:11` — `MenuItem { id, menuId, label, command, group, order, when }` + `menuId` sebagai container (`menubar/file`, `explorer/context`, `editor/context`, `tab/context`, `view/title`, dll). Rendering via `getMenuItems(menuId)` yang otomatis resolve `commandRegistry.execute`. Menubar grouping di `getMenubarMenus`.
- Eksplorasi event bus internal `src/lib/utils/eventBus.ts:1` — `AppEvents` berbasis `window: EventTarget` + `CustomEvent`. `EventBus { on, off, once, emit, onRaw, emitRaw }` singleton `eventBus` di `eventBus.ts:77`. Daftar `AppEvents:7` berisi 14 event typed.
- Eksplorasi Tauri `emit`/`listen` nyata: Rust emit di `watcher_service.rs:346` (`fs-change`, `git-status-refresh`), `file_ops.rs:392` (`search-invalidate`, `git-invalidate` pada create/delete/rename/copy), `git_service.rs:346` (`git-decorations-changed` delta, `git-output`, `git-output-warning`, `git-status-refresh`), `lib.rs:237` (`open-path` single-instance). Frontend listen di `App.svelte:844` (`fs-change`), `gitRepo.ts:182` (`git-status-refresh` debounced 400ms + `git-output*`), `gitDecoration.ts:59` (`git-decorations-changed`), `FileTree.svelte:49` (`fs-change`). Tidak ada `Tauri listen` untuk `onDidOpenTextDocument` dll — semua document events berbasis store, bukan Tauri event.
- Eksplorasi file system access `src/lib/services/fileService.ts:43` — interface `IFileService` (13 method: `readText`, `readChunked`, `save`, `readDirectory`, `readDirectoryFlat`, `readDirectoryBatch`, `getFilesMetadata`, `batchReadFiles`, `getEncodingInfo`, `detectLanguage`, `createFile`, `createDirectory`, `renameItem`, `deleteItem`, `copyItem`, `fileExists`, `listAllFiles`). Implementasi `TauriFileService:66` memanggil `invoke('snake_case')` per `src/lib/platform/ipc.ts:11` (`fileIpc`). Batch ops: `read_directory_batch`, `rename_items`, `delete_items`, `copy_items`, `batch_read_files`, `get_files_metadata`.
- Eksplorasi IPC catalog `src/lib/platform/ipc.ts:10` — 6 namespace: `fileIpc` (17 cmd), `settingsIpc` (5 cmd: `load_global_settings`, `load_workspace_settings`, `save_global_setting`, `save_workspace_setting`, `delete_workspace_setting`), `startupIpc` (3), `gitIpc` (9), `workspaceIpc` (5), `systemIpc` (8). Semua `invoke('snake_case_command')` terketat di catalog; tidak ada scattered `invoke` mentah di luar service.
- Eksplorasi konfigurasi/settings `src/lib/stores/settings.svelte.ts:1` — 2-scope layering `HARDCODED_DEFAULTS:36` ← `rawGlobalSettings` ← `rawWorkspaceSettings` = `effectiveSettings:59`. 15 key (`theme:36`, `font_size`, `font_family`, `tab_size`, `word_wrap`, `line_numbers`, `auto_save`, `auto_save_delay_ms`, `default_encoding`, `icon_theme`, `search_exclude/include`, `default_svg_view`, `default_md_view`, `discord_presence`, `default_shell`, `confirm_delete`). Persistensi via `db.rs:528` — SQLite `pool (r2d2) @ app_data_dir/notron_db.sqlite`, tabel `global_settings (key TEXT PK, value TEXT)` dan `workspace_settings (workspace_id, key) PK`. Load via single `load_startup_state:58` di `lib.rs:81` (menggabungkan global+workspace+ui+session+crash flag). Settings schema registry `src/lib/workbench/settingsRegistry.ts:34` (15 schema terdaftar, per kategori `general/appearance/editor/files/terminal`).
- Eksplorasi state persistence tambahan `db.rs:31` — `critical_state`, `ui_state` (sidebar_width, panel_height, sidebar_visible, expanded_folder_paths, active_sidebar_panel, is_minimap_enabled), `session_state` (open_tabs_json, active_tab_id, scroll_positions, editor_snapshots), `dirty_tab_snapshots`, `workspace_state` legacy.
- Eksplorasi tema `src/lib/themes/index.ts:159` — `THEMES: Record<string,{extension: CodeMirrorTheme, isDark, label}>` (51 tema dari `@uiw/codemirror-themes-all`) + `hcDark`/`hcLight` custom. CSS variables di `applyThemeVariables:303`.
- Eksplorasi bahasa `src/lib/editor/languageRegistry.ts:15` + `file_ops.rs:589` — `LanguageContribution {id, extensions, filenames, load}` + `FILENAME_LANGUAGE_MAP:593` (50+ mapping) + `detect_language` Tauri command (exact filename → extension fallback). Mapping JSON di `src/lib/constants/languages.json` (via `include_str!` di `file_ops.rs:659`).
- Eksplorasi template kosong `packages/notron-sdk/src/` — `index.ts:8` re-export `api`, `types`, `host`; `types/index.ts:5` hanya `Uri/Range/Position/Location`; `api/extensions.ts:7` `Extension{ id, name, version, contributes?, activate }` + `api/events.ts:5` `NotronEvent/TextDocumentEvent` minimal; `host/index.ts:5` `ExtensionContext` stub (throws `not implemented`); `utils/index.ts:5` `joinUri/isString`; `cli/index.ts:10` `createCli()` stub; `test/utils.test.ts:4` 1 placeholder test. Build contract `package.json:22` (`build: tsc -p tsconfig.json`), `tsconfig.json:2` (`emitDeclarationOnly: true`, `strict: true`). Tidak ada kode SDK yang ditulis di fase ini — sesuai kriteria keluar Fase 0.

### Stub / Belum Bisa Diimplementasikan Penuh
- Tidak ada stub SDK di Fase 0 (eksplorasi murni). Namun gap core yang akan memaksa stub di Fase 1–4 teridentifikasi dan dicatat di Catatan Tambahan / tabel Status di bawah.

### File yang Diubah/Ditambah
- `packages/notron-sdk/IMPLEMENTATION_LOG.md` — dibuat (file ini), entry Fase 0.

### Catatan Tambahan
- Verifikasi: `bun run check` dan `cargo check` tidak dijalankan di Fase 0 karena fase ini read-only dan tidak boleh menulis kode; akan dijalankan mulai Fase 1 sesuai roadmap.
- Konvensi yang dipertahankan dari template: `type: module`, export via `*.js` (ESM), `emitDeclarationOnly`, seluruh tipe di `src/types/` & `src/api/`, util di `src/utils/`, host di `src/host/`. Semua struktur ini akan dipakai untuk implementasi Fase 1+ tanpa mengubah `package.json`/`tsconfig.json` kecuali diperlukan.

#### Inventory Command Registry Nyata (ringkas, 87 ID unik — format `namespace.aksi`)

| Kategori | ID | Label | Sumber Core | Capability |
|---|---|---|---|---|
| **Core (baseCommands)** | `workbench.action.newUntitledFile` | New File | `coreCommands.ts:37` | safe |
| | `workbench.action.toggleSidebar` | Toggle Sidebar | `coreCommands.ts:45` | safe |
| | `workbench.action.openSettings` | Settings | `coreCommands.ts:52` | safe |
| | `workbench.action.gotoLine` | Go to Line | `coreCommands.ts:59` | safe |
| | `workbench.action.navigateBack` | Go Back | `coreCommands.ts:66` | safe |
| | `workbench.action.navigateForward` | Go Forward | `coreCommands.ts:74` | safe |
| | `workbench.action.run` / `runCurrentFile` / `stopRun` | Run: * | `coreCommands.ts:82` | `trusted` |
| | `workbench.action.reloadWindow` | Developer: Reload Window | `coreCommands.ts:106` | safe |
| | `workbench.action.closeWindow` / `closeFolder` / `closeWorkspace` | File: Close * | `coreCommands.ts:112` | safe |
| | `workbench.action.explorer.newFile/newFolder/refresh/collapseAll` | Explorer: * | `coreCommands.ts:134` | safe |
| **File** | `workbench.action.files.newUntitledFile` | File: New Untitled File | `fileCommands.ts:22` | safe |
| | `workbench.action.files.newFile` | File: New File… | `fileCommands.ts:23` | safe |
| | `workbench.action.files.openFile` | File: Open File… | `fileCommands.ts:24` | safe (dialog) |
| | `workbench.action.files.openFolder` | File: Open Folder… | `fileCommands.ts:28` | safe |
| | `workbench.action.openRecent` | File: Open Recent… | `fileCommands.ts:36` | safe |
| | `workbench.action.newWindow` / `window.newWindow` | File: New Window | `fileCommands.ts:37` / `windowCommands.ts:16` | safe |
| | `workbench.action.files.save` | File: Save | `fileCommands.ts:41` | `trusted` |
| | `workbench.action.files.saveAs` | File: Save As… | `fileCommands.ts:61` | `dangerous` |
| | `workbench.action.reopenClosedEditor` | View: Reopen Closed Editor | `fileCommands.ts:77` | safe |
| **Editor** | `editor.action.undo` / `redo` / `selectAll` | Edit: * | `editorCommands.ts:25` & `editorActionCommands.ts:12` | safe |
| | `editor.action.copyLinesUp/DownAction` | Copy Line Up/Down | `editorCommands.ts:28` | safe |
| | `editor.action.moveLinesUp/DownAction` | Move Line Up/Down | `editorCommands.ts:30` | safe |
| | `editor.action.find` / `replace` / `actions.find` / `editor.action.startFindReplaceAction` | Find/Replace | `editorCommands.ts:32` | safe |
| | `editor.action.revealDefinition` | Go to Definition | `editorCommands.ts:34` | safe |
| | `editor.action.referenceSearch.trigger` | Find All References | `editorCommands.ts:35` | safe |
| | `workbench.action.debug.run` | Run Code | `editorCommands.ts:36` / `runCommands.ts:14` | `trusted` |
| | `editor.action.clipboardCut/Copy/PasteAction` | Cut/Copy/Paste | `workbenchCommands.ts:93` | safe |
| | `actions.find` | Find | `workbenchCommands.ts:101` | safe |
| **Explorer** | `explorer.newFile/newFolder/refresh/collapseAll` | Explorer: * | `explorerCommands.ts:28` | safe |
| | `explorer.copy/cut/paste` | Copy/Cut/Paste | `explorerCommands.ts:32` | `trusted` (paste) |
| | `explorer.duplicate` | Duplicate | `explorerCommands.ts:35` | `trusted` |
| | `explorer.rename` | Rename | `explorerCommands.ts:36` | `trusted` |
| | `explorer.delete` | Delete | `explorerCommands.ts:37` | `dangerous` |
| | `explorer.copyPath/copyRelativePath/revealInFileManager/openInTerminal/openToSide/toggleDotFiles` | Explorer: * | `explorerCommands.ts:38` | `trusted` (openInTerminal) |
| **Tab/Split** | `workbench.action.closeActiveEditor` | Close Active Editor | `tabCommands.ts:22` | safe |
| | `workbench.action.closeOtherEditors` | Close Other Editors | `tabCommands.ts:46` | safe |
| | `workbench.action.closeEditorsToTheRight` | Close Editors to the Right | `tabCommands.ts:65` | safe |
| | `workbench.action.pinEditor` | Pin Editor | `tabCommands.ts:85` | safe |
| | `workbench.action.splitEditorRight/Down/Left/Up` | Split Editor * | `tabCommands.ts:95` | safe |
| | `workbench.action.focusActiveEditorGroup` | Focus Active Editor Group | `tabCommands.ts:135` | safe |
| | `workbench.action.closeAllEditors` | View: Close All Editors | `workbenchCommands.ts:69` | safe |
| **View/Shell** | `workbench.action.toggleSidebarVisibility` / `toggleSidebar` | View: Toggle Sidebar | `viewCommands.ts:10` / `workbenchCommands.ts:73` | safe |
| | `workbench.view.explorer/search/scm/debug` | View: Show * | `viewCommands.ts:12` | safe |
| | `workbench.action.showCommands` | Show All Commands | `viewCommands.ts:16` | safe |
| | `workbench.action.quickOpen` | Quick Open | `viewCommands.ts:17` | safe |
| | `workbench.action.openSettings` | Preferences: Open Settings | `viewCommands.ts:27` | safe |
| | `workbench.action.toggleMinimap/Breadcrumbs/StickyScroll/StatusbarVisibility` | View: Toggle * | `viewCommands.ts:21` | safe |
| | `workbench.action.findInFiles/replaceInFiles` | Search: * in Files | `viewCommands.ts:25` | safe |
| | `explorer.open/openToSide/workbench.action.showWelcomePage` | Explorer: Open * | `viewCommands.ts:28` | safe |
| | `workbench.action.search.refresh/collapseAll` | Search: * | `searchCommands.ts:9` | safe |
| | `workbench.action.explorer.refresh/collapseAll` (mirror) | Explorer: * | `searchCommands.ts:11` | safe |
| **Terminal** | `workbench.action.terminal.new` | Terminal: Create New Terminal | `terminalCommands.ts:13` | `trusted` |
| | `workbench.action.terminal.toggleTerminal` | View: Toggle Terminal | `terminalCommands.ts:17` | safe |
| | `workbench.action.terminal.killActive` | Terminal: Kill Active Terminal | `terminalCommands.ts:26` | `trusted` |
| | `workbench.action.terminal.clear` | Terminal: Clear Output | `terminalCommands.ts:30` | safe |
| | `workbench.action.terminal.maximize` | Terminal: Maximize Panel | `terminalCommands.ts:33` | safe |
| | `workbench.action.terminal.show` | Terminal: Show Terminal | `terminalCommands.ts:36` | safe |
| | `workbench.actions.view.problems/output` | View: Problems/Output | `terminalCommands.ts:40` | safe |
| **Git** | `git.init` / `refresh` / `stageAll` / `unstageAll` / `commit` / `push` / `pull` / `fetch` / `publish` / `cancel` / `reDetect` / `openOutput` / `stage` / `unstage` / `discard` | Git: * | `gitCommands.ts:14` | `trusted`/`dangerous` (push/pull/fetch/publish/commit/discard) |
| **Run/Debug** | `workbench.action.run` / `runCurrentFile` / `stopRun` / `debug.run` | Run: * | `runCommands.ts:11` | `trusted` |
| | `run.createLaunchJson` / `openLaunchJson` / `openFileForRunning` | Run: * | `runCommands.ts:15` | `trusted` |
| **Preview** | `workbench.action.preview.showCode/showPreview/showSplit` | Preview: Show * | `previewRegistry.ts:130` | safe |
| **Window** | `window.minimize/maximize/close/reload` | * Window | `windowCommands.ts:12` | safe |

> Total deduplicated: ±87. Mapping `actions.find` (tanpa prefix) adalah alias legacy untuk `editor.action.find` — SDK harus menormalisasi alias ini.

#### Inventory Workbench Registries (modular points)

| Registry | File | Isi Kontribusi Nyata |
|---|---|---|
| Activity Bar | `activityBarRegistry.ts:9` | 4 item: `explorer`, `search`, `git`, `run` (+ `extensions` reserved) dengan `order`, `icon`, `getBadgeText` |
| Sidebar | `sidebarRegistry.ts:9` | 4 view: `explorer/search/git/run` (lazy `loadComponent`) |
| Bottom Panel | `bottomPanelRegistry.ts:11` | 3 tab: `terminal/output/problems` (order, badge, `when`) |
| Status Bar | `statusBarRegistry.ts:10` | `StatusBarContribution {id, alignment left/right, priority, getText(), command, when}` — dipakai `statusBarContributions.ts` (git branch, encoding, line/column, dll) |
| Menu | `menuRegistry.ts:13` | container `menubar/file`, `menubar/edit`, `menubar/view`, `explorer/context`, `editor/context`, `tab/context`, `pane/context` — tiap item `command → registry.execute` |
| Context Menu | `contextMenuRegistry.ts` | dipakai `tabCommands.ts:148` untuk `tab/context` + `pane/context` |
| Preview | `previewRegistry.ts:14` | 2 provider: `markdown` (`.md/.markdown`, mode `preview/code/split`) + `svg` (`.svg`, `image/code/split`) |
| SCM | `scmRegistry.ts:9` | 1 provider `git` generic (`loadComponent: SourceControlPanel`) |
| Run Provider | `runRegistry.ts:10` | per-runtime provider (`node/python/go/...`) via `build(resolved,args,ctx)` → `ShellDialect` statements |
| Language | `languageRegistry.ts:7` | per-language CodeMirror extension (`id, extensions, load`) — resolved via `detect_language` |
| Settings Schema | `settingsRegistry.ts:34` | 15 schema `key/type/default/description/category` — sumber render `SettingsPage` |
| Snippet/Task/Walkthrough | `snippetRegistry.ts`, `taskRegistry.ts`, `walkthroughRegistry.ts` | data-driven, masih minimal di core |

#### Draft Tabel Pemetaan Event Core → SDK Event (NOTRON_SDK_03_HOST_RUNTIME §3.2)

| Event SDK | Sumber Event Core (hasil eksplorasi) | Status |
|---|---|---|
| `onDidOpenTextDocument` | Tidak ada Tauri emit khusus; dipicu `eventBus.emit('request-open-file', {path})` di `eventBus.ts:8` + `openFileInActivePane()` (`App.svelte:271`, `fileOperations.ts`) → `editorStore.addTab()` + `invoke('read_file_text')` | Implementasi nyata (bridge via eventBus+store) |
| `onDidCloseTextDocument` | `eventBus.emit('split:request-close-tab', {tabId})` (`eventBus.ts:11`, `App.svelte:287`) + `editorStore.closeTab()` — tidak ada Tauri event | Implementasi nyata |
| `onDidSaveTextDocument` | Command `workbench.action.files.save:invoke('save_file', {path,content})` (`fileService.ts:73`, `fileCommands.ts:41`) + `editorStore.markSaved()` — tidak ada emit save, harus hook save handler | Implementasi nyata |
| `onDidChangeTextDocument` | CodeMirror `updateListener` di `Editor.svelte` → `editorStore.updateContent(tabId, content)` → `window.dispatchEvent('editor:sync-content')` (`editor.ts:279`) — belum event bus, perlu bridge | Implementasi nyata (butuh hook Editor updateListener) |
| `onDidChangeActiveTextEditor` | `editorStore.activeTabId.subscribe()` + `onFocus` di `Editor.svelte` + `eventBus 'editor:focus-group'` (`navigation.ts:152`) — tidak ada emit terpusat | Implementasi nyata |
| `onDidChangeActiveTab` (tabGroups) | `splitStore.panes[paneId].activeTabId` + `editorStore.activeTabId` + `split:request-close-tab` — sumber store Svelte, bukan Tauri | Implementasi nyata |
| `onDidChangeWorkspaceFolders` | `eventBus.emit('request-workspace-switch', {path})` (`eventBus.ts:10`, `App.svelte:262`) + `uiStore.explorerRoot` + IPC `load_startup_state` per workspace | Implementasi nyata |
| `onDidChangeConfiguration` | `settingsStore.updateSetting(key,value,scope)` (`settings.svelte.ts:111`) → `invoke('save_global_setting'/'save_workspace_setting')` (`db.rs:588`) — tidak ada event perubahan yang dipublish, perlu menambah emit `settings-changed` | Stub (SDK bisa wrap `settingsStore`; event belum emit — gap) |
| `onDidChangeActiveColorTheme` | `themeStore.setTheme()` + `applyThemeVariables(theme)` (`themes/index.ts:303`) + `localStorage.getItem(THEME_KEY:19)` — tidak ada emit tema, perlu hook store | Stub → Implementasi nyata ringan (sudah ada `themeStore.isDark` derived) |
| `onDidChangeTextEditorSelection` | CodeMirror `selection` di `Editor.svelte` → `editorStore.setCursor(tabId, {line,column})` — belum dipublish sebagai event SDK | Stub (perlu bridge gutter/selection) |
| `onDidChangeVisibleTextEditors` (split) | `splitStore.rootNode` (SplitView) — belum event, observable via `splitStore` subscribe | Stub → Implementasi nyata ringan |
| `onFileSystemChange` (custom) | Tauri `watcher_service.rs:346` `app.emit('fs-change', FsChangePayload)` + `FileTree.svelte:49` + `App.svelte:844` listener | Implementasi nyata (sudah modular) |
| `onDidChangeGitDecorations` | `git_service.rs:992` `app.emit('git-decorations-changed', DecorationDelta)` → `gitDecoration.ts:59` listener | Implementasi nyata |
| `onDidChangeGitState` (repo) | `git_service.rs:792` `get_repo_state` + polled via `git-status-refresh:346` (`watcher_service.rs` .git watcher → `gitRepo.ts:182` listen debounced 400ms) | Implementasi nyata |
| `onDidOpenTerminal` / `onDidCloseTerminal` | `terminalStore.newTerminal/closeTerminal` (`terminal.ts`) — event belum emit, hanya store mutation | Stub (perlu hook store) |

> Catatan: event `onDid*` SDK yang “Implementasi nyata” di atas belum tentu sudah diekspos sebagai `NotronEventBus.on(...)` — artinya runtime bridge host perlu menyambungkan store/eventBus/Tauri listen tersebut ke `NotronEventBus` (sesuai desain `NOTRON_SDK_03_HOST_RUNTIME §3.1`).

#### Draft Tabel Pemetaan Command SDK → Command/Invoke Core (NOTRON_SDK_03_HOST_RUNTIME §3.3)

| Command SDK (`executeCommand(id)`) | Sumber Core (registry / invoke) | Status |
|---|---|---|
| `commands.registerCommand` / `getCommands` / `executeCommand` | `commandRegistry.register/get/execute` (`registry.ts:52/72/107`) + `toPaletteItems` (`registry.ts:136`) | Implementasi nyata |
| `workbench.action.files.save/saveAs` | `fileCommands.ts:41/61` → `fileService.save()` → `invoke('save_file')` (`file_ops.rs:260`) + `MARK_SAVED` | Implementasi nyata |
| `workbench.action.files.newUntitledFile/newFile/openFile/openFolder` | `fileCommands.ts:22/24/28` → `handleNewTextFile` / `@tauri/plugin-dialog open()` + `openFileInActivePane` | Implementasi nyata |
| `explorer.newFile/newFolder/rename/delete/duplicate/copy/cut/paste` | `explorerCommands.ts:28` → `fileService.createFile/createDirectory/renameItem/deleteItem/copyItem` → `invoke('create_file','rename_item',...)` (`file_ops.rs:389`) | Implementasi nyata |
| `editor.action.*` (undo/redo/selectAll/find/replace/gotoDefinition) | `editorActionCommands.ts:12` → `eventBus.emit('editor:action', {action})` (`eventBus.ts:12`) → CodeMirror extension di `Editor.svelte` | Implementasi nyata (via eventBus, bukan invoke) |
| `workbench.action.*` (toggleSidebar/showExplorer/showSearch/…) | `viewCommands.ts:10` → `uiStore.toggleSidebar/setActiveSidebarPanel` (`ui.ts`) + `window.dispatchEvent('open-command-palette')` | Implementasi nyata |
| `workbench.action.closeActiveEditor/closeOtherEditors/splitEditor*` | `tabCommands.ts:22/46/95` → `splitStore.closeTabInPane/splitPane` + `editorStore.closeTab` | Implementasi nyata |
| `workbench.action.terminal.*` | `terminalCommands.ts:13` → `terminalStore.newTerminal/clearOutput/toggleMaximize` + bottom panel via `bottomPanelRegistry` | Implementasi nyata |
| `git.*` (stage/unstage/commit/push/pull/fetch/publish/clone) | `gitCommands.ts:14` → `gitRepoStore.*` → `gitIpc` (`ipc.ts:66`) → `invoke('git_*')` (`git_service.rs:1146`) + streaming `Channel<GitProgress>` | Implementasi nyata |
| `workbench.action.run/runCurrentFile/stopRun` | `runCommands.ts:11` → `runService.runSelectedConfiguration/runCurrentFile/stopActiveRuns` → `runProviderRegistry.getForType` | Implementasi nyata |
| `workbench.action.preview.showCode/showPreview/showSplit` | `previewRegistry.ts:130` → `previewRegistry.getForFile(path, language)` → `editorStore.updateTab(mdViewMode/svgViewMode)` | Implementasi nyata |
| `workspace.fs.*` (`readFile/writeFile/readDirectory/createDirectory/rename/delete`) | `fileIpc.*` (`ipc.ts:11`) / `fileService.*` (`fileService.ts:66`) → Tauri `invoke('read_file_text','read_directory','create_file',...)` (`file_ops.rs`) | Implementasi nyata (paling fundamental — harus dijembatani 1:1) |
| `workspace.getConfiguration/update` / `onDidChangeConfiguration` | `settingsIpc` (`ipc.ts:37`) → `invoke('load/save_global_settings', 'load/save_workspace_settings')` (`db.rs:577`) + `settingsRegistry.ts:34` schema | Implementasi nyata (CRUD) |
| `window.showInformation/Warning/ErrorMessage` | Tidak ada Tauri dialog terpusat; `notificationService.ts` / `uiStore.addToast()` (toast berbasis `constants.ts:116` MAX_TOASTS) | Implementasi nyata ringan (wrap notificationService) |
| `window.showQuickPick/showInputBox` | `dialogService.ts` + `QuickPickDialog.svelte` / `InputBoxDialog.svelte` + `Palette` store (`palette.ts`) | Implementasi nyata |
| `window.createStatusBarItem` | `statusBarRegistry.register(item)` (`statusBarRegistry.ts:24`, `statusBarContributions.ts`) | Implementasi nyata |
| `window.createOutputChannel` | `terminalStore` output log + `OUTPUT_CATEGORIES:101` (`constants/index.ts:102`) — belum channel terpisah per ekstensi | Stub → Implementasi nyata ringan (butuh host OutputChannel wrapper) |
| `window.activeTextEditor/visibleTextEditors` / `tabGroups` | `editorStore.tabs/activeTabId` + `splitStore.panes/rootNode` — bukan API, langsung Svelte store | Implementasi nyata ringan (wrapper read-only) |
| `window.registerCustomEditorProvider` / `openEditorToSide` / `createWebviewPanel` | Belum ada Custom Editor / Webview di core (hanya `MarkdownPreview.svelte:1`, `ImageViewer.svelte:1`, `DiffEditor.svelte:1`, `SplitView.svelte:1` hardcoded) | Stub (gap prioritas tinggi — butuh registry custom editor generik) |
| `languages.registerCompletionItemProvider/ Hover/DiagnosticCollection` | `commonExtensions.ts`, `extensionRegistry.ts`, `languageRegistry.ts:15` — per-bahasa CodeMirror extension sudah modular tapi belum ada provider API dinamis per ekstensi | Stub bertipe (gap prioritas tinggi — highlight Lezer per bahasa sudah modular, autocomplete/diagnostic belum pluggable runtime) |
| `views.registerTreeDataProvider/registerWebviewViewProvider` | `sidebarRegistry.ts:9` + `activityBarRegistry.ts:9` (viewContainer/view CRUD) sudah registry-driven tapi belum ada `TreeDataProvider<T>/WebviewViewProvider` generik untuk ekstensi | Stub bertipe (perlu host adapter yang render tree ke Sidebar) |
| `menus.evaluateWhenClause` | `when` di `menuRegistry.ts:21`, `commandRegistry.ts:34` sudah pakai `when: () => boolean` (fungsi), bukan expression string — belum `whenClause` evaluator string seperti VS Code | Stub (perlu parser `when` string → fungsi) |
| `theming.getActiveColorTheme/onDidChangeActiveColorTheme` | `themes/index.ts:159` `THEMES` + `settingsStore.effectiveSettings.theme:36` + `themeStore` — belum event | Stub → mudah jadi nyata (hook store) |
| `debug.*` / `tasks.*` / `scm.createSourceControl` / `terminal.createTerminal` / `authentication.*` | `scmRegistry.ts:9` (sudah generik), `runRegistry.ts:10`, `terminal.svelte.ts` — sebagian sudah registry, tapi API SDK belum dijembatani; `snippets` data-driven (`snippets.ts`) | Stub bertipe (sesuai prioritas rendah di `NOTRON_SDK_01_API_SURFACE:217`) |
| `extensions.getExtension/all/onDidChange` | Belum ada ekstensi loader `.ntrn` di core — akan dikelola host SDK sendiri (`host/index.ts` lifecycle, `activate`/`deactivate`, `context.subscriptions`) | Implementasi nyata sisi SDK (tidak butuh core) |

#### Struktur Akses File System yang Sudah Ada (ringkas)

- **Abstraksi**: `IFileService:43` di `fileService.ts:66` (TauriFileService) — satu-satunya jalur UI → FS. Swap via `setFileService()` (`fileService.ts:140`) untuk testing/Mock Host.
- **Tauri commands**: `read_file_text/read_file_chunked/read_file_stream/batch_read_files` (read), `save_file/create_file/create_directory/rename_item(s)/delete_item(s)/copy_item(s)/file_exists/list_all_files/get_files_metadata/get_file_encoding_info` (CRUD) — semua di `file_ops.rs:76` + `ipc.ts:11`.
- **Cache**: `workspace_cache.rs:1` (`WorkspaceCache`) — cache satu level `get_children(path, showDotFiles)` yang dipakai `read_directory/read_directory_batch`. Invalidasi melalui `cache.invalidate/invalidate_parent` pada setiap `save/rename/delete/copy` dan `fs-change` fan-out (`watcher_service.rs:313`).
- **Watcher**: `watcher_service.rs:195` `start_fs_watch(root)` — satu `notify::Watcher` per workspace (NonRecursive per folder, skip `WATCHER_EXCLUDE`), debounced 250ms (`watcher_service.rs:70`), coalesce dedupe `coalesce:182`, fan-out 3 jalur: (1) invalidasi `WorkspaceCache`, (2) `app.emit('fs-change')` → Explorer + open tabs (`App.svelte:844`, `FileTree.svelte:49`), (3) `app.emit('git-status-refresh')` → Source Control. `.git` watcher terpisah (`register_git_keyfiles:106`) untuk status.
- **Chunking**: `CHUNK_THRESHOLD_LOW 512KB / HIGH 5MB / INITIAL 100KB` (`file_ops.rs:68`) — `read_file_chunked` + `read_file_stream(Channel<FileChunk>)` untuk file besar.

#### Struktur Konfigurasi/Settings yang Sudah Ada (ringkas)

- **Source of truth**: `HARDCODED_DEFAULTS:36` di `settings.svelte.ts:36` (15 key, lihat Inventory di atas) → `rawGlobalSettings` → `rawWorkspaceSettings` → `effectiveSettings:59` via `resolveSettings:93` (array merge, bukan replace).
- **Scope**: `global` (semua workspace) + `workspace` (override per `workspaceId`). API `loadAllSettings(workspaceId):66`, `applyLoadedSettings(global,workspace):82` (dipakai `load_startup_state:81`), `updateSetting(key,value,scope):111` (debounced 500ms `SETTINGS_SAVE_DEBOUNCE_MS:27`), `scheduleSave:153` → `invoke('save_global_setting'/'save_workspace_setting')`, `resetToGlobal:176` → `delete_workspace_setting`.
- **Persistensi**: SQLite `global_settings` + `workspace_settings` (`db.rs:128`) via `load_global_settings/load_workspace_settings/save_global_setting/save_workspace_setting/delete_workspace_setting:577` + batch `load_startup_state:81` (satu IPC untuk semua state). Search include/exclude khusus di `search_exclude/include:140` (`addSearchExclude/Remove` per scope, dipakai `ignore_rules.rs` untuk Quick Open & Search).
- **Schema Registry**: `settingsRegistry.ts:34` — `SettingSchema {key, type 'string'|'number'|'boolean'|'enum', default, title, description, enum, category, min/max/step, when}`. 15 entry terdaftar `registerAll:75` (kategori `general/appearance/editor/files/terminal`). `contributes.configuration` (SDK) akan memetakan ke registry ini.

#### Hasil Baca Template `packages/notron-sdk/src/` Saat Ini

| File | Isi Aktual | Konvensi Ditemukan |
|---|---|---|
| `src/index.ts:1` | `export * from './api/index.js'; export * from './types/index.js'; export * from './host/index.js';` | Entry point re-export, ESM via `*.js` |
| `src/types/index.ts:5` | `Uri{ scheme,authority,path,query,fragment }`, `Range{start,end}`, `Position{line,character}`, `Location{uri,range}` | Primitive types minimal, akan diperluas di Fase 1 (manifest types) |
| `src/api/index.ts:1` | `export * from './extensions.js'; export * from './events.js';` | Split per namespace, cocok dengan `NOTRON_SDK_01_API_SURFACE` |
| `src/api/extensions.ts:5` | `Extension{id,name,version,contributes?,activate,deactivate?}`, `ExtensionContributions{commands,menus,keybindings}` + contribution types | Hanya 3 kategori, Fase 1 akan tambah `viewsContainers/views/languages/grammars/themes/snippets/configuration/debuggers/taskDefinitions` |
| `src/api/events.ts:5` | `NotronEvent<T>`, `TextDocumentEvent{uri}`, `WorkspaceEvent{uri}`, `EventEmitter<T>` | Masih sangat minimal; `NotronEventBus` (§3.1) akan menggantikan/memperluas ini |
| `src/host/index.ts:5` | `class ExtensionContext { id, subscriptions: Disposable[], pushDisposable, dispose, workspaceState()/globalState() → throw }` | `Memento`/`SecretStorage`/`OutputChannel` belum ada — akan ditambah Fase 2 sesuai §2 |
| `src/utils/index.ts:5` | `joinUri(base,...parts)`, `isString(value)` | Pure helpers; validasi manifest (`validateManifest`, `validatePermissions`) akan ditambah Fase 1 |
| `src/cli/index.ts:10` | `createCli() → throw 'not implemented'` | CLI entry stub, Fase 5 akan isi `init/build/package/validate/verifyChecksum` |
| `test/utils.test.ts:4` | 1 test `joinUri` (`bun:test`) | Runner `bun:test`, belum ada test manifest/host |
| `package.json:7` / `tsconfig.json:2` | `type: module`, `build: tsc -p tsconfig.json`, `emitDeclarationOnly: true`, `strict: true`, `noUnusedLocals/Params` | Harus dipertahankan; `emitDeclarationOnly` wajib untuk `.d.ts` distribusi (`NOTRON_SDK_02_MANIFEST_TYPES §5`) |

> Tidak ada file tambahan di `src/api/`, `src/cli/`, `src/host/`, `src/types/`, `src/utils/` selain di atas. Semua akan diperluas secara inkremental mulai Fase 1 — tidak ada rename/move di Fase 0.

---

## Fase 1 — Fondasi Tipe & Manifest (selesai: 2026-09-12)

### Selesai
- Implementasi seluruh interface di `src/types/index.ts:1` sesuai `NOTRON_SDK_02_MANIFEST_TYPES.md:1` — `ExtensionManifest` (identitas, engines, extensionDependencies/extensionPack, main, activationEvents, contributes, permissions), `ActivationEvent` (8 varian: `onCommand:`, `onLanguage:`, `onView:`, `onUri`, `onStartupFinished`, `workspaceContains:`, `onCustomEditor:`, `*`), `ExtensionPermission` (7 nilai: `fs:workspace-read`, `fs:workspace-write`, `fs:outside-workspace`, `shell:execute`, `network:fetch`, `clipboard:read`, `clipboard:write`), `ExtensionContributes` beserta 13 sub-tipe: `CommandContribution:4.1`, `MenuItemContribution:4.2`, `KeybindingContribution:4.3`, `ViewContainerContribution:4.4`, `ViewContribution:4.5`, `LanguageContribution:4.6`, `GrammarContribution:4.7` (disesuaikan ke CodeMirror 6/Lezer, bukan TextMate — path menunjuk modul highlighting CM6), `ColorThemeContribution:4.8`, `IconThemeContribution:4.8`, `ProductIconThemeContribution:4.8`, `SnippetContribution:4.9`, `ConfigurationContribution:4.10`, `DebuggerContribution:4.11`, `TaskDefinitionContribution:4.11` + `MenuLocation` (13 lokasi: `commandPalette`, `editor/context`, `editor/title`, `explorer/context`, `view/title`, `view/item/context`, `tab/context`, `menubar/file`, `menubar/edit`, `menubar/view`, `menubar/run`, `menubar/help`, `pane/context`), `ValidationResult`, serta preservasi tipe primitif `Uri`, `Range`, `Position`, `Location`.
- Implementasi util validasi di `src/utils/index.ts:1` — `validateManifest()`, `validatePermissions()`, `validateActivationEvents()`, plus helper `isValidActivationEvent()` — semua mengembalikan `{ valid, errors }` dengan path dot/bracket (`engines.notron`, `contributes.commands[0].command`, `permissions[1]`, `activationEvents[0]`). Validasi mencakup: regex `ID_REGEX` untuk `id` (`publisher.name`), `SEMVER_REGEX`/`SEMVER_RANGE_LOOSE` untuk `version` dan `engines.notron`, cross-check prefix publisher vs id, pengecekan required/non-empty untuk `name`, `version`, `publisher`, `main`, validasi `activationEvents` (allowed patterns + duplicate + minimal 1 item), validasi `permissions` (enum 7 nilai + duplicate), validasi `keywords`/`categories`/`extensionDependencies`/`extensionPack` (array string), serta validasi mendalam seluruh `contributes.*` (commands, menus, keybindings, viewsContainers, views, languages, grammars, themes/iconThemes/productIconThemes, snippets, configuration, debuggers, taskDefinitions) termasuk pengecekan tipe, required field, enum `uiTheme`/`type`, dan `MenuLocation` whitelist.
- Dukungan convenience unwrapping: `validatePermissions(input)` dan `validateActivationEvents(input)` menerima baik array mentah maupun objek manifest-like yang memiliki field `permissions`/`activationEvents` — memudahkan delegasi dari `validateManifest()`.
- Penanganan konflik export `CommandContribution`/`KeybindingContribution` antara `src/types/index.ts` dan `src/api/extensions.ts:22` (template lama) dengan mengubah `src/index.ts:8` menjadi re-export selektif: `export * from './types/index.js'` + `export type { Extension, ExtensionContributions, MenuContribution } from './api/extensions.js'` + alias `ApiCommandContribution`/`ApiKeybindingContribution` — `tsc --noEmit` tetap lolos tanpa mengubah kontrak publik manifest.
- Generate JSON Schema manifest di `src/types/manifest.schema.json:1` (opsional tapi disarankan di `NOTRON_SDK_02_MANIFEST_TYPES.md:5`) — mencakup seluruh field `ExtensionManifest` plus `contributes.*` dengan `pattern` untuk `id`, `version`, `activationEvents`, enum untuk `permissions` dan `MenuLocation`; dapat dipakai `cli validate` dan editor lain untuk validasi `manifest.json`.
- Tulis unit test untuk seluruh util validasi di `test/manifest.test.ts:1` (78 test) + preservasi `test/utils.test.ts:1` (1 test) — total 79 test via `bun:test`. Coverage: `isValidActivationEvent` (6 kasus), `validateActivationEvents` (9 kasus: valid, unwrapping, empty, non-array, non-string, invalid event, duplicate, index reporting), `validatePermissions` (10 kasus: empty, all allowed, single valid, invalid, non-string, duplicate, non-array, manifest unwrapping, undefined), `validateManifest` valid (7 kasus: minimal, semver variants, semver range, all activation types, optional identity, dependencies, full contributes), `validateManifest` invalid required (10 kasus: null, missing/invalid id, mismatch publisher, name/version/publisher/engines/main/activationEvents/permissions), `validateManifest` invalid optional (5 kasus: displayName, keywords, extensionDependencies), `validateManifest` contributes (22 kasus: semua kategori contributes + multiple errors).
- Verifikasi `bunx tsc --noEmit` lolos (exit 0) dan `bun run build` (`tsc -p tsconfig.json`) menghasilkan `.d.ts` di `dist/` (`dist/types/index.d.ts:1`, `dist/utils/index.d.ts:1`, `dist/index.d.ts:1`).
- Verifikasi `bun test` lolos: `79 pass, 0 fail, 149 expect() calls` (2 files).

### Stub / Belum Bisa Diimplementasikan Penuh
- Tidak ada stub fungsional di Fase 1 — seluruh tipe dan validator yang disyaratkan `NOTRON_SDK_02_MANIFEST_TYPES.md` telah diimplementasikan penuh tanpa memerlukan akses core Notron (sesuai aturan read-only di `NOTRON_SDK_00_INDEX.md:1` — Fase 1 tidak butuh menjembatani ke core, hanya definisi tipe + validasi murni).
- `GrammarContribution.path` sengaja dibiarkan generik (path ke modul CodeMirror 6/Lezer, bukan `.tmLanguage.json` TextMate) sesuai catatan di `NOTRON_SDK_02_MANIFEST_TYPES.md:171` — keputusan desain ini dicatat untuk `README` di Fase 6, bukan stub.

### File yang Diubah/Ditambah
- `src/types/index.ts` — rewrite total: tambah `ExtensionManifest`, `ActivationEvent`, `ExtensionPermission`, `ExtensionContributes` + 13 sub-tipe + `MenuLocation` + `ValidationResult` (sambil preservasi `Uri/Range/Position/Location`).
- `src/types/manifest.schema.json` — baru, JSON Schema draft-07 untuk `ExtensionManifest` (opsional, disarankan §5).
- `src/utils/index.ts` — rewrite: preservasi `joinUri`/`isString`, tambah `isValidActivationEvent`, `validateActivationEvents`, `validatePermissions`, `validateManifest` + helpers internal (`isRecord`, `isNonEmptyString`, `isValidSemver`, `isValidSemverRange`, `ALLOWED_PERMISSIONS`, `ALLOWED_MENU_LOCATIONS`, `ID_REGEX`, dll).
- `src/index.ts` — diubah: re-export selektif untuk menghindari duplikat `CommandContribution`/`KeybindingContribution` antara `types` dan `api/extensions.ts`.
- `test/manifest.test.ts` — baru, 78 unit test validasi (bun:test) mencakup seluruh util Fase 1.
- `test/utils.test.ts` — tetap (placeholder joinUri), tidak diubah.
- `dist/` — hasil `bun run build` (`.d.ts` ter-generate, tidak di-commit tapi diverifikasi).
- `IMPLEMENTATION_LOG.md` — entry Fase 1 (file ini).

### Catatan Tambahan
- Verifikasi kriteria keluar Fase 1: `bunx tsc --noEmit` exit 0, `bun test` 79 pass — keduanya dijalankan dan lolos di workdir `packages/notron-sdk`.
- Tidak ada perubahan di luar `packages/notron-sdk` (read-only core dihormati): eksplorasi core hanya sebagai referensi (inventory Fase 0 dipakai untuk memetakan `MenuLocation` dan memastikan `GrammarContribution` tidak meniru TextMate).
- `MenuLocation` diperluas dari 7 nilai di `NOTRON_SDK_01_API_SURFACE.md:151` menjadi 13 nilai (tambah `menubar/file`, `menubar/edit`, `menubar/view`, `menubar/run`, `menubar/help`, `pane/context`) agar selaras dengan container nyata di `src/lib/workbench/menuRegistry.ts:11` (`menubar/*`, `pane/context`) — tidak breaking, hanya superset, menjaga kompatibilitas dengan kontribusi core yang sudah ada.
- `validatePermissions` dan `validateActivationEvents` dirancang idempotent terhadap pemanggilan dengan objek manifest (unwrapping) — memudahkan `validateManifest` mendelegasikan tanpa duplikasi error path; perilaku ini diuji eksplisit.
- `dist/` tetap `emitDeclarationOnly: true` sesuai kontrak `package.json:22` dan `tsconfig.json:2`; tidak ada perubahan `package.json`/`tsconfig.json` di Fase 1.

**Kriteria keluar Fase 1: Tercapai.**
- `tsc --noEmit` lolos tanpa error (exit 0, verified).
- Semua unit test validasi manifest lolos, termasuk kasus invalid (79 pass, 0 fail, mencakup valid/invalid untuk `validateManifest`, `validatePermissions`, `validateActivationEvents` + `isValidActivationEvent`).

---

## Fase 2 — API Inti (Prioritas Tinggi) + Host Bridge Dasar (selesai: 2026-09-12)

Rujukan wajib: `NOTRON_SDK_01_API_SURFACE.md:1-3` (commands, window, workspace) + `NOTRON_SDK_03_HOST_RUNTIME.md:1-5` (lifecycle, ExtensionContext, event bus, error isolation) + `NOTRON_SDK_06_ROADMAP_PHASING.md: Fase 2`.

### Selesai

- **types plumbing** — buat `src/api/types.ts:1` sebagai single source of truth untuk seluruh tipe Fase 2: `Disposable` + `toDisposable`/`combinedDisposable`, `Event<T>`/`Emitter<T>` (error isolation per listener), `FileType` enum, `WorkspaceFolder`, `ConfigurationChangeEvent`, `TextDocument`/`TextEditor`, `Tab`/`TabGroup`, `StatusBarItem`, `OutputChannel`, `Webview`/`WebviewPanel`, `TreeItem`, `FileSystemProvider`, plus `QuickPickItem`/`QuickPickOptions`/`InputBoxOptions`/`Progress`.
- **errors & isolation** — buat `src/host/errors.ts:1` dengan hierarki `NotronError` → `NotImplementedError` (stub bertipe lengkap per `NOTRON_SDK_00_INDEX.md:2`, pesan jelas + code `NOT_IMPLEMENTED`), `ExtensionActivationError`, `ExtensionRuntimeError`, plus helper `withErrorIsolation`/`safeInvoke` yang membungkus setiap titik masuk ekstensi (activate/deactivate/command handler/event callback) — memastikan satu ekstensi gagal tidak menghentikan host atau ekstensi lain (03 §5).
- **Event bus** — buat `src/host/eventBus.ts:1` dengan `NotronEventBus` (`emit`/`on`/`onTyped`/`clear`/`count` + per-listener try/catch) sesuai `NOTRON_SDK_03_HOST_RUNTIME.md:3.1`. Tambah `SDK_EVENTS` (15 nama event SDK) dan helper `bridgeExternalEvent`/`toEvent` untuk menjembatani Tauri `listen` / `window.EventTarget` ke bus (dipakai host saat injeksi delegate).
- **Memento / SecretStorage** — buat `src/host/memento.ts:1` dengan interface `Memento` (CRUD penuh: `get`/`update`/`keys`, delete via `update(key, undefined)`), `SecretStorage` (§4), implementasi `InMemoryMemento` (dengan optional `MementoPersistence` file-backed) dan `InMemorySecretStorage` (peringatan `console.warn` sekali bahwa belum encrypted — sesuai catatan §4). Tambah `createFileMementoPersistence` untuk lokasi final `<app_data>/extensions-state/...` (gap dicatat, untuk sekarang in-memory fallback).
- **commands (§1)** — buat `src/api/commands.ts:1` dengan `registerCommand(id, handler, extensionId?) → Disposable` (throw jika ID sudah terdaftar ekstensi lain, allow re-register oleh extension sama), `executeCommand<T>(id, ...args) → Promise<T|undefined>` (lookup lokal → delegate ke core `commandDelegate` jika tidak ditemukan, error isolation via `ExtensionRuntimeError`), `getCommands(filterInternal?) → Promise<string[]>` (merge extension registry + delegate/core `KNOWN_CORE_COMMANDS` deduplicated), serta `setCommandDelegate`/`getCommandDelegate` untuk injeksi host. Inventory 87 ID unik dari Fase 0 direplikasi sebagai `KNOWN_CORE_COMMANDS` untuk discoverability saat tanpa delegate (standalone test). Ditambah introspection `__getRegisteredIds`/`__clearRegistry`/`__isKnownCoreCommand` untuk test.
- **window — notifikasi & dialog (§2.1)** — buat `src/api/window.ts:1` bagian notifikasi (`showInformationMessage`/`showWarningMessage`/`showErrorMessage` → delegate `windowDelegate.show*Message` jika ada, else fallback in-memory `shownMessages` queue + `__getShownMessages()` untuk test). Dialog (`showQuickPick`/`showInputBox` → delegate `dialogService` jika ada, else `undefined` (cancelled) — tidak silently fail; `withProgress` → delegate `dialogService.withProgress` / `uiStore.addProcessToast` jika ada, else no-op progress). Semua melempar typed error jika dipanggil tanpa delegate? Tidak — fallback no-op dengan log, memenuhi aturan stub-vs-nyata (00_INDEX §2): fallback in-memory adalah implementasi nyata ringan, bukan stub.
- **window — status bar CRUD (§2.2)** — `createStatusBarItem(alignment?, priority?) → StatusBarItem` dengan `text`/`tooltip`/`command`/`priority`/`alignment` + `show()`/`hide()`/`dispose()` (per table di 01 §2.2). In-memory `StatusBarItemImpl` + `Map` registry; jika `windowDelegate.createStatusBarItemInternal` ada (bridges ke `statusBarRegistry.register` di `src/lib/workbench/statusBarRegistry.ts:24`), delegasikan. Introspection `__getStatusBarItems()`/`__clearWindowState()` untuk test. Prioritas `right`/`left` + `priority` sorting diserahkan ke host bila delegate ada.
- **window — output channel (§2.3)** — `createOutputChannel(name) → OutputChannel` dengan `append`/`appendLine`/`clear`/`show`/`hide`/`dispose`. Fallback `OutputChannelImpl` (buffer string, `__getOutputChannelText(name)` untuk test). Dipakai juga untuk `ExtensionContext.logChannel` dan `ExtensionHost.hostLog` (kategori `OUTPUT_CATEGORIES: Extension Host`).
- **window — tab/editor groups (§2.4 + §2.7)** — `tabGroups.all`/`activeTab`/`openTab(uri, {viewColumn, preview?}) → Promise<Tab>`, plus `Tab` methods `close()`/`pin()`/`move(toGroup, index?)` dan events `onDidChangeActiveTab`/`onDidChangeTabGroups` (via `Emitter`). Fallback stores `tabGroupsStore` (array `TabGroup`) + `activeTabStore`; `openTab` sinkronkan `activeTextEditor`/`visibleTextEditors`. `activeTextEditor`/`visibleTextEditors`/`onDidChangeActiveTextEditor` diekspos di `window` namespace (per 01 §2.7). Jika `windowDelegate.tabGroups` ada (bridges ke `splitStore` + `editorStore`), delegasikan.
- **window — custom editor & webview stubs (§2.5 + §2.6)** — `registerCustomEditorProvider`/`openEditorToSide`/`createWebviewPanel`/`registerWebviewViewProvider` diimplementasikan sebagai **stub bertipe lengkap** yang `throw new NotImplementedError` dengan detail gap (00_INDEX §2) — karena Notron core hanya punya `MarkdownPreview`/`ImageViewer`/`DiffEditor` hardcoded (`previewRegistry.ts:14`, `SplitView`) dan belum ada registry generik custom editor / webview CSP (`NOTRON_MODULARITY_MAP` Batasan #4). Signature tetap final, dicatat di tabel stub.
- **workspace — fs CRUD (§3.3)** — buat `src/api/workspace.ts:1` `workspace.fs` dengan `readFile`/`writeFile`/`readDirectory`/`createDirectory`/`delete`/`rename`/`stat` (semua `Promise`, `Uint8Array` untuk file, `FileType` untuk directory). Implementasi dual: jika `workspaceDelegate.fs` ada (bridges ke `fileService`/`fileIpc` → `invoke('read_file_text','save_file','read_directory',…)` di `file_ops.rs:76`), delegasikan; else fallback in-memory tree `Map<string, FsEntry>` (mendukung `fsRoot`, `ensureParent`, `normalizePath`). Semua error typed (not found / not a file / not empty). `onFileSystemChange` event dipancarkan dari setiap mutasi fallback dan dari bridge `watcher_service.rs:346` `fs-change` saat delegate ada.
- **workspace — configuration (§3.4)** — `getConfiguration(section?) → WorkspaceConfiguration` dengan `.get<T>(key, default?)`/`.has(key)`/`.update(key, value, target?)` dan `onDidChangeConfiguration: Event<ConfigurationChangeEvent>` (`affectsConfiguration(section)`). Fallback `configStore` (Map) dengan defaults `theme/system`, `font_size`, dll mirroring `HARDCODED_DEFAULTS` (`settings.svelte.ts:36`); `update` memicu `configEmitter` yang delegate-aware (`workspaceDelegate.onDidChangeConfigurationEmitter` jika ada, else lokal). Jika delegate ada, `getConfiguration` delegasikan ke `settingsStore.settingsIpc` (`db.rs:577`, `settingsRegistry.ts:34`). Section prefix `editor.tabSize` → `editor.*` ditangani.
- **workspace — document/workspace events (§3.2 + §3.1)** — `onDidOpenTextDocument`/`onDidCloseTextDocument`/`onDidSaveTextDocument`/`onDidChangeTextDocument` (via `Emitter<TextDocument>`), `onDidChangeWorkspaceFolders` (via `Emitter<WorkspaceFolder[]>`), dipancarkan lewat helper `__fireDidOpen/Close/Save/Change` dan `__setWorkspaceFolders`. Ketika host diinjeksikan, bridge dari `eventBus.emit('request-open-file')`/`split:request-close-tab`/`editor:action` (`eventBus.ts:7`) dan `watcher_service.rs: fs-change` dapat memforward ke bus ini (lihat tabel event final di bawah).
- **workspace — fs provider stub (§3.5)** — `registerFileSystemProvider(scheme, provider)` didefinisikan sebagai **stub bertipe** (`throw NotImplementedError`) — prioritas rendah per 01 §3.5, core belum punya routing scheme virtual.
- **host: ExtensionContext (§2)** — buat `src/host/context.ts:1` `ExtensionContext` lengkap: `subscriptions: Disposable[]`, `extensionPath`/`extensionUri` (default `/extensions/<id>` + `file://`), `globalState: Memento`, `workspaceState: Memento`, `secrets: SecretStorage`, `logChannel: OutputChannel` (per-extension `Extension:<id>`), `extensionId`/`id`, `pushDisposable`, `dispose()` LIFO (reverse loop, per §1 step 7). Konstruktor menerima `string|ExtensionContextOptions` untuk kompatibilitas legacy `host/index.ts:5`. Mempreservasi stub lama `workspaceState()`/`globalState()` → throw dengan pesan migrasi ke property Memento.
- **host: lifecycle (§1 + §6)** — buat `src/host/lifecycle.ts:1` dengan `ExtensionModule`/`ResolvedExtension`, `topologicalSort` (DFS + cycle detection + missing deps), `isCompatible` (lenient semver range: `^`/`~`/`>=`/`>`/`<=`/`<`/`=`/`x` wildcard — untuk `engines.notron`), dan `ExtensionHost` yang mengelola: `register(manifests, modules)` (sort, catat circular/missing ke `hostLog`), `activateAll()` (cek deps gagal → skip, cek `isCompatible` → skip + log, `try/catch` per ekstensi → `ExtensionActivationError`, continue), `deactivateAll()` (reverse order, `try/catch` per `deactivate()` → `ExtensionRuntimeError`, LIFO dispose subscriptions), `getExtension`/`allExtensions`/`eventBus`/`logChannel`/`dispose()`. Error isolation di semua titik masuk (§5): `activate`/`deactivate`/command/event.
- **host entry** — rewrite `src/host/index.ts:1` sebagai re-export terpusat: `ExtensionContext`, `Memento`/`SecretStorage`, `NotronEventBus`/`SDK_EVENTS`, `NotronError`/`NotImplementedError`/`ExtensionActivationError`/`ExtensionRuntimeError`/`withErrorIsolation`, `ExtensionHost`/`topologicalSort`/`isCompatible`, plus helper `createHost()`/`createDefaultEventBus()`.
- **api & SDK surface** — update `src/api/index.ts:1` untuk re-export `types`/`commands`/`window`/`workspace` (selain `extensions`/`events`); update `src/index.ts:8` untuk re-export namespaced `commands`/`window`/`workspace` (alias `windowNamespace`/`workspaceNamespace`/`workspaceFs`) di atas `types`/`host`, preservasi alias `ApiCommandContribution` avoidance.
- **hello-world smoke test** — buat `examples/hello-world/manifest.json:1` (id `acme.hello-world`, `onCommand:acme.helloWorld` + `onStartupFinished`, contributes command, permissions `fs:workspace-read/write`), `examples/hello-world/src/extension.ts:1` (activate: increment `globalState.helloCount`, read `workspace.getConfiguration().get('theme')`, create `OutputChannel Hello World` + `StatusBarItem $(smiley) Hello` + subscriptions untuk `onDidOpenTextDocument`/`onDidSaveTextDocument`/`onDidChangeConfiguration`/`tabGroups.onDidChangeActiveTab`, register `acme.helloWorld` yang `showInformationMessage` + `workspace.fs.writeFile/readFile` + `tabGroups.openTab`, verifikasi `getCommands()`), `examples/hello-world/package.json` (file dep `notron-sdk`), `examples/hello-world/README.md` (api coverage + dev loop).
- **tests** — buat `test/host.test.ts:1` dengan 40 test (total suite kini 119): `topologicalSort` (sort before dependents, circular, missing), `isCompatible` (accept/reject), `ExtensionHost lifecycle` (order, isolate activation error, skip due to dependency failure, circular blocked, LIFO dispose, deactivate isolation, exports, version incompat), `Memento`/`SecretStorage`, `NotronEventBus` (emit/on/dispose/isolation/count), `commands` (register/execute, duplicate throw, same-ext re-register, getCommands core+extra, delegate forwarding, handler error → ExtensionRuntimeError), `window` (notifications capture, status bar CRUD, output channel, withProgress, tabGroups open/pin/move/close + events, activeTextEditor, custom editor stubs → NotImplementedError), `workspace` (fs CRUD + onFileSystemChange, config get/has/update + onDidChangeConfiguration, workspaceFolders event, document events, registerFileSystemProvider stub, section prefix), `hello-world smoke` (activate via `ExtensionHost`, execute command, assert status bar + output channel, deactivate clears).

### Stub / Belum Bisa Diimplementasikan Penuh

Semua stub di bawah diimplementasikan sebagai **fungsi/kelas bertipe lengkap** yang `throw new NotImplementedError(feature, detail)` dengan pesan jelas per `NOTRON_SDK_00_INDEX.md:2` (bukan diam-diam dilewati / `return undefined` silent):

- `window.registerCustomEditorProvider(viewType, provider, options?)` — alasan: Notron core hanya punya 2 preview provider hardcoded (`markdown` untuk `.md`, `svg` untuk `.svg` di `previewRegistry.ts:14`) + `SplitView`/`DiffEditor`/`ImageViewer`, belum ada registry generik `CustomEditorProvider`/`WebviewPanel` dengan CSP isolation (`NOTRON_MODULARITY_MAP.md` Batasan #4). Implementasi nyata menunggu `window` Fase 3 + core menyediakan host webview. Status: **Stub bertipe** (signature final).
- `window.openEditorToSide(uri, viewType?)` — alasan: `SplitView.svelte:1` dan `split:request-close-tab` ada, tapi routing `viewType → CustomEditorProvider` belum ada. Status: **Stub bertipe**.
- `window.createWebviewPanel(viewType, title, showOptions, options?)` — alasan: belum ada webview host process / CSP (`NOTRON_MODULARITY_MAP` Batasan #4). Status: **Stub bertipe**.
- `window.registerWebviewViewProvider(viewId, provider)` — alasan: `sidebarRegistry.ts:9`/`activityBarRegistry.ts:9` sudah registry-driven, tapi belum ada `WebviewViewProvider` generik untuk ekstensi (hanya `SourceControlPanel` untuk `scmRegistry`). Status: **Stub bertipe**.
- `workspace.registerFileSystemProvider(scheme, provider)` — alasan: prioritas rendah per `NOTRON_SDK_01_API_SURFACE.md:3.5` (virtual FS untuk remote/in-memory). Core hanya punya `TauriFileService` (`fileService.ts:66`) + `fileIpc` (`ipc.ts:11`) untuk FS lokal. Status: **Stub bertipe** (tipe `FileSystemProvider` sudah final).
- Event `onDidChangeActiveColorTheme` — alias `theming.getActiveColorTheme`/`onDidChangeActiveColorTheme` (01 §7) belum diekspos sebagai API publik di Fase 2; `NotronEventBus` sudah punya `SDK_EVENTS.DID_CHANGE_COLOR_THEME` internal, tapi belum ada `workspace`/`window` getter+event publik. Implementasi menunggu Fase 3 (theming) + bridge ke `themeStore.setTheme()`/`applyThemeVariables()` + `THEMES` (`themes/index.ts:159`). Status: **Stub** (event bus ready, API publik belum).
- Event `onDidChangeTextEditorSelection` — alasan: CodeMirror `selection` di `Editor.svelte` → `editorStore.setCursor` belum dipublish sebagai SDK event; SDK sudah punya `TextEditor.selection` tipe tapi belum ada emitter publik. Status: **Stub** (akan dijembatani di Fase 3 languages).
- Event `onDidChangeVisibleTextEditors` (split) — alasan: `splitStore.rootNode` observable belum di-bridge ke SDK emitter publik; `window.visibleTextEditors` sudah ada (read dari fallback) tapi event perubahan belum. Status: **Stub ringan** (store ready, bridge belum).
- Git-related `onDidChangeGitDecorations` / `onDidChangeGitState` / `onFileSystemChange` custom Git — alasan: Rust `git_service.rs:992` `git-decorations-changed` + `watcher_service.rs:346` `git-status-refresh` sudah ada (Fase 0), tapi belum ada `scm` namespace publik di SDK (prioritas rendah, Fase 4). `NotronEventBus` sudah punya `DID_CHANGE_GIT_DECORATIONS/STATE` internal. Status: **Stub** (event core ada, API SDK `scm` menunggu Fase 4).
- `onDidOpenTerminal` / `onDidCloseTerminal` — alasan: `terminal.ts` store ada tapi belum ada emit event; `terminal` namespace prioritas rendah (01 §13). Status: **Stub**.

> Semua non-stub di atas adalah **implementasi nyata** (atau fallback in-memory yang bridgeable) — tidak ada API Fase 2 yang dilewati tanpa pembahasan. Fallback in-memory (fs, config, tabGroups, notifications) sengaja bukan stub: mereka memenuhi kontrak tipe & bisa di-bridge ke delegate nyata saat `setCommandDelegate`/`setWindowDelegate`/`setWorkspaceDelegate` dipanggil oleh host Notron.

### File yang Diubah/Ditambah

- `src/api/types.ts` — **baru**, 251 baris: primitives Fase 2 (Disposable, Event, Emitter, FileType, WorkspaceFolder, Tab, StatusBarItem, OutputChannel, dll).
- `src/host/errors.ts` — **baru**, error hierarchy + isolation helpers.
- `src/host/eventBus.ts` — **baru**, `NotronEventBus` + `SDK_EVENTS`.
- `src/host/memento.ts` — **baru**, `Memento`/`SecretStorage` + file persistence stub.
- `src/api/commands.ts` — **baru**, ~320 baris, commands namespace + delegate + known core list.
- `src/api/window.ts` — **baru**, ~560 baris, window namespace (notifications/dialogs/status bar/output/tabs/editor + typed stubs).
- `src/api/workspace.ts` — **baru**, ~390 baris, workspace namespace (fs/config/document events + typed stub).
- `src/host/context.ts` — **baru**, `ExtensionContext` lengkap (LIFO dispose).
- `src/host/lifecycle.ts` — **baru**, ~430 baris, `ExtensionHost` + `topologicalSort`/`isCompatible`.
- `src/host/index.ts` — **rewrite total** (dari stub 25 baris → re-export terpusat 45 baris, delegasi ke `context`/`memento`/`eventBus`/`errors`/`lifecycle`).
- `src/api/index.ts` — **diubah** (tambah re-export `types`/`commands`/`window`/`workspace`).
- `src/index.ts` — **diubah** (tambah re-export namespaced `commands`/`window`/`workspace` + `Api*` aliases).
- `examples/hello-world/manifest.json` — **baru** (valid minimal + contributes command + permissions).
- `examples/hello-world/src/extension.ts` — **baru**, smoke test activate (Memento, config, output, status bar, document/config/tab events, register `acme.helloWorld`, verify `getCommands`).
- `examples/hello-world/package.json` — **baru** (file dep `notron-sdk`).
- `examples/hello-world/README.md` — **baru** (coverage table + dev loop).
- `test/host.test.ts` — **baru**, 40 test lifecycle/error isolation/commands/window/workspace/hello-world smoke (bun:test).
- `test/manifest.test.ts` — **tetap** (78 test Fase 1, tidak diubah).
- `test/utils.test.ts` — **tetap** (1 test, tidak diubah).
- `dist/` — hasil `bun run build` ter-generate ulang (tambahan `.d.ts` untuk `api/types`, `api/commands`, `api/window`, `api/workspace`, `host/context`, `host/errors`, `host/eventBus`, `host/lifecycle`, `host/memento`).
- `IMPLEMENTATION_LOG.md` — entry Fase 2 (file ini).

### Catatan Tambahan

- Verifikasi: `bunx tsc --noEmit` exit 0 (strict `noUnusedLocals/Params` lolos — semua import dipakai atau dihapus). `bun run build` menghasilkan `dist/` 15 file `.d.ts` tambahan tanpa error.
- Verifikasi: `bun test` — **119 pass, 0 fail, 233 expect()** (3 files: `utils.test.ts` 1 + `manifest.test.ts` 78 + `host.test.ts` 40). Termasuk smoke `examples/hello-world` yang diaktifkan via `ExtensionHost` dan menjalankan `acme.helloWorld` command tanpa error.
- Host lifecycle error isolation diverifikasi eksplisit: aktivasi gagal satu ekstensi tidak menghentikan ekstensi lain; deactivate throw tidak menghentikan deactivate lain; event listener throw tidak menghentikan listener lain; command handler throw dibungkus `ExtensionRuntimeError`.
- `ExtensionContext.dispose()` LIFO diverifikasi via test `disposed: ['second','first']` — sesuai spec §1 step 7 (reverse subscriptions).
- `topologicalSort` diverifikasi: `acme.a → acme.b` diurutkan `b` dulu; circular `[a->b->a]` terdeteksi; missing dep dilaporkan; `isCompatible` `^0.1.0` vs `0.1.5` pass, `^0.2.0` vs `0.1.0` fail.
- Delegate pattern: `setCommandDelegate`/`setWindowDelegate`/`setWorkspaceDelegate` memungkinkan SDK berjalan **standalone** (in-memory fallback untuk test/smoke) dan **bridged** di dalam Notron (host injects `commandRegistry.execute`, `statusBarRegistry.register`, `fileService.*`, `settingsStore.*`, `dialogService.*`). Tidak ada perubahan di luar `packages/notron-sdk` (read-only core dihormati — semua bridging hanya via interface injeksi, bukan import langsung `src/lib/...`).
- Perbedaan dengan Fase 0 draft: event `onDidChangeConfiguration` semula "Stub (gap: belum emit)" — kini **implementasi nyata** via `configEmitter` + delegate ke `settingsStore`/`db.rs`; `onDidChangeActiveColorTheme` tetap stub (theming Fase 3); `outputChannel` semula "Stub → ringan" — kini implementasi nyata ringan (fallback + bridge).
- Tidak ada perubahan `package.json`/`tsconfig.json` (tetap `emitDeclarationOnly: true`, `type: module`, `strict: true`).

#### Tabel Pemetaan Event Core → SDK Event (Final Fase 2)

| Event SDK | Sumber Event Core (hasil eksplorasi) | Status Final Fase 2 |
|---|---|---|
| `onDidOpenTextDocument` | `eventBus.emit('request-open-file', {path})` (`eventBus.ts:8`) + `openFileInActivePane()` (`App.svelte:271`, `fileOperations.ts`) → `editorStore.addTab()` + `invoke('read_file_text')` — tidak ada Tauri emit | **Implementasi nyata ringan** — SDK `workspace.onDidOpenTextDocument` via `Emitter<TextDocument>` + helper `__fireDidOpen`; host menjembatani `eventBus.on('request-open-file') → __fireDidOpen` saat `setWorkspaceDelegate` ada; fallback in-memory untuk test |
| `onDidCloseTextDocument` | `eventBus.emit('split:request-close-tab', {tabId})` (`eventBus.ts:11`, `App.svelte:287`) + `editorStore.closeTab()` — tidak ada Tauri event | **Implementasi nyata ringan** — `__fireDidClose` + bridge `eventBus.on('split:request-close-tab')` |
| `onDidSaveTextDocument` | Command `workbench.action.files.save: invoke('save_file')` (`fileService.ts:73`) + `editorStore.markSaved()` — tidak ada emit save, hook save handler | **Implementasi nyata ringan** — `__fireDidSave` + bridge dari `fileService.save()` wrapper saat delegate ada |
| `onDidChangeTextDocument` | CodeMirror `updateListener` → `editorStore.updateContent` → `window.dispatchEvent('editor:sync-content')` (`editor.ts:279`) — belum event bus, perlu bridge | **Implementasi nyata ringan** — `__fireDidChange` + bridge `window.addEventListener('editor:sync-content') → __fireDidChange`; error isolation per listener |
| `onDidChangeActiveTextEditor` | `editorStore.activeTabId` + `Editor.svelte:onFocus` + `eventBus 'editor:focus-group'` (`navigation.ts:152`) | **Implementasi nyata ringan** — `window.onDidChangeActiveTextEditor` via `activeEditorEmitter`; host dapat injeksi via `__setActiveTextEditor` (bridges ke `editorStore` subscribe) |
| `onDidChangeActiveTab` (`tabGroups`) | `splitStore.panes[active].activeTabId` + `editorStore.activeTabId` + `split:request-close-tab` — store, bukan Tauri | **Implementasi nyata ringan** — `window.tabGroups.onDidChangeActiveTab` via `activeTabEmitter` + in-memory `TabGroup[]`; host dapat injeksi `windowDelegate.tabGroups` |
| `onDidChangeWorkspaceFolders` | `eventBus.emit('request-workspace-switch', {path})` (`eventBus.ts:10`, `App.svelte:262`) + `uiStore.explorerRoot` + `load_startup_state` | **Implementasi nyata ringan** — `workspace.onDidChangeWorkspaceFolders` + `__setWorkspaceFolders`; bridge via `eventBus.on('request-workspace-switch') → __setWorkspaceFolders` |
| `onDidChangeConfiguration` | `settingsStore.updateSetting(key,value,scope)` (`settings.svelte.ts:111`) → `invoke('save_global_setting')` (`db.rs:588`) — tidak ada emit perubahan | **Implementasi nyata** — `workspace.onDidChangeConfiguration` via `configEmitter` (fallback) atau `workspaceDelegate.onDidChangeConfigurationEmitter` (bridges ke `settingsStore` subscribe); `affectsConfiguration` diimplementasikan |
| `onDidChangeActiveColorTheme` | `themeStore.setTheme()` + `applyThemeVariables(theme)` (`themes/index.ts:303`) + `localStorage THEME_KEY` — tidak ada emit tema | **Stub** — `NotronEventBus.SDK_EVENTS.DID_CHANGE_COLOR_THEME` internal ready, tapi API publik `theming.getActiveColorTheme`/`onDidChangeActiveColorTheme` menunggu Fase 3 (theming namespace). Mudah jadi nyata (hook store) — dicatat gap prioritas menengah |
| `onDidChangeTextEditorSelection` | CodeMirror selection → `editorStore.setCursor(tabId, {line,column})` — belum dipublish | **Stub** — tipe `TextEditor.selection` sudah ada di `api/types.ts`, tapi emitter publik belum diekspos. Menunggu Fase 3 `languages` / editor state full |
| `onDidChangeVisibleTextEditors` (split) | `splitStore.rootNode` (SplitView) — observable via subscribe, belum event | **Stub ringan** — `window.visibleTextEditors` getter sudah nyata (fallback store), event perubahan belum. Mudah jadi nyata (subscribe `splitStore`) |
| `onFileSystemChange` (custom) | Tauri `watcher_service.rs:346` `app.emit('fs-change', FsChangePayload)` + `FileTree.svelte:49` + `App.svelte:844` listener | **Implementasi nyata ringan** — `workspace.onFileSystemChange` + `workspace.fs` mutasi fallback memancarkan; delegate ada → bridge `listen('fs-change') → fsChangeEmitter.fire` |
| `onDidChangeGitDecorations` | `git_service.rs:992` `app.emit('git-decorations-changed', DecorationDelta)` → `gitDecoration.ts:59` | **Stub** — event core ada, tapi API SDK `scm` menunggu Fase 4 (prioritas rendah). `SDK_EVENTS.DID_CHANGE_GIT_DECORATIONS` internal ready |
| `onDidChangeGitState` (repo) | `git_service.rs:792` `get_repo_state` + polled via `git-status-refresh:346` (`watcher_service.rs` .git watcher → `gitRepo.ts:182` listen debounced 400ms) | **Stub** — sama, menunggu `scm` Fase 4; `SDK_EVENTS.DID_CHANGE_GIT_STATE` internal ready |
| `onDidOpenTerminal` / `onDidCloseTerminal` | `terminalStore.newTerminal/closeTerminal` (`terminal.ts`) — belum emit, hanya mutation | **Stub** — menunggu `terminal` Fase 4 (prioritas rendah); `SDK_EVENTS.DID_OPEN/CLOSE_TERMINAL` internal ready |

#### Tabel Pemetaan Command SDK → Command/Invoke Core (Final Fase 2)

| Command SDK (`executeCommand(id)`) | Sumber Core (registry / invoke) | Status Final Fase 2 |
|---|---|---|
| `commands.registerCommand` / `getCommands` / `executeCommand` | `commandRegistry.register/get/execute` (`registry.ts:52/72/107`) + `toPaletteItems:136` + `executeCommand(id)` helper `registry.ts:150` | **Implementasi nyata** — SDK `src/api/commands.ts` memiliki registry lokal; saat `setCommandDelegate` dipasang, `executeCommand` delegasi ke `commandRegistry.execute` nyata; `getCommands` merge lokal+core |
| `workbench.action.files.save/saveAs` | `fileCommands.ts:41/61` → `fileService.save()` → `invoke('save_file')` (`file_ops.rs:260`) | **Implementasi nyata** (via delegate) — terdaftar di `KNOWN_CORE_COMMANDS`; `executeCommand('workbench.action.files.save')` forward ke core saat delegate ada |
| `workbench.action.files.newUntitledFile/newFile/openFile/openFolder` | `fileCommands.ts:22/24/28` → `handleNewTextFile` / `@tauri/plugin-dialog open()` + `openFileInActivePane` | **Implementasi nyata** (via delegate) |
| `explorer.newFile/newFolder/rename/delete/duplicate/copy/cut/paste` | `explorerCommands.ts:28` → `fileService.createFile/renameItem/deleteItem/copyItem` → `invoke(...)` (`file_ops.rs:389`) | **Implementasi nyata** (via delegate) |
| `editor.action.*` (undo/redo/selectAll/find/replace/gotoDefinition) | `editorActionCommands.ts:12` → `eventBus.emit('editor:action', {action})` (`eventBus.ts:12`) → CodeMirror | **Implementasi nyata** (via delegate eventBus) |
| `workbench.action.*` (toggleSidebar/showExplorer/showSearch/…) | `viewCommands.ts:10` → `uiStore.toggleSidebar/setActiveSidebarPanel` (`ui.ts`) | **Implementasi nyata** (via delegate) |
| `workbench.action.closeActiveEditor/closeOtherEditors/splitEditor*` | `tabCommands.ts:22/46/95` → `splitStore.closeTabInPane/splitPane` + `editorStore.closeTab` | **Implementasi nyata** (via delegate `window.tabGroups`) |
| `workbench.action.terminal.*` | `terminalCommands.ts:13` → `terminalStore` + `bottomPanelRegistry` | **Implementasi nyata** (via delegate; terminal prioritas rendah tapi command ada) |
| `git.*` (stage/unstage/commit/push/pull/fetch/publish/clone) | `gitCommands.ts:14` → `gitRepoStore.*` → `gitIpc` (`ipc.ts:66`) → `invoke('git_*')` + `Channel<GitProgress>` | **Implementasi nyata** (via delegate) |
| `workbench.action.run/runCurrentFile/stopRun` | `runCommands.ts:11` → `runService.*` → `runProviderRegistry` | **Implementasi nyata** (via delegate) |
| `workbench.action.preview.showCode/showPreview/showSplit` | `previewRegistry.ts:130` → `previewRegistry.getForFile(path)` → `editorStore.updateTab` | **Implementasi nyata** (via delegate) |
| `workspace.fs.*` (`readFile/writeFile/readDirectory/createDirectory/rename/delete/stat`) | `fileIpc.*` (`ipc.ts:11`) / `fileService.*` (`fileService.ts:66`) → Tauri `invoke('read_file_text','read_directory',…)` (`file_ops.rs:76`) | **Implementasi nyata ringan** — dual: delegate ke `fileService` saat ada, else fallback in-memory tree (fully functional untuk test/smoke). CRUD penuh |
| `workspace.getConfiguration/update` / `onDidChangeConfiguration` | `settingsIpc` (`ipc.ts:37`) → `invoke('load/save_global_settings')` (`db.rs:577`) + `settingsRegistry.ts:34` | **Implementasi nyata ringan** — dual: delegate ke `settingsStore` saat ada, else `configStore` in-memory + `onDidChangeConfiguration` emitter |
| `window.showInformation/Warning/ErrorMessage` | `notificationService.ts` / `uiStore.addToast()` (`constants.ts:116` MAX_TOASTS) | **Implementasi nyata ringan** — delegate ke `notificationService` saat ada, else queue fallback + `__getShownMessages()` untuk test |
| `window.showQuickPick/showInputBox` | `dialogService.ts` + `QuickPickDialog`/`InputBoxDialog` + `palette.ts` | **Implementasi nyata ringan** — delegate ke `dialogService` saat ada, else `undefined` (cancelled) |
| `window.createStatusBarItem` | `statusBarRegistry.register(item)` (`statusBarRegistry.ts:24`, `statusBarContributions.ts`) | **Implementasi nyata ringan** — delegate ke `statusBarRegistry` saat ada, else `StatusBarItemImpl` + Map |
| `window.createOutputChannel` | `terminalStore` output log + `OUTPUT_CATEGORIES:Extension Host` (`constants/index.ts:102`) | **Implementasi nyata ringan** — fallback `OutputChannelImpl` (buffer); host juga bisa bridge ke bottom panel Output |
| `window.activeTextEditor/visibleTextEditors` / `tabGroups` | `editorStore.tabs/activeTabId` + `splitStore.panes/rootNode` | **Implementasi nyata ringan** — getter fallback + `__setActiveTextEditor`/`__setTabGroups` untuk bridge |
| `window.withProgress` | `dialogService.withProgress` + `uiStore.addProcessToast/process` | **Implementasi nyata ringan** — delegate saat ada, else no-op progress wrapper |
| `window.registerCustomEditorProvider` / `openEditorToSide` / `createWebviewPanel` | Belum ada Custom Editor / Webview di core (hanya `MarkdownPreview`, `ImageViewer`, `DiffEditor`, `SplitView` hardcoded) | **Stub bertipe** — `throw new NotImplementedError` (typed, pesan gap prioritas tinggi) |
| `languages.register*` / `createDiagnosticCollection` | `commonExtensions.ts`, `extensionRegistry.ts`, `languageRegistry.ts:15` — per-bahasa CM6 extension sudah modular tapi provider API dinamis belum pluggable runtime | **Stub bertipe** — tidak dikerjakan di Fase 2 (menunggu Fase 3 languages) — dicatat di 01 §4, gap prioritas menengah |
| `views.registerTreeDataProvider/registerWebviewViewProvider` | `sidebarRegistry.ts:9` + `activityBarRegistry.ts:9` | **Stub bertipe** — menunggu Fase 3 views (01 §5) |
| `menus.evaluateWhenClause` | `when` di `menuRegistry.ts:21` sudah `when: () => boolean` (fungsi), bukan string evaluator | **Stub** — menunggu Fase 3 menus (parser when string) |
| `theming.getActiveColorTheme/onDidChangeActiveColorTheme` | `themes/index.ts:159` `THEMES` + `themeStore` | **Stub** — menunggu Fase 3 theming |
| `debug.*` / `tasks.*` / `scm.createSourceControl` / `terminal.createTerminal` / `authentication.*` | `scmRegistry.ts:9`, `runRegistry.ts:10`, `terminal.svelte.ts` | **Stub bertipe** — menunggu Fase 4 prioritas rendah (01 §8-13) |
| `extensions.getExtension/all/onDidChange` | Belum ada loader `.ntrn` — dikelola `ExtensionHost` sendiri (`lifecycle.ts`) | **Implementasi nyata sisi SDK** — `ExtensionHost.getExtension/all` via `Map` (tidak butuh core) |

**Kriteria keluar Fase 2: Tercapai.**

- Semua API di atas bisa dipanggil dari `examples/hello-world` tanpa error TypeScript (`bunx tsc --noEmit` exit 0, `examples/hello-world/src/extension.ts` typechecked via its own tsconfig; SDK `tsc --noEmit` exit 0).
- Unit test host (lifecycle + error isolation) lolos: `bun test` 119 pass / 0 fail (40 di `host.test.ts`), termasuk isolasi aktivasi/deactivate/event/command dan LIFO dispose.

---

## Fase 3 — API Menengah (Views, Menus, Languages, Theming) (selesai: 2026-09-12)

Rujukan wajib: `NOTRON_SDK_01_API_SURFACE.md:4-7` (views, menus, languages, theming), `NOTRON_MODULARITY_MAP.md:9` (modularitas bahasa/syntax), `NOTRON_SDK_06_ROADMAP_PHASING.md: Fase 3`.

### Selesai

- **views (§5)** — buat `src/api/views.ts:1` (262 baris) dengan `ViewContainer {id,title,icon}`, `View {id,name,containerId,type,when,icon}`, `TreeDataProvider<T> {onDidChangeTreeData,getTreeItem,getChildren,getParent}`, `ProviderResult<T>`, `WebviewViewProvider` (reuse `types.ts`). Public CRUD: `getContainer(id)`, `getContainers()`, `getView(viewId)`, `getViews(containerId?)`, `registerTreeDataProvider<T>(viewId,provider) → Disposable`, `registerWebviewViewProvider(viewId,provider) → Disposable`, `onDidChangeViews`. In-memory fallback `containers`/`viewsMap`/`treeProviders`/`webviewProviders` + `viewEmitter`. Delegate `ViewsDelegate {registerViewContainer, registerView, registerTreeDataProviderInternal, registerWebviewViewProviderInternal, getContainers, getViews}` untuk bridge ke `activityBarRegistry`/`sidebarRegistry` (`src/lib/workbench/activityBarRegistry.ts:9`, `sidebarRegistry.ts:9`). Declarative helpers `__registerViewContainer`/`__registerView` dipanggil `host/contributions.ts`. Introspection `__getViewContainers`/`__getViews`/`__getTreeDataProviders`/`__getWebviewViewProviders`/`__clearViews`. Diuji 3 test di `test/fase3.test.ts` (container/view read via manifest, Tree/Webview provider CRUD).
- **menus (§6)** — buat `src/api/menus.ts:1` (470 baris) dengan `MenuLocation` (13 nilai: `commandPalette`, `editor/context`, `editor/title`, `explorer/context`, `view/title`, `view/item/context`, `tab/context`, `menubar/file`, `menubar/edit`, `menubar/view`, `menubar/run`, `menubar/help`, `pane/context`), `WhenContext = Record<string,unknown>`, `evaluateWhenClause(expression, context) → boolean` (parser lengkap). Tokenizer menangani `STRING` quoted (`''`/`""`), `IDENT` (allow `.`/`:`/`/`/`-` untuk `.js`/`.md`), operator `&&` `||` `!` `==` `!=` `===` `!==` `=~` `>` `<` `>=` `<=` `in`/`not in`, `(`/`)`. Parser recursive-descent: `or → and → equality (==/!=/=~/>/</>=/<=/in) → unary (!/not) → primary (IDENT/STRING/parens)`. Ident missing di boolean context → `undefined` (falsy), di RHS comparison → literal string (untuk `.md` literal). Regex `=~` stripping `/.../flags` + `new RegExp`. Mendukung truthiness (`editorTextFocus`), negasi, logical, parens, equality quoted/unquoted (`view == 'explorer'` / `resourceExtname == .md`), `in`. In-memory `menuContributions: Map<MenuLocation, MenuEntry[]>` + delegate `MenusDelegate`. Helper `__registerMenu`/`getMenus(location, context)` (filter via `evaluateWhenClause`), `__clearMenus`. Diuji 5 test (ident truthiness, &&/||/parens, equality, regex, host contributes filtering).
- **languages (§4) — dengan gap analysis eksplisit** — buat `src/api/languages.ts:1` (486 baris). Tipe: `TextDocument`, `Position`/`Range` (reuse `types/index.ts`), `CancellationToken`, `CompletionItem`/`CompletionItemKind`/`CompletionList`/`CompletionTriggerKind`, `CompletionItemProvider`, `Hover`/`HoverProvider`, `Location`/`Definition`/`DefinitionProvider`, `TextEdit`/`FormattingOptions`/`DocumentFormattingEditProvider`, `Diagnostic`/`DiagnosticSeverity`/`DiagnosticCollection`, `LanguageConfiguration`. Delegate `LanguagesDelegate` (4 provider register). In-memory registries `completionProviders`/`hoverProviders`/`definitionProviders`/`formattingProviders`/`languageConfigs` + declarative `declaredLanguages`/`declaredGrammars`. Public: `registerCompletionItemProvider(selector, provider, ...triggerChars)`, `registerHoverProvider`, `registerDefinitionProvider`, `registerDocumentFormattingEditProvider` (semua `warnStubOnce` + simpan in-memory bila delegate tidak ada), `setLanguageConfiguration(languageId, configuration) → Disposable`, `createDiagnosticCollection(name?) → DiagnosticCollection` (real CRUD: `set`/`get`/`has`/`delete`/`clear`/`forEach`/`dispose` via `DiagnosticCollectionImpl` + `Emitter`), `matchesSelector`. Declarative `__registerLanguage`/`__registerGrammar` dipanggil host/contributions. Introspection `__getCompletionProviders` etc. + `__clearLanguages`. Verifikasi highlighting modular di `src/` (lihat Catatan Tambahan) — highlighting IS modular, provider runtime stub prioritas tinggi.
- **theming (§7)** — buat `src/api/theming.ts:1` (105 baris) dengan `ColorTheme {kind:'light'|'dark', id, label?}`, `ThemeContribution`/`IconThemeContribution`, delegate `ThemingDelegate {getActiveColorTheme, onDidChangeActiveColorTheme, getContributedThemes}`. Fallback `activeTheme: {id:'vscode-dark', kind:'dark'}` + `themeEmitter`. Public: `getActiveColorTheme()`, `onDidChangeActiveColorTheme` (delegate-aware), host helpers `__setActiveTheme`/`__getActiveThemeFallback`. Declarative `__registerTheme`/`__registerIconTheme`/`__registerProductIconTheme`, `__getContributedThemes` etc. + `__clearTheming`. Diuji 2 test (get/set + event, host contributes).
- **host contributions (declarative via manifest)** — buat `src/host/contributions.ts:1` (115 baris) `registerManifestContributions(manifest, extensionId) → Disposable` membaca `contributes.*`: `viewsContainers.activitybar/panel` → `__registerViewContainer`, `views: Record<containerId, View[]>` → `__registerView`, `menus: Record<MenuLocation, MenuItem[]>` → `__registerMenu`, `languages[]` → `__registerLanguage`, `grammars[]` → `__registerGrammar`, `themes[]`/`iconThemes`/`productIconThemes` → `__registerTheme/Icon/Product`. Validasi via `validateManifest` Fase 1 (enum `MenuLocation`, `uiTheme`, dll). Update `src/host/lifecycle.ts:1` untuk memanggil `registerManifestContributions` di `register()` per manifest, push ke `context.subscriptions` (LIFO dispose) + `this.contributions` map; update `deactivateAll()` untuk dispose `context` bahkan bila `!activated` (agar declarative contributes selalu dibersihkan tanpa harus `activateAll`) dan clear `contributions` map (idempotent).
- **api/host index** — update `src/api/index.ts:1` untuk selective re-export (hindari `ProviderResult`/`DocumentSelector` collision: `views/languages/window` via namespace + `type` aliases), update `src/index.ts:8` untuk re-export `views`/`menus`/`languages`/`theming` namespaced (`viewsModule`, `menusModule`, `languagesModule`, `themingModule` + `views`/`menus`/`languages`/`theming`), update `src/host/index.ts:1` untuk re-export `registerManifestContributions`.
- **examples/sidebar-counter** — buat `examples/sidebar-counter/manifest.json:1` (viewsContainers `acme.counterContainer`, views `acme.sidebarCounterView` type webview + `acme.counterTree` type tree, commands `acme.sidebarCounter.increment/reset`, menus `view/title` + `view/item/context` dengan `when: view == ...`), `src/extension.ts:1` (108 baris) activate: read `views.getContainer/getView`, theming `getActiveColorTheme/onDidChange`, `menus.evaluateWhenClause` smoke, `globalState.counter` + `Emitter<CounterItem|undefined>` + `TreeDataProvider` (`getTreeItem` label `Counter: N`, `getChildren`) + `WebviewViewProvider` (html `getHtml(count, themeId)` + `onDidReceiveMessage({type:'increment'|'reset'})` → update `globalState` + `changeEmitter.fire`), commands register, `OutputChannel Sidebar Counter`. README dengan coverage + dev loop.
- **examples/markdown-preview** — lanjut sebagai smoke `languages+menus+theming+customEditor stub`: `manifest.json:1` (activation `onLanguage:markdown`, contributes `menus.editor/title` when `editorTextFocus && resourceExtname == .md` + `commandPalette`, `languages` `markdown-preview-demo` `.mdp`, `grammars` `source.mdp`, `themes` `acme.preview-dark`, `configuration` `markdownPreview.autoRefresh/theme`), `src/extension.ts:1` (96 baris) activate: theming get/onDidChange, `menus.evaluateWhenClause` 3 kasus (`visible`, `hidden`, `paren`), `languages.setLanguageConfiguration` markdown, `registerHoverProvider`/`registerCompletionItemProvider` markdown, `createDiagnosticCollection` set 1 diag, `window.registerCustomEditorProvider` try/catch `NotImplementedError` (expected stub sampai `NOTRON_MODULARITY_MAP` Batasan #4), `commands.registerCommand acme.markdownPreview.toggle` (log + `showInformationMessage` + try `createWebviewPanel` fallback ke `OutputChannel`). README dengan coverage + expected log.
- **tests** — buat `test/fase3.test.ts:1` (19 test baru) coverage views/menus/languages/theming + smoke kedua examples via `ExtensionHost` (manifest → `registerManifestContributions` → providers). Total suite kini 138 pass (utils 1 + manifest 78 + host 40 + fase3 19). Host lifecycle tests Fase 2 tetap 40 pass (error isolation, topologicalSort, Memento, EventBus, commands/window/workspace/hello-world).
- **Build** — `bunx tsc --noEmit` exit 0 (strict `noUnusedLocals/Params` lolos), `bun run build` (`tsc -p tsconfig.json`) emit `dist/` 19 files `.d.ts` tambahan (`api/views`, `api/menus`, `api/languages`, `api/theming`, `host/contributions`).
- **Verify CodeMirror modularitas** — eksplorasi eksplisit `src/lib/utils/languageDetector.ts:1` (805 baris, `LANG_LOADERS: Record<string, Loader>` 80+ bahasa, `StreamLanguage` vs `@codemirror/*` per bahasa, `FILENAME_TO_LANG` 30+ dotfiles, `languageRegistry.register({id, extensions, load})` loop 80+), `src/lib/editor/languageRegistry.ts:15` (per-language `register`/`get`/`getByExtension`), `src/lib/editor/Editor.svelte:232` (`langCompartment.reconfigure(await getLanguageExtension(filePath))` lazy), `src/lib/editor/commonExtensions.ts:31` (COMMON_EXTENSIONS static tanpa bahasa), `src/lib/editor/extensionRegistry.ts:32` (editorExtensionRegistry per-feature, `core.common` + `core.searchHighlight`). Kesimpulan: highlighting per-bahasa SUDAH modular — setiap bahasa modul CodeMirror terpisah, load via dynamic import per ekstensi file, bukan satu config besar. Gap hanya provider dinamis (autocomplete/hover) — stub prioritas tinggi.

### Stub / Belum Bisa Diimplementasikan Penuh

Semua di bawah typed stub (signature final + `NotImplementedError` atau in-memory fallback dengan warning) per `NOTRON_SDK_00_INDEX.md:2`:

- `languages.registerCompletionItemProvider` / `registerHoverProvider` / `registerDefinitionProvider` / `registerDocumentFormattingEditProvider` — **stub prioritas tinggi** — highlighting/syntax per bahasa SUDAH modular (`NOTRON_MODULARITY_MAP.md:9` terpenuhi: `languageDetector.ts:EXT_TO_LANG` + `LANG_LOADERS` 80+ bahasa + `languageRegistry` per-bahasa + `Editor.svelte:langCompartment` lazy). Namun runtime provider (memberi autocomplete/hover ke editor live) BELUM ada jembatan ke CodeMirror 6. SDK menyimpan provider di `completionProviders`/`hoverProviders` arrays dan `warnStubOnce` dengan pesan gap; host belum bisa inject via `editorExtensionRegistry`/`Compartment` karena core belum expose `CompletionProviderRegistry` generik. Di `src/api/languages.ts:184` warning: “requires a CodeMirror extension bridge (editorExtensionRegistry/compartment)”. Implementasi nyata menunggu core menambah facet/extension untuk provider contributions (prioritas tinggi). `DiagnosticCollection` dan `setLanguageConfiguration` SUDAH real (in-memory CRUD penuh) karena tidak butuh bridge editor.
- `window.registerCustomEditorProvider` / `openEditorToSide` / `createWebviewPanel` — tetap **stub bertipe** `throw new NotImplementedError` (`src/api/window.ts:491`) — core hanya punya `MarkdownPreview.svelte`/`ImageViewer.svelte`/`DiffEditor.svelte` hardcoded (`previewRegistry.ts:14` 2 provider .md/.svg) + `SplitView`, belum ada registry generik custom editor / webview CSP isolation (`NOTRON_MODULARITY_MAP` Batasan #4). `examples/markdown-preview` menangkap `NotImplementedError` dan fallback ke `OutputChannel` sebagai smoke yang valid.
- `window.registerWebviewViewProvider` (window namespace) — tetap stub (window) sedangkan `views.registerWebviewViewProvider` (views namespace) SUDAH real via `src/api/views.ts:195` (in-memory + delegate ke `sidebarRegistry`). `examples/sidebar-counter` memakai views namespace, bukan window, sehingga smoke views real tanpa stub.
- `menus` declarative `when` sebagai fungsi (`menuRegistry.ts:23` `when?: () => boolean`) vs SDK `when: string` dievaluasi via `evaluateWhenClause` — SDK evaluator SUDAH real (470 baris, support `&&`/`||`/`!`/`==`/`!=`/`=~`/`>`/`<`/`in`), tapi host belum wiring string → function bridge (perlu wrapper `() => evaluateWhenClause(when, context)` di core). Fallback in-memory filter di `menus.getMenus` sudah real untuk test.
- `theming` declarative `contributes.themes`/`iconThemes` SUDAH real via `__registerTheme` (host/contributions) tapi aplikasi tema ke `THEMES` (`themes/index.ts:159` 51 tema) + `applyThemeVariables` + CSS variables belum dynamic import dari file `path` JSON token; hanya disimpan sebagai `ThemeContribution {id,label,uiTheme,path}` untuk host inspection. Gap prioritas rendah.
- `views` declarative panel `viewsContainers.panel` disamakan ke `ViewContainer` yang sama (activitybar) — core punya `bottomPanelRegistry.ts:11` untuk panel bawah tapi belum di-bridge; SDK simpan sebagai container biasa (gap rendah).
- `workspace.registerFileSystemProvider` tetap stub (Fase 2, prioritas rendah, `fileService.ts:66` hanya lokal).
- `scm` / `terminal` / `debug` / `tasks` / `authentication` tetap stub menunggu Fase 4 (tidak dikerjakan di Fase 3 sesuai peta).

> Semua non-stub di atas (views CRUD + declarative, menus evaluator + declarative, languages DiagnosticCollection + LanguageConfiguration + declarative languages/grammars, theming read/event + declarative themes) adalah **implementasi nyata** (atau fallback in-memory bridgeable via `setViewsDelegate`/`setMenusDelegate`/`setLanguagesDelegate`/`setThemingDelegate`). Tidak ada API Fase 3 yang dilewati tanpa pembahasan.

### File yang Diubah/Ditambah

- `src/api/views.ts` — **baru**, 262 baris: ViewContainer/View/ProviderResult/TreeDataProvider + delegate + fallback + introspection.
- `src/api/menus.ts` — **baru**, 470 baris: MenuLocation, evaluateWhenClause (tokenizer+parser), MenuEntry store + delegate.
- `src/api/languages.ts` — **baru**, 486 baris: provider interfaces (Completion/Hover/Definition/Formatting), DiagnosticCollectionImpl, LanguageConfiguration, delegate + declared languages/grammars stores.
- `src/api/theming.ts` — **baru**, 105 baris: ColorTheme, contributions, fallback, delegate.
- `src/host/contributions.ts` — **baru**, 115 baris: `registerManifestContributions` reader untuk 7 kategori contributes.
- `src/host/lifecycle.ts` — **diubah**: import `registerManifestContributions`, field `contributions: Map<string,Disposable>`, panggil di `register()` per manifest (push ke `context.subscriptions`), ubah `deactivateAll()` untuk dispose `context` bahkan bila `!activated` + clear `contributions` map (idempotent).
- `src/api/index.ts` — **diubah**: selective re-export untuk hindari `ProviderResult`/`DocumentSelector`/`registerWebviewViewProvider` collision (views/languages/window via namespace).
- `src/index.ts` — **diubah**: tambah `viewsModule`/`menusModule`/`languagesModule`/`themingModule` + `views`/`menus`/`languages`/`theming` namespaced exports.
- `src/host/index.ts` — **diubah**: re-export `registerManifestContributions`.
- `examples/sidebar-counter/manifest.json` — **baru**, declares viewsContainers/views/commands/menus.
- `examples/sidebar-counter/src/extension.ts` — **baru**, 108 baris, smoke views+menus+theming.
- `examples/sidebar-counter/package.json` — **baru**, file dep `notron-sdk`.
- `examples/sidebar-counter/README.md` — **baru**, coverage + dev loop.
- `examples/markdown-preview/manifest.json` — **baru**, declares commands/menus/languages/grammars/themes/configuration.
- `examples/markdown-preview/src/extension.ts` — **baru**, 96 baris, smoke languages+menus+theming+customEditor stub.
- `examples/markdown-preview/package.json` — **baru**.
- `examples/markdown-preview/README.md` — **baru**.
- `test/fase3.test.ts` — **baru**, 19 test (views 3, menus 5, languages 5, theming 2, examples smoke 2) via bun:test.
- `dist/` — hasil `bun run build` ter-generate ulang (tambahan `.d.ts` untuk 4 namespace baru + `host/contributions`).
- `IMPLEMENTATION_LOG.md` — entry Fase 3 (file ini).

### Catatan Tambahan

- Verifikasi: `bunx tsc --noEmit` exit 0 (strict, `src` only, tidak termasuk `test/` per `tsconfig.json:28` `include: ["src"]`), `bun run build` emit `dist/` 19 `.d.ts` tanpa error.
- Verifikasi: `bun test` — **138 pass, 0 fail, 296 expect()** (4 files: `utils.test.ts` 1 + `manifest.test.ts` 78 + `host.test.ts` 40 + `fase3.test.ts` 19). Termasuk smoke `examples/sidebar-counter` (`views.getContainer` + `TreeDataProvider` + `WebviewViewProvider` + `commands.executeCommand increment`) dan `examples/markdown-preview` (hover/completion providers + `DiagnosticCollection` + `evaluateWhenClause` 3 kasus + `NotImplementedError` catch + `createWebviewPanel` fallback).
- Eksplisit cek modularitas bahasa di `src/` (root repo, CodeMirror 6 setup):
  - **SUDAH modular per bahasa** untuk highlighting/grammar: `languageDetector.ts:116` `LANG_LOADERS: Record<string, Loader>` berisi 80+ loader (`javascript`, `typescript`, `python`, `rust`, `c`, `cpp`, `java`, `html`, `css`, `sass`, `json`, `xml`, `markdown` dengan `codeLanguages` lazy, `sql`, `php`, `go`, `yaml`, `vue`, `liquid`, `svelte`, `lezer`, `elixir`, `nix`, `prisma`, `astro`, `dot`, `handlebars`, `hcl`, `julia`, `wgsl`, `zig`, `glsl`, `csharp`, `solidity`, `clojure`, `r`, `kotlin`, `scala`, `swift`, `lua`, `luau`, + 40+ legacy `StreamLanguage` `shell`/`ruby`/`perl`/`dockerfile`/`toml`/`diff`/`cmake`/`dart`/`pascal`/`haskell`/…), tiap loader `async () => import('@codemirror/lang-...')`. Registrasi via `languageRegistry.register({id, extensions: langMap[id], load})` loop `Object.entries(LANG_LOADERS)` (80+). Deteksi via `getLanguageExtension(filename)` (732): 1) `FILENAME_TO_LANG[basename]` exact (dotfiles `.env`, `Dockerfile`, `Makefile`, `Cargo.lock`… 30+), 2) `EXT_TO_LANG[ext]` via `languages.json` reverse map (single source `constants/languages.json` share dengan `file_ops.rs:659` `include_str!`), 3) fallback `[]`. Pemakaian di `Editor.svelte:232` `loadLanguage()` → `langCompartment.reconfigure(await getLanguageExtension(filePath))` (lazy, `isLargeFile` skip). `languageRegistry.ts:15` `LanguageContribution {id, extensions, filenames, load}` + `Map`. `commonExtensions.ts:31` `COMMON_EXTENSIONS` static TANPA bahasa (history, bracketMatching, closeBrackets, autocompletion generic, highlightSpecialChars, etc.). `extensionRegistry.ts:32` `EditorExtensionContribution {id, label, priority, create: () => Extension, when?}` + `core.common`/`core.searchHighlight` prioritas 10/20. Jadi **bukan satu config CodeMirror besar** — tiap bahasa modul terpisah, dynamic import, reconfigure via Compartment. `NOTRON_MODULARITY_MAP.md:9` terpenuhi untuk highlight.
  - **Belum modular untuk provider dinamis**: `commonExtensions.ts:72` `autocompletion()` generic tanpa `CompletionItemProvider` per extension + `snippetCompletionExtensions()` data-driven tapi tidak per-provider API dinamis; tidak ada `CompletionProviderRegistry` yang menerima `registerCompletionItemProvider` dari ekstensi. `extensionRegistry.ts` hanya untuk editor feature umum (minimap, breadcrumbs), bukan per-language provider. Oleh karena itu `languages` SDK menyediakan signature final tapi runtime `warnStubOnce` + in-memory fallback — **gap prioritas tinggi** dicatat di Stub di atas dan di tabel.
- Delegate pattern diperluas: `setViewsDelegate`/`setMenusDelegate`/`setLanguagesDelegate`/`setThemingDelegate` memungkinkan SDK standalone (fallback) dan bridged (host injects `activityBarRegistry.register`, `menuRegistry.register`, `editorExtensionRegistry` / `THEMES` bila tersedia). Tidak ada perubahan di luar `packages/notron-sdk` (read-only core dihormati — semua bridging hanya via interface injeksi, bukan `import` langsung `src/lib/...`).
- `evaluateWhenClause` lolos uji: `editorTextFocus` truthiness (missing → false), `a && b`/`a || b`/parens, `view == 'explorer'` (quoted), `resourceExtname == .md` (unquoted `.` literal), `!=`, `=~ /test/` dan `/\.md$/` regex stripping slashes, empty → true, `in` stub.
- `views` lifecycle: declarative `viewsContainers`/`views` via manifest dibaca host saat `register()` (sebelum `activateAll`), sehingga `views.getContainer`/`getView` sudah tersedia di `activate()` (sidebar-counter memverifikasi). Dispose LIFO via `context.subscriptions` + `ExtensionHost.contributions` map; `deactivateAll()` kini dispose bahkan untuk `!activated` agar `bun test` isolated (sebelumnya gagal 6 test karena `!activated` skip).
- Tidak ada perubahan `package.json`/`tsconfig.json` (tetap `emitDeclarationOnly`, `type: module`, `strict`). `dist/` emit `declarationMap:true`.

**Kriteria keluar Fase 3: Tercapai.**
- `examples/sidebar-counter` (Webview View) berhasil dibangun dan berfungsi sebagai smoke test `views` — manifest declares `acme.counterContainer` + 2 views (webview + tree), `views.registerTreeDataProvider`/`registerWebviewViewProvider` menyimpan provider di `__get*Providers()` (fallback) dan delegate bila ada; `ExtensionHost` smoke di `fase3.test.ts:292` mengaktivasi via host, memverifikasi `views.getContainer` + `executeCommand increment` + `deactivate` clear; `bun test` 19 Fase 3 pass termasuk smoke ini.
- `examples/markdown-preview` mulai dikerjakan sebagai smoke test custom editor + `menus` untuk tombol toggle preview — manifest declares `menus.editor/title` `when: editorTextFocus && resourceExtname == .md`, `languages`/`grammars`/`themes`/`configuration`; activate smoke `languages` 4 provider + `DiagnosticCollection` CRUD + `setLanguageConfiguration` + `menus.evaluateWhenClause` 3 kasus + `theming` + `window.registerCustomEditorProvider`/`createWebviewPanel` stub catch `NotImplementedError` fallback ke `OutputChannel`; `fase3.test.ts:311` host smoke pass (declared languages/grammars/themes via `__get*` + `executeCommand toggle` tanpa throw).

---

## Fase 4 — API Prioritas Rendah (Stub Penuh) (selesai: 2026-09-12)

Rujukan wajib: `NOTRON_SDK_01_API_SURFACE.md:8-13` (debug, tasks, scm, snippets, authentication, terminal), `NOTRON_SDK_02_MANIFEST_TYPES.md:4.11` (debuggers/taskDefinitions), `NOTRON_SDK_06_ROADMAP_PHASING.md: Fase 4`.

**Tugas:** implementasi seluruh tipe & signature untuk debug, tasks, scm, snippets, authentication, terminal; cek nyata vs stub ke `src-tauri/` + `src/`; pastikan 14 namespace terwakili.

### Selesai

- **debug (§8) — stub bertipe** — buat `src/api/debug.ts:1` (165 baris) dengan tipe `DebugConfiguration {type,name,request,program,cwd,args,env,...}`, `DebugSession`, `DebugAdapterDescriptor` (`executable|server|pipe`), `DebugAdapterDescriptorFactory {createDebugAdapterDescriptor(session,executable)}`. Delegate `DebugDelegate {registerDebugAdapterDescriptorFactory, startDebugging, onDidStart/Terminate}`. In-memory fallback `adapterFactories:Map<string,Factory>` + `declaredDebuggers[]` + `Emitter<DebugSession>` untuk `onDidStartDebugSession`/`onDidTerminateDebugSession`. Public: `registerDebugAdapterDescriptorFactory(debugType, factory)→Disposable` (delegate jika ada, else simpan in-memory + `warnStubOnce`), `startDebugging(folder,config)→Promise<boolean>` (delegate jika ada, else `throw NotImplementedError('debug.startDebugging', detail DAP belum ada + saran pakai Run)`), events `onDidStart/TerminateDebugSession` (delegate-aware). Declarative `__registerDebugger`/`__getDeclaredDebuggers` dipanggil `host/contributions.ts`; introspection `__getAdapterFactories`/`__clearDebug`/`__fireDidStart/TerminateDebugSession`. Verifikasi core **belum punya DAP** (lihat Catatan Tambahan: hanya `runService` + `runProviderRegistry` tanpa DAP) — stub prioritas rendah.

- **tasks (§9) — real bridge sebagian** — buat `src/api/tasks.ts:1` (210 baris) dengan tipe `TaskDefinition`, `Task {definition,name,source,scope,detail,group,execution}`, `ShellExecution`/`ProcessExecution`/`CustomExecution`, `TaskProvider {provideTasks,resolveTask}`, `TaskExecution {task,terminate}`, `TaskStartEvent`/`TaskEndEvent`. Delegate `TasksDelegate {registerTaskProvider, executeTask, fetchTasks, onDidStart/End}`. In-memory fallback `providers:ProviderEntry[]` + `declaredTaskDefinitions[]` + `Emitter<TaskStart/End>` + `activeExecutions:Set<TaskExecutionImpl>` + `warnOnce`. Public: `registerTaskProvider(type,provider)→Disposable` (delegate→`taskRegistry.register` jika ada, else in-memory), `executeTask(task)→Promise<TaskExecution>` (delegate jika ada, else fallback in-memory yang fire `onDidStartTask` lalu `onDidEndTask` via `queueMicrotask`), `fetchTasks(filter?)` (delegate atau kumpulkan dari in-memory providers), events `onDidStartTask`/`onDidEndTask`/`onDidStart/EndTaskProcess`. Declarative `__registerTaskDefinition`/`__getDeclaredTaskDefinitions` dipanggil host. Verifikasi core **sudah punya taskRegistry** generic (`taskRegistry.ts:9` 102 baris + `taskProviders.ts:27` npm/cargo, `runService.ts:334` task→RunConfiguration type `task`) — real bridge (provider registration) bukan stub, execution fallback prioritas rendah.

- **scm (§10) — real bridge sebagian** — buat `src/api/scm.ts:1` (185 baris) dengan tipe `SourceControl {id,label,rootUri,inputBox,count,createResourceGroup(id,label),dispose}`, `SourceControlResourceGroup {id,label,hideWhenEmpty,resourceStates,dispose}`, `SourceControlResourceState {resourceUri,command,decorations,contextValue}`, `SourceControlInputBox {value,placeholder}`. Delegate `ScmDelegate {createSourceControl, getSourceControls}`. In-memory fallback `SourceControlImpl` + `ResourceGroupImpl` (CRUD penuh: `createResourceGroup` throw duplicate, `resourceStates` setter clone + fire `onDidChange`, `dispose` LIFO, `inputBox` mutable, `count` auto) + `Map<string,SourceControlImpl>` + `Emitter<SourceControl>`. Public: `createSourceControl(id,label,rootUri?)→SourceControl` (delegate→`scmRegistry.register` jika ada, else Fallback CRUD), `getSourceControls()`, event `onDidChangeSelectedSourceControl`. Introspection `__getSourceControls`/`__clearScm`. Verifikasi core **sudah punya scmRegistry** generic (`scmRegistry.ts:9` id=`git`, label=Git, priority 10, `loadComponent: SourceControlPanel`) + `git.ts` + `gitRepo.ts` + `git_service.rs` — real bridge (generic SCM) bukan stub.

- **snippets (§11) — declarative real** — buat `src/api/snippets.ts:1` (115 baris) dengan tipe `Snippet {prefix,description,body,scope}`, re-export `SnippetContribution` dari `types`, `SnippetContributionEntry {language,path,snippets?,extensionId}`. Delegate `SnippetsDelegate {getSnippets,registerSnippets}`. In-memory fallback `snippetContributions:SnippetContributionEntry[]` + `languageSnippetMap:Map<string,Snippet[]>` (case-insensitive). Public: `__registerSnippetContribution(entry)→Disposable` (dipanggil host/contributions, index ke map jika `snippets[]` sudah loaded), `registerSnippets(language,snippets)→Disposable` (imperative helper untuk test/host, delegate→`snippetRegistry.register` jika ada, else fallback), `getSnippetsForLanguage(language)` (delegate atau `Map`), `getAllSnippetContributions()`, `validateSnippet(obj)→ValidationResult`. Introspection `__getLanguageSnippetMap`/`__clearSnippets`. Verifikasi core **sudah punya snippetRegistry** data-driven (`snippetRegistry.ts:29` 72 baris, `getForLanguage`, `register`/`registerAll`) + `snippetCompletion.ts:14` `EditorState.languageData.of(autocomplete:snippetSource)` per bahasa + `snippets.ts` core 6 bahasa (js/ts/py/rust/go/md) — declarative real, bukan stub (tidak ada `SnippetString` imperative — sesuai spec).

- **authentication (§12) — stub prioritas sangat rendah** — buat `src/api/authentication.ts:1` (120 baris) dengan tipe `AuthenticationSession {id,accessToken,account,scopes}`, `AuthenticationProvider {id,label,onDidChangeSessions,getSessions,createSession,removeSession}`, `AuthenticationGetSessionOptions`. Delegate `AuthenticationDelegate {registerAuthenticationProvider,getSession,onDidChangeSessions}`. In-memory fallback `providers:Map<string,{label,provider}>` + `Emitter<{providerId}>`. Public: `registerAuthenticationProvider(id,label,provider)→Disposable` (delegate jika ada, else in-memory + `warnStubOnce`), `getSession(providerId,scopes,options)→Promise<AuthenticationSession|undefined>` (delegate jika ada, else `throw NotImplementedError('authentication.getSession', ...)`), event `onDidChangeSessions`. Introspection `__getAuthenticationProviders`/`__clearAuthentication`. Verifikasi core **tidak ada** auth (grep `authentication` di `src` 0 hit, hanya `SecretStorage` placeholder per `Memento`) — stub sangat rendah.

- **terminal (§13) — real bridge** — buat `src/api/terminal.ts:1` (190 baris) dengan tipe `TerminalOptions {name,shellPath,shellArgs,cwd,env,message,iconPath,color,hideFromUser}`, `Terminal {name,processId,creationOptions,exitStatus,sendText,show,hide,dispose}`, `TerminalProfile`, `TerminalProfileProvider`, `TerminalLink`, `TerminalLinkContext`, `TerminalLinkProvider`. Delegate `TerminalDelegate {createTerminalInternal,getTerminals,onDidOpen/Close,registerTerminalProfileProviderInternal,registerTerminalLinkProviderInternal}`. In-memory fallback `TerminalImpl` (id=`term-${counter}-${now}`, name auto `Terminal N`, `processId=1000+counter`, `buffer:string` + `sendText(text,addNewLine)` append + `console.log`, `show`/`hide` flag, `dispose` → `didCloseEmitter`), `terminals:Map<string,TerminalImpl>` + `profileProviders:Map` + `linkProviders:array` + `Emitter<Terminal>` `didOpen`/`didClose`. Public: `createTerminal(options?)→Terminal` (delegate→`terminalStore.newTerminal(type,cwd,{initialCommand,env})` + `ptyProcess.write` jika ada, else fallback), `registerTerminalProfileProvider(id,provider)`, `registerTerminalLinkProvider(provider)`, events `onDidOpenTerminal`/`onDidCloseTerminal`. Introspection `__getTerminals`/`__getProfileProviders`/`__getLinkProviders`/`__clearTerminal`. Verifikasi core **sudah punya terminal nyata**: `terminal.ts:74` 222 baris (`newTerminal`/`closeTerminal`/`killProcess`/`registerProcess`), `TerminalInstance.svelte:1` 185 baris (`tauri-pty spawn`, `xterm FitAddon`, `ptyProcess.onData/onExit/write`), `bottomPanelRegistry.ts:1` 72 baris (panel `terminal/output/problems`), `terminalCommands.ts:12` 6 command — real bridge.

- **extensions (§14) runtime** — perluas `src/api/extensions.ts:1` (dari 39 baris → 95 baris) dari tipe legacy `Extension/ExtensionContributions` menjadi namespace runtime `extensions` (§14): tipe `ExtensionInfo {id,version,publisher,isActive,exports,packageJSON}`, delegate `ExtensionsDelegate {getExtension, getAll, onDidChange}`, fallback `localExtensions:Map` + `changeEmitter`. Public: `getExtension(id)`, getter `all`, event `onDidChange`, helper `__registerExtensionInfo`/`__clearExtensions`, `setExtensionsDelegate`. Re-export tetap preservasi `Extension`/`CommandContribution`. Dipakai `ExtensionHost` fallback.

- **host contributions (declarative via manifest)** — ubah `src/host/contributions.ts:1` (dari 160 → 195 baris) tambah imports `__registerSnippetContribution`, `__registerDebugger`, `__registerTaskDefinition`; baca `contributes.snippets` → `__registerSnippetContribution`, `contributes.debuggers` → `__registerDebugger`, `contributes.taskDefinitions` → `__registerTaskDefinition` (loop + push ke `disposables` + `combinedDisposable`). Update header komentar. `configuration` declarative tetap divalidasi tanpa runtime registry.

- **api/host index** — update `src/api/index.ts:1` (tambah export `debug`/`tasks`/`scm`/`snippets`/`authentication`/`terminal`/`extensions` namespaced + `extensionsNamespace`), update `src/index.ts:8` (tambah `debugModule`/`tasksModule`/`scmModule`/`snippetsModule`/`authenticationModule`/`terminalModule`/`extensionsModule` + `debug`/`tasks`/`scm`/`snippets`/`authentication`/`terminal`/`extensions` namespaced). Semua re-export selective untuk hindari collision `ProviderResult`/`DocumentSelector` (sudah ada).

- **tests** — buat `test/fase4.test.ts:1` (280 baris) 28 test baru: debug 4 (declarative debuggers via host, factory CRUD, startDebugging throw `NotImplementedError`, events via `__fire`), tasks 4 (declarative taskDefinitions, provider CRUD, executeTask fallback starts/ends async, fetchTasks), scm 4 (createSourceControl+groups CRUD, inputBox, duplicate throw, selected event), snippets 4 (declarative via host, imperative registerSnippets case-insensitive, validateSnippet valid/invalid, multi-language), authentication 3 (register+dispose, getSession throw, event via `__fire`), terminal 5 (create+sendText+show/hide+dispose, onDidOpen, profile provider, link provider, auto-name), extensions 1 (all/getExtension via host), 14-namespace re-export 2 (import `src/index.js` semua terdefinisi + validateManifest debuggers/taskDefinitions/snippets valid), host contributions Fase 4 1 (host reads all Fase 4 declarative + dispose clears). BeforeEach clear semua 11 registries (registry/window/workspace/views/menus/languages/theming/debug/tasks/scm/snippets/auth/terminal/extensions) untuk isolasi. Total suite kini 166 pass (utils 1 + manifest 78 + host 40 + fase3 19 + fase4 28).

- **Build** — `bunx tsc --noEmit` exit 0 (strict `noUnusedLocals/Params` lolos), `bun run build` (`tsc -p tsconfig.json`) emit `dist/` 6 `.d.ts` baru (`api/debug`, `api/tasks`, `api/scm`, `api/snippets`, `api/authentication`, `api/terminal`) + `api/extensions` diperluas; total `dist/api` 17 file.

### Stub / Belum Bisa Diimplementasikan Penuh

Semua di bawah typed stub (signature final + `NotImplementedError` atau in-memory fallback dengan warning) per `NOTRON_SDK_00_INDEX.md:2`:

- `debug.registerDebugAdapterDescriptorFactory` — **stub prioritas rendah** — disimpan in-memory (`adapterFactories`) agar dapat diinspeksi via `__getAdapterFactories`, `warnStubOnce`. Notron core tidak punya DAP host (hanya `runService`/`runProviderRegistry` untuk run tanpa debug; pencarian `DAP`/`debug` di `src-tauri` 0 hit, `src` hanya `contrib/debug` task providers `npm`/`cargo`). `debug.startDebugging` `throw NotImplementedError` deskriptif (sarankan Run sebagai interim). Declarative `contributes.debuggers` **nyata** (data-only, disimpan `declaredDebuggers` via `__registerDebugger`), karena hanya butuh inspeksi manifest tanpa runtime.
- `debug.startDebugging` — **stub** — `throw NotImplementedError('debug.startDebugging', 'Notron core does not yet implement Debug Adapter Protocol...Use Run...Declarative debuggers remain inspectable via __getDeclaredDebuggers()')`. Status stub bertipe (prioritas rendah, menunggu DAP host + breakpoint UI).
- `tasks.registerTaskProvider` — **real bridge sebagian** (bukan stub) — bila `TasksDelegate` ada (host bridges ke `taskRegistry.register({id:type, provideTasks})`), delegasikan; else fallback in-memory `providers[]` (bridgeable, warnOnce). Core sudah punya `taskRegistry` generic + `taskProviders` npm/cargo + `runService` task→Run. `tasks.executeTask` fallback in-memory (simulasi `onDidStartTask` → `queueMicrotask` → `onDidEndTask` tanpa shell nyata, prioritas rendah). Jadi bukan stub murni — provider registration nyata, execution hybrid.
- `tasks.executeTask`/`fetchTasks` — **hybrid**: `executeTask` fallback simulasikan dengan `TaskExecutionImpl` + `Emitter` (tanpa spawn `cargo`/`npm` nyata); host dapat inject `executeTask` yang memanggil `taskRegistry.provideAll` + `runService`/`terminal`. Gap rendah (butuh wiring PTY).
- `scm.createSourceControl` — **real bridge sebagian** (bukan stub) — bila `ScmDelegate` ada (host bridges ke `scmRegistry.register({id,label,loadComponent})` + `ScmView` render), delegasikan; else fallback in-memory `SourceControlImpl` CRUD penuh (inputBox, groups, resourceStates). Core sudah punya `scmRegistry.ts:9` generic + `gitService`/`gitRepoStore`/`git_service.rs:792` — SCM generic nyata, bukan stub git-only. Implementasi fallback sudah memungkinkan extension membuat SCM non-git (prioritas tinggi) tanpa menunggu core; host wiring ke view adalah gap rendah (render resourceStates di `SourceControlPanel`).
- `snippets` imperative `SnippetString` — **tidak diekspos** (sesuai spec: snippet deklaratif via manifest, bukan API imperatif). `snippets.validateSnippet` dan `getSnippetsForLanguage` **nyata** via `languageSnippetMap` atau `snippetRegistry` delegate. Declarative `contributes.snippets` **real** (disimpan `snippetContributions` + `languageSnippetMap`). Core snippetRegistry data-driven + snippetCompletion sudah nyata (`snippetRegistry.ts:19` `getForLanguage`, `snippetCompletion.ts:14` `languageData.of({autocomplete:snippetSource})`). Tidak ada stub — hanya tidak ada `insertSnippet` imperative (prioritas sangat rendah).
- `authentication.registerAuthenticationProvider` — **stub prioritas sangat rendah** — disimpan in-memory `providers Map` (inspeksi `__getAuthenticationProviders`), `warnStubOnce`. `authentication.getSession` `throw NotImplementedError('authentication.getSession', 'No authentication delegate...')`. Core tidak ada auth service (grep `authentication` 0 hit). Session operations menunggu `SecretStorage` encrypted + OAuth service.
- `terminal.createTerminal` — **real bridge** — bila `TerminalDelegate.createTerminalInternal` ada (host bridges ke `terminalStore.newTerminal(runShellType(),cwd,{initialCommand,env})` + `TerminalInstance.svelte` `ptyProcess.write`), delegasikan; else fallback in-memory `TerminalImpl` CRUD penuh (`sendText` buffer + `console.log`, `show`/`hide`, `dispose` → `didCloseEmitter`, `processId` mock). Core memiliki terminal nyata (`terminal.ts:74` 222 baris, `TerminalInstance.svelte:1` `tauri-pty spawn` + `xterm FitAddon`, `bottomPanelRegistry` panel `terminal`, 6 `terminalCommands`). Jadi **bukan stub** — fallback sudah real untuk test/s standalone, delegate akan membuatnya nyata di host.
- `terminal.registerTerminalProfileProvider`/`registerTerminalLinkProvider` — **hybrid** (in-memory fallback + delegate). Core belum wiring profile/link provider ke terminal selector — gap rendah, fallback tetap bridgeable.
- `extensions` runtime `getExtension`/`all`/`onDidChange` ( §14 ) — **real** via `ExtensionHost` `allExtensions`/`getExtension` + fallback `localExtensions` Map. Tidak ada stub — lifecycle `activateAll`/`deactivateAll` sudah diuji 40 test Fase 2.

> Semua non-stub di atas (tasks provider, scm SourceControl CRUD, snippets declarative, terminal create/show/sendText, extensions) adalah **implementasi nyata** (atau fallback in-memory bridgeable via `setTasksDelegate`/`setScmDelegate`/`setSnippetsDelegate`/`setTerminalDelegate`/`setAuthenticationDelegate`). Tidak ada API Fase 4 yang dilewati tanpa pembahasan. Setiap stub disertai `NotImplementedError` deskriptif + komentar kode `/** Stub prioritas ... — Notron core ... */` per `NOTRON_SDK_00_INDEX.md:2` dan `NOTRON_SDK_06_ROADMAP_PHASING.md: Fase 4`.

### File yang Diubah/Ditambah

- `src/api/debug.ts` — **baru**, 165 baris: DebugConfiguration/Session/AdapterDescriptorFactory + delegate + fallback + declarative helpers + introspection.
- `src/api/tasks.ts` — **baru**, 210 baris: Task/TaskProvider/TaskExecution + delegate + fallback + declarative + events + introspection.
- `src/api/scm.ts` — **baru**, 185 baris: SourceControl/ResourceGroup/ResourceState + delegate + SourceControlImpl/ResourceGroupImpl fallback + introspection.
- `src/api/snippets.ts` — **baru**, 115 baris: Snippet types + delegate + languageSnippetMap fallback + registerSnippets + validateSnippet + declarative helpers.
- `src/api/authentication.ts` — **baru**, 120 baris: AuthenticationProvider/Session + delegate + fallback + NotImplementedError untuk getSession.
- `src/api/terminal.ts` — **baru**, 190 baris: Terminal/TerminalOptions/Profile/Link + delegate + TerminalImpl fallback + events.
- `src/api/extensions.ts` — **diubah**: dari 39 → 95 baris, tambah `ExtensionInfo`, `ExtensionsDelegate`, `localExtensions` Map + `changeEmitter`, `getExtension`/`all`/`onDidChange` runtime + helpers `__registerExtensionInfo`/`__clearExtensions`/`setExtensionsDelegate`; preservasi tipe legacy.
- `src/host/contributions.ts` — **diubah**: 160→195 baris, tambah imports `__registerSnippetContribution`/`__registerDebugger`/`__registerTaskDefinition`, handling `contributes.snippets`/`debuggers`/`taskDefinitions` (loop+disposables).
- `src/api/index.ts` — **diubah**: tambah export `debug`/`tasks`/`scm`/`snippets`/`authentication`/`terminal`/`extensions` namespaced.
- `src/index.ts` — **diubah**: tambah `debugModule`/`tasksModule`/`scmModule`/`snippetsModule`/`authenticationModule`/`terminalModule`/`extensionsModule` + `debug`/`tasks`/`scm`/`snippets`/`authentication`/`terminal`/`extensions` namespaced + `extensionsModule`.
- `test/fase4.test.ts` — **baru**, 280 baris, 28 test Fase 4 (debug 4, tasks 4, scm 4, snippets 4, authentication 3, terminal 5, extensions 1, 14-namespace 2, host contributions 1) via bun:test.
- `dist/` — hasil `bun run build` ter-generate ulang (tambahan 6 `.d.ts` Fase 4 + `api/extensions` diperluas).
- `IMPLEMENTATION_LOG.md` — entry Fase 4 (file ini).

### Catatan Tambahan

- Verifikasi: `bunx tsc --noEmit` exit 0 (strict, `src` only), `bun run build` emit `dist/` 6 `.d.ts` Fase 4 tanpa error.
- Verifikasi: `bun test` — **166 pass, 0 fail, 373 expect()** (5 files: `utils.test.ts` 1 + `manifest.test.ts` 78 + `host.test.ts` 40 + `fase3.test.ts` 19 + `fase4.test.ts` 28). Termasuk `tasks` execution async (`queueMicrotask` → `onDidEndTask`), `debug` throw `NotImplementedError`, `scm` duplicate throw, `snippets` case-insensitive, `authentication` throw, `terminal` buffer, `extensions` host `exports` check, 14-namespace `sdk.commands`..`sdk.terminal` exist, `validateManifest` debuggers/taskDefinitions/snippets valid.
- Eksplisit cek nyata vs stub di `src/` (root repo) dan `src-tauri/` untuk tiap namespace Fase 4 (read-only, tidak ubah core — hanya via delegate injection):
  - **terminal nyata** — bukti: `src/lib/stores/terminal.ts:74` (`newTerminal(type,cwd,opts)→id`, `closeTerminal`, `killProcess`, `registerProcess`, `terminalStore.addOutputLog`), `TerminalInstance.svelte:1` (`tauri-pty spawn(SHELL_BINARIES[type],...,{cwd,env})`, `ptyProcess.onData→term.write`, `term.onData→ptyProcess.write`, `onExit→closeTerminal`, `resize` via `FitAddon`), `bottomPanelRegistry.ts:11` panel `terminal` (order, badge), `terminalCommands.ts:13` 6 commands (`new`/`toggle`/`killActive`/`clear`/`maximize`/`show`), `BottomPanel.svelte:9` terminal UI (`TerminalInstance` per `term.id`, resizeObserver). Keputusan: **implementasi nyata** (fallback in-memory + delegate ke `terminalStore`; `createTerminal` bukan stub).
  - **scm nyata (generic)** — bukti: `scmRegistry.ts:9` (`ScmProvider {id,label,priority,loadComponent,getBadgeText,when}`, `register/getAll/getVisible/onDidChange`, default `git` provider `loadComponent→SourceControlPanel`), `services/git.ts:1` 308 baris (`RepoState`, `GitProgress Channel`, `runNetworkOp` push/pull/fetch `Channel<GitProgress>`, `cloneRepository`, `getRepoState`, `commit`, `stage/unstage`), `stores/gitRepo.ts:1` 384 baris (`refreshRepo` debounced, `git-status-refresh` 400 ms, `git-decorations-changed`, `sync` publish). `SourceControlPanel` sudah ada. Keputusan: **real bridge sebagian** — `scm.createSourceControl` CRUD nyata (inputBox+groups), host wiring ke view adalah gap rendah (tapi data flow `resourceStates=` sudah siap).
  - **tasks nyata (provider)** — bukti: `taskRegistry.ts:9` 102 baris (`TaskDefinition {id,label,group,command,args,cwd}`, `TaskProvider {id,label,provideTasks,resolveTask,priority,when}`, `register/getAll/provideAll(on workspaceRoot)`, default `shell` provider priority 100), `contrib/debug/taskProviders.ts:1` 64 baris (`npm` `package.json.scripts`→tasks build/test/none + `cargo` Cargo.toml→build/test/run), `services/runService.ts:334` (`taskRegistry.provideAll(workspaceFolder)→taskConfigs`, dedup `taskConfigs` → `RunConfiguration` type `task`, `command→launchInTerminal`). Keputusan: **real bridge sebagian** — `registerTaskProvider` langsung ke `taskRegistry.register`, `executeTask` hybrid (fallback emit tanpa shell, delegate dapat spawn terminal).
  - **snippets nyata (declarative)** — bukti: `snippetRegistry.ts:29` 72 baris (`Snippet {prefix,description,body,scope}`, `SnippetContribution {language,snippets,priority}`, `register/registerAll/getForLanguage/getAll`), `editor/snippetCompletion.ts:14` (`snippetSource` via `EditorState.languageData.of({autocomplete:snippetSource})` + `snippetCompletion(body,{label,detail,type:'keyword'})`), `contrib/editor/snippets.ts:10` 6 bahasa (js/ts/py/rust/go/md), `commonExtensions.ts:74` `snippetCompletionExtensions()`. Keputusan: **declarative real** — `contributes.snippets` langsung ke `snippetRegistry`, tidak stub.
  - **debug stub** — bukti: `git grep DAP` 0 hit di `src-tauri/src` (lib.rs, file_ops.rs, git_service.rs, search.rs), `src` hanya `contrib/debug/contribution.ts:1` 18 baris (register view `run` saja) + `taskProviders.ts` (bukan DAP), `runRegistry.ts:10` + `runService.ts:1` 772 baris (run without debug), tidak ada `Breakpoint`/`DebugAdapter`. `NOTRON_MODULARITY_MAP` Batasan #4 (webview/CSP) terkait. Keputusan: **stub bertipe** (signature final + `NotImplementedError` untuk `startDebugging`; declarative `debuggers` data-only).
  - **authentication stub** — bukti: `grep authentication` 0 hit di `src` (kecuali utest), `host/memento.ts` `InMemorySecretStorage` fallback `console.warn` belum encrypted (`NOTRON_SDK_00_INDEX` §4 SecretStorage gap), tidak ada `OAuth`/`provider` di `src-tauri`. Keputusan: **stub sangat rendah** (tipe final, `registerAuthenticationProvider` in-memory + `getSession` throw).
  - Delegate pattern diperluas: `setDebugDelegate`/`setTasksDelegate`/`setScmDelegate`/`setSnippetsDelegate`/`setAuthenticationDelegate`/`setTerminalDelegate`/`setExtensionsDelegate` (7 baru) memungkinkan SDK standalone (fallback bridgeable) dan bridged (host injects `terminalStore.newTerminal`, `scmRegistry.register`, `taskRegistry.register`, `snippetRegistry.register`, dll). Tidak ada perubahan di luar `packages/notron-sdk` (read-only core dihormati — semua bridging hanya via interface injeksi, bukan `import` langsung `src/lib/...`).
  - Manifest validation `debuggers`/`taskDefinitions`/`snippets` sudah ada Fase 1 (`validateManifest` + `manifest.schema.json`); Fase 4 memverifikasi `validateManifest` valid untuk ketiganya (`fase4.test.ts:242` host + `fase4` validate).
  - Tidak ada perubahan `package.json`/`tsconfig.json` (tetap `emitDeclarationOnly`, `type: module`, `strict`). `dist/` emit `declarationMap:true`.

**Kriteria keluar Fase 4: Tercapai.**
- Seluruh permukaan API dari dokumen 01 (semua 14 namespace) sudah punya representasi kode (nyata atau stub) — tidak ada namespace yang terlewat sama sekali. Verifikasi: `src/api` kini berisi 11 file namespace (`commands`, `window`, `workspace`, `views`, `menus`, `languages`, `theming`, `debug`, `tasks`, `scm`, `snippets`, `authentication`, `terminal`, `extensions`) + `types`/`events`; `src/index.ts` re-export 14 (`commands`/`window`/`workspace`/`views`/`menus`/`languages`/`theming`/`debug`/`tasks`/`scm`/`snippets`/`authentication`/`terminal`/`extensions`); `test/fase4.test.ts` 14-namespace import test pass; `bun test` 166 pass termasuk 14-namespace smoke.
---

## Fase 5 — Packaging: Format `.ntrn` & CLI (selesai: 2026-09-12)

Rujukan wajib: `NOTRON_SDK_04_PACKAGE_CLI.md:1` (format paket, bundling, 6 perintah) + `NOTRON_SDK_06_ROADMAP_PHASING.md: Fase 5`.

### Selesai

- **Checksums & verifyChecksum (utils)** — perluas `src/utils/index.ts:683` dengan pure-JS SHA-256 sync (FIPS 180-4, 64-round, tanpa `node:crypto` agar aman di browser bundle) + async Web Crypto `crypto.subtle` fallback. Public API: `computeChecksumSync(data) → `sha256:<hex>`, `computeChecksum(data) → Promise<string>`, `verifyChecksum(data, expected) → Promise<boolean>`, `verifyChecksumSync(data, expected) → boolean`, `generateChecksums(files)` / `generateChecksumsSync`. Verifikasi vektor `hello → sha256:2cf24dba...` dan tamper detection. Dipakai CLI saat `checksums.json` generation dan dapat dipakai ulang core nanti (`NOTRON_SDK_04_PACKAGE_CLI §1`).

- **NotronExtensionConfig type** — tambah `NotronExtensionConfig` + `defineConfig` helper + `Checksums`/`PackageSignature` di `src/types/index.ts:284` per `NOTRON_SDK_04_PACKAGE_CLI §4` (override bundler: `entry`, `outfile`, `external`, `target`, `format` `cjs`|`esm`, `minify`, `sourcemap`, `esbuildOptions`). Mendukung `notron-extension.config.{ts,js,mjs,cjs,json}` untuk IntelliSense penulis ekstensi.

- **esbuild integration** — tambah `esbuild ^0.28.2` + `adm-zip ^0.6.1` ke `package.json:10` (`dependencies`), `@types/adm-zip` + `@types/node` ke `devDependencies`; ubah `tsconfig.json:11` `emitDeclarationOnly: true → false` + `esModuleInterop: true` agar `dist/` emit JS + `.d.ts` (CLI butuh JS). Bin `notron-sdk`/`ntrn` → `dist/cli/bin.js` (`package.json:bin`). `src/cli/build.ts:1` membungkus `esbuild`: `bundle:true`, `platform: node`, `target: es2020` (configurable), `format: cjs` default (Host memuat via dynamic import interop, didokumentasikan di README; `esm` via config), `external: ['notron-sdk', ...config.external]` (peer, tidak dibundel), `sourcemap`/`minify` sesuai mode (build dev: sourcemap true/minify false; package prod: sourcemap false/minify true, `sourcesContent:false`). Dukung `--watch` via `esbuild.context().watch()`.

- **CLI 6 perintah** — buat `src/cli/utils.ts:1` (helpers: `findProjectRoot`, `readManifest`, `loadExtensionConfig`, `collectContributedFiles`, `getTemplatesDir`/`listTemplatesSync`, `validateManifestWithFiles`, `getDefaultDevExtensionsDir`), `src/cli/validate.ts:1` (`validateCommand`: cek `manifest.json` via `validateManifest` + existence `contributes.*` file refs, output `path: message` + `✖`/`✓`), `src/cli/build.ts:1` (`buildCommand`), `src/cli/package.ts:1` (`packageCommand`: validate fail-fast → prod build → kumpulkan `manifest.json`, `dist/extension.js` (dari `manifest.main`), `README.md`/`CHANGELOG.md` jika ada, file `contributes` + `assets/` rekursif → `computeChecksumSync` per file → `checksums.json` (JSON map) → ZIP `<name>-<version>.ntrn` via `AdmZip`; hapus stale `.map` dari dev build; log `signature placeholder`), `src/cli/install.ts:1` (`installLocalCommand`: baca `manifest.json` dari ZIP → `~/.notron/extensions-dev/<id>/`, `extractAllTo` + clear dest, print dev-only note + gap core), `src/cli/init.ts:1` (`initCommand`: `listTemplatesCommand` → `getTemplatesDir` (dist + src fallback) → `copyTemplateDir` (`gitignore` → `.gitignore`) → patch `manifest.json`/`package.json`/`src/extension.ts` `hello-world` → `<sanitized>`), `src/cli/index.ts:1` (`runCli(argv)` parser untuk 6 perintah + `--help`/`--version`, `createCli` back-compat), `src/cli/bin.ts:1` (`#!/usr/bin/env node` entry, `runCli(process.argv.slice(2))`).

- **Templates** — buat `src/cli/templates/hello-world/` (full: `manifest.json` `acme.hello-world`, `src/extension.ts` `import * as notron from 'notron-sdk'` + `registerCommand hello-world.hello`, `package.json` `notron-sdk ^0.1.0`, `tsconfig.json`, `gitignore` → `.gitignore`, `README.md`) + 3 stub `webview-view`/`language-support`/`custom-editor` (manifest valid minimal + `src/extension.ts` placeholder + `package.json`/`tsconfig.json`/`README.md` `stub`). `dist/cli/templates` disalin via `fs.cpSync` post-`tsc` (fallback `getTemplatesDir` cari `dist` lalu `src`). `files: ["dist","src/cli/templates"]` agar template terbawa publish.

- **package.json / tsconfig** — `bin` `notron-sdk`/`ntrn`, `files`, `scripts` `test`/`typecheck`, `dependencies` `esbuild`/`adm-zip`, `emitDeclarationOnly: false`.

- **README dev-loop** — rewrite `README.md:1` dengan Pendahuluan, Struktur, Instalasi, Quick Start, Konsep Inti, Referensi API (14 namespace tabel), CLI Reference (6 perintah + bundling details + checksums), Status Implementasi, Dev Loop (`init → build --watch → install-local → package`), Bundling Details (esbuild cjs, external, config), Checksums & Code Signing, Contributing, License. Memenuhi `NOTRON_SDK_04_PACKAGE_CLI §5` + `NOTRON_SDK_05_TESTING_DX §5` (CLI ref + tabel status).

- **Tests Fase 5** — buat `test/fase5.test.ts:1` 16 test (checksums 6, validate file refs 2, list-templates/init 3, build+package flow 3, install-local 1, validate negative 1) via `bun:test`. Total suite kini 182 pass (utils 1 + manifest 78 + host 40 + fase3 19 + fase4 28 + fase5 16). Termasuk full flow `init hello-world → build → package` (verifikasi `dist/extension.js` ada, `.ntrn` ZIP 4 file tanpa `.map`, `checksums.json` 3 entries verified + tamper detection), `examples/hello-world` build+package (verifikasi repo example), `package` gagal pada manifest invalid (no `.ntrn`), `install-local` ekstrak ke `tmpDev`, `validate` menangkap missing contributed file & invalid manifest.

- **Build verification** — `bunx tsc --noEmit` exit 0, `bun run build` emit `dist/` (JS + `.d.ts`) + `dist/cli/templates` raw copy, `bun test` 182 pass.

### Stub / Belum Bisa Diimplementasikan Penuh

- **Template `webview-view` / `language-support` / `custom-editor`** — **stub folder** (manifest valid minimal + placeholder `src/extension.ts` + README `stub`). Alasan: waktu terbatas Fase 5, prioritas `hello-world` wajib; gap inti tetap (`window.registerWebviewViewProvider`/`registerCustomEditorProvider`/`languages` provider runtime belum di-bridge ke core per `NOTRON_MODULARITY_MAP` Batasan #4 & Fase 3 gap). Tidak blocking kriteria keluar (`NOTRON_SDK_04_PACKAGE_CLI §3`: template lain boleh stub dengan catatan). Dicatat di log ini.

- **Code signing (`signature`)** — **placeholder** `signature?: string` tidak diimplementasikan (`NOTRON_SDK_04_PACKAGE_CLI §1` eksplisit: cukup sediakan field kosong + catat rekomendasi lanjutan). `checksums.json` hanya map `sha256:`, log package cetak `note: code signing not implemented — placeholder reserved`. Rekomendasi: penerbit tanda tangan digital (mis. `minisign`/`cosign`) di luar cakupan sekarang.

- **`install-local` core gap** — `install-local` **berfungsi** (ekstrak `.ntrn` ke `~/.notron/extensions-dev/<id>/` + cek `manifest.json` dari ZIP), tapi **Notron core belum punya loader** `.ntrn` atau watcher untuk `extensions-dev` (verifikasi `grep ntrn` di `src`/`src-tauri` 0 hit; `host` sudah punya `ExtensionHost` tapi tidak ada Tauri `load_startup_state` untuk `.ntrn`). Perintah mencetak catatan dev-only jelas per spec §3. Gap prioritas tinggi, dicatat di laporan akhir nanti (bukan blocking CLI).

- **`notron-extension.config.ts` TS loading** — `loadExtensionConfig` mendukung `.ts` via `import(pathToFileURL)` (Bun bisa, Node ESM mungkin perlu loader). Jika `.ts` gagal di-load di Node murni, fallback ke defaults + warning. Tidak blocking (JSON/JS tetap work, TS direkomendasikan via Bun).

### File yang Diubah/Ditambah

- `src/types/index.ts` — **diubah**: tambah `NotronExtensionConfig`/`defineConfig`/`Checksums`/`PackageSignature` (§4).
- `src/utils/index.ts` — **diubah**: tambah `sha256HexSync` pure-JS + `computeChecksumSync`/`computeChecksum`/`verifyChecksum`/`verifyChecksumSync`/`generateChecksums`/`generateChecksumsSync` (§1).
- `src/index.ts` — **diubah**: tambah `export * from './utils/index.js'` (expose checksum utils).
- `package.json` — **diubah**: tambah `bin` `notron-sdk`/`ntrn`, `files` `src/cli/templates`, `scripts` `test`/`typecheck`, `dependencies` `esbuild`/`adm-zip`, `devDependencies` `@types/adm-zip`/`@types/node`.
- `tsconfig.json` — **diubah**: `emitDeclarationOnly: false`, `esModuleInterop: true`, `allowSyntheticDefaultImports: true`.
- `src/cli/utils.ts` — **baru**, 250 baris: helpers projectRoot, manifest I/O, config loader, contributed files, templates dir, validate with files, dev dir.
- `src/cli/validate.ts` — **baru**, 60 baris: `validateCommand`.
- `src/cli/build.ts` — **baru**, 90 baris: `buildCommand` (esbuild, watch).
- `src/cli/package.ts` — **baru**, 130 baris: `packageCommand` (validate→prod build→collect→checksums→ZIP).
- `src/cli/install.ts` — **baru**, 70 baris: `installLocalCommand`.
- `src/cli/init.ts` — **baru**, 140 baris: `initCommand`/`listTemplatesCommand`.
- `src/cli/index.ts` — **rewrite** (dari stub 12 baris → 130 baris): `runCli` + 6 commands + help.
- `src/cli/bin.ts` — **baru**, 15 baris: shebang entry.
- `src/cli/templates/hello-world/` — **baru**: `manifest.json`, `src/extension.ts`, `package.json`, `tsconfig.json`, `gitignore` (→ `.gitignore`), `README.md`.
- `src/cli/templates/webview-view/` — **baru stub**: `manifest.json`, `src/extension.ts`, `package.json`, `tsconfig.json`, `README.md`.
- `src/cli/templates/language-support/` — **baru stub**: idem.
- `src/cli/templates/custom-editor/` — **baru stub**: idem.
- `test/fase5.test.ts` — **baru**, 320 baris, 16 test Fase 5.
- `dist/` — hasil `bun run build` ter-generate ulang (`dist/cli/*.js` + `dist/cli/templates` raw copy, `dist/utils` checksum, `dist/types` config).
- `README.md` — **rewrite** (dari 33 baris scaffolding → 150+ baris: struktur, instalasi, quick start, konsep, API ref, CLI ref, dev loop, bundling, checksums, contributing).
- `IMPLEMENTATION_LOG.md` — entry Fase 5 (file ini).

### Catatan Tambahan

- Verifikasi: `bunx tsc --noEmit` exit 0, `bun run build` emit `dist/` + `dist/cli/templates` (via `fs.cpSync`), `bun test` **182 pass, 0 fail, 439 expect()** (6 files: utils 1 + manifest 78 + host 40 + fase3 19 + fase4 28 + fase5 16) — termasuk full flow `init → build → package` untuk `hello-world` template (`dist/extension.js` 2.1 KB dev / 960 B prod, `.ntrn` 4 file, checksums verified) dan `examples/hello-world` (`dist/extension.js` 29 KB dev / 17 KB prod, `.ntrn` 4 file, checksums verified + tamper detection), plus `validate` invalid (8 errors: id/name/version/publisher/engines/main/activationEvents/permissions) dan `package` fail-fast (no `.ntrn`), `install-local` ekstrak ke `tmpDev` + manifest id match.
- Manual CLI smoke (di luar `bun test`): `node dist/cli/bin.js --help` cetak help 6 perintah; `list-templates` cetak 4; `init my-test-ext --template hello-world` → `build` → `package` → `hello-world-0.1.0.ntrn` valid (ZIP: `manifest.json`, `dist/extension.js`, `README.md`, `checksums.json` 3 entries, tanpa `.map`; checksums verified via `computeChecksumSync`); `validate invalid-manifest.json` → 8 errors + exit 1; `install-local *.ntrn` → `~/.notron/extensions-dev/acme.my-test-ext` (4 file).
- Eksplorasi core gap `.ntrn` loader: `grep ntrn` di `src`/`src-tauri` 0 hit; `src-tauri/src/db.rs`/`lib.rs`/`startup.rs` tidak ada baca `.ntrn` atau `extensions-dev`; `ExtensionHost` di SDK sudah ada tapi core belum inject. Dicatat sebagai gap prioritas tinggi untuk laporan akhir Fase 7 (bukan blocking Fase 5).
- Keputusan desain: `format: cjs` default (bukan `esm`) — Host akan `import()` dengan interop CJS `default` (lebih kompatibel dengan `require('notron-sdk')` peer). `esm` tetap tersedia via `notron-extension.config.ts` `format: 'esm'`. Didokumentasikan di README + `build.ts` komentar.
- `emitDeclarationOnly: false` diperlukan agar `dist/cli/*.js` ter-emit untuk `bin`; `declaration: true` tetap agar `dist/*.d.ts` ter-generate. Tidak breaking Fase 1–4 (sebelumnya `emitDeclarationOnly: true` hanya untuk `.d.ts`, sekarang juga JS — `bun test` tetap via `bun` transpiler, `tsc --noEmit` tetap gate).
- Tidak ada perubahan di luar `packages/notron-sdk` (read-only core dihormati): eksplorasi `.ntrn` hanya via `grep` read-only.
- Template `hello-world` sudah diuji e2e; 3 template lain stub dengan README `belum lengkap` + log Fase 5 — sesuai `NOTRON_SDK_04_PACKAGE_CLI §3` (minimal 2 template, `hello-world` wajib, sisanya boleh stub dengan catatan).

**Kriteria keluar Fase 5: Tercapai.**
- `notron-sdk init` → `notron-sdk build` → `notron-sdk package` berhasil menghasilkan file `.ntrn` valid untuk `examples/hello-world` (ZIP 4 file, `manifest.json`/`dist/extension.js`/`README.md`/`checksums.json` 3 entries, checksums verified, tanpa `.map`; `bun test` 16 Fase 5 pass termasuk flow ini).
- `notron-sdk validate` berhasil menangkap manifest yang sengaja dibuat invalid (8 errors, exit 1; `package` juga fail-fast tanpa `.ntrn`).

---

## Fase 6 — Testing, Mock Host, Dokumentasi (selesai: 2026-09-12)

Rujukan wajib: `NOTRON_SDK_05_TESTING_DX.md:1` (Testing QA & DX — unit, integration, Mock Host, lint, typecheck, README, CHANGELOG) + `NOTRON_SDK_06_ROADMAP_PHASING.md: Fase 6`.

### Selesai

- **Mock Host `notron-sdk/testing` (14 namespace, §2)** — buat `src/testing/index.ts:1` (446 baris) dengan `createMockNotron(options?: {reset?:boolean}) → MockNotron` + `createMockContext(id | MockContextOptions)` + `resetAllMocks()`. In-memory untuk **ke-14 namespace** `NOTRON_SDK_01_API_SURFACE.md:1` (commands, window, workspace, views, menus, languages, theming, debug, tasks, scm, snippets, authentication, terminal, extensions) — boleh sederhana untuk stub (stub tetap `throw NotImplementedError` deskriptif, bukan silent). Helper `withHelpers<T>(ns, helpers) → Proxy` memakai `Proxy` + `Reflect.get` agar `mock.window.activeTextEditor` getter tetap delegasi ke `windowApi.window.activeTextEditor` (tidak hilang saat spread). Tiap namespace di-proxy dengan introspection `__*` (mis. `commands.__getRegisteredIds` alias `__getRegisteredCommands`, `window.__getShownMessages`/`__getStatusBarItems`/`__getOutputChannelText`/`__setActiveTextEditor`, `workspace.__fireDidOpen`/`__getFsEntries`, `views.__getViewContainers`/`__getTreeDataProviders`, `menus.__getMenuContributions`/`evaluateWhenClause`, `languages.__getCompletionProviders`/`__getLanguageConfigurations`/`__clearLanguages`, `theming.__setActiveTheme`, `debug.__getAdapterFactories`, `tasks.__getTaskProviders`, `scm.__getSourceControls`, `snippets.__getLanguageSnippetMap`, `authentication.__getAuthenticationProviders`, `terminal.__getTerminals`, `extensions.__registerExtensionInfo`). 14 namespace terwakili, diuji 34 test di `test/testing.test.ts` (lihat di bawah). Dipakai juga internal untuk sebagian unit test (spec §2: "Dipakai juga secara internal oleh SDK sendiri").

- **package.json / tsconfig — testing exports & lint** — tambah `package.json:exports "./testing": {types: "./dist/testing/index.d.ts", import: "./dist/testing/index.js"}` (`src` only, `dist/testing` ter-generate via `tsc`), `tsconfig.json:paths "notron-sdk/testing": ["./src/testing/index.ts"]` agar `import from 'notron-sdk/testing'` resolve via bundler, `package.json:scripts lint/lint:fix` (`eslint src test --ext .ts`), `devDependencies` `eslint@8.57.1` + `@typescript-eslint/parser@6.21.0` + `@typescript-eslint/eslint-plugin@6.21.0`, buat `.eslintrc.cjs` (parser `@typescript-eslint/parser`, `extends eslint:recommended` + `plugin:@typescript-eslint/recommended`, `ignorePatterns dist/node_modules/examples/**/dist/templates`, `rules prefer-const warn, no-console off, no-unused-vars off via @typescript-eslint/no-unused-vars with ignore ^_`). Setup minimal tapi konsisten dengan gaya Notron (cek monorepo, tidak ada eslint sebelumnya — buat baru). Script `typecheck` (`tsc --noEmit`), `lint` (`eslint src test --ext .ts`), `test` (`bun test`), `build` (`tsc -p tsconfig.json`) semua lolos (lihat Catatan Tambahan).

- **tests — fase 6** — buat `test/testing.test.ts:1` (360 baris) 34 test baru: 14-namespace reachable (commands/window/workspace/views/menus/languages/theming/debug/tasks/scm/snippets/authentication/terminal/extensions), `createMockContext` isolated Memento, `__resetAll` clears every registry, commands register/execute/getCommands/duplicate/isolate, window notifications/status/output/tabs/withProgress/custom editor stub throw, workspace fs CRUD+onFileSystemChange/config+events/provider stub/workspaceFolders, views CRUD+Tree/Webview providers via host, menus evaluateWhenClause + getMenus filter, languages providers + DiagnosticCollection + setLanguageConfiguration, theming get/set event, debug stub startDebugging throw, tasks execute fallback async, scm createSourceControl, snippets register/case-insensitive/validate, authentication register/throw, terminal create/sendText/events/profile/link, extensions via `__registerExtensionInfo`, integration `myExtension.activate` → `executeCommand` → `__getShownMessages` (spec example §2) + `hello-world` smoke via mock, `validateManifest` types invalid. `beforeEach` `resetAllMocks()` untuk isolasi. Total suite kini **216 pass, 0 fail, 542 expect()** (7 files: `utils.test.ts` 1 + `manifest.test.ts` 78 + `host.test.ts` 40 + `fase3.test.ts` 19 + `fase4.test.ts` 28 + `fase5.test.ts` 16 + `testing.test.ts` 34). Menutup gap `NOTRON_SDK_05_TESTING_DX §1.1` (utils/types/api/host/cli) dan `§1.2` integration `init→build→package→extract` (sudah ada Fase 5, diverifikasi ulang di Fase 6 via `testing.test.ts` + `fase5.test.ts`).

- **lint fixes** — perbaiki 2 error `eslint src test --ext .ts`: `src/api/snippets.ts:139` `no-dupe-else-if` (duplicate branch `else if (before.length===0 && cur.length===0)` tidak terjangkau setelah `if (cur.length===0) delete` — hapus branch, tambah `void before`), `src/cli/utils.ts:27` `no-constant-condition` (`while(true)` → `// eslint-disable-next-line no-constant-condition`). Perbaiki 2 warning `prefer-const`: `src/api/menus.ts:354` `let p → const p`, `src/utils/index.ts:713` `let len → const len`. Setelah fix `eslint src test --ext .ts` exit 0, 0 errors, 0 warnings (sebelumnya 2 errors + 2 warnings).

- **README** — rewrite total `README.md:1` (dari 201 baris → 350+ baris) sesuai struktur `NOTRON_SDK_05_TESTING_DX.md §5` 8 poin wajib: 1) Pendahuluan (analogi `vscode`, prinsip command-based `namespace.aksi`), 2) Instalasi (`npm install notron-sdk`, peer `external`), 3) Quick Start lengkap `init`→`package` (8 langkah, struktur folder, manifest snippet, dev loop), 4) Konsep Inti (Extension, Manifest, Activation Event, Contribution Point 13 kategori, Disposable + `IMPLEMENTATION_LOG` tautan), 5) Referensi API (14 namespace tabel Status/Catatan/Signature, host runtime, TSDoc manual belum TypeDoc), 6) CLI Reference (6 perintah `init`/`build`/`package`/`validate`/`install-local`/`list-templates` + `notron-extension.config.ts` + bundling details `esbuild cjs`), 7) Status Implementasi (tabel jujur per namespace/API: **Stabil**/**Stub**/Deprecated — diambil dari `IMPLEMENTATION_LOG.md` Fase 1–5, konsisten dengan Changelog, 0 Deprecated), 8) Testing & DX (unit 7 files 216 pass + Mock Host `notron-sdk/testing` contoh `createMockNotron`/`createMockContext`/`__getShownMessages`/`__resetAll` + lint/typecheck + examples `hello-world`/`sidebar-counter`/`markdown-preview` di `packages/notron-sdk/examples/` karena batasan HANYA `packages/notron-sdk` boleh tulis — root `examples/` tidak dibuat), 9) Contributing (scripts `typecheck/lint/test/build`). Memenuhi kriteria "self-contained" (§5: README bisa dipakai orang lain dari nol tanpa bertanya).

- **CHANGELOG** — buat `CHANGELOG.md:1` entry rilis awal `[0.1.0] — 2026-09-12` dengan 4 bagian: Ditambahkan (types/manifest, utils checksum, 14 namespace stabil/stub, host, CLI 6 perintah, testing Mock Host, examples, dx), Stub/Belum (10 baris jujur: window custom/webview, workspace fsProvider, languages providers, theming path, debug, tasks hybrid, auth, templates stub, signature placeholder, install-local loader), Diuji (216 pass, `tsc --noEmit` exit 0, `eslint` 0 errors), Catatan (format `.ntrn` ZIP, testing exports, Deprecated 0). Memenuhi `NOTRON_SDK_05_TESTING_DX.md §6`.

- **Build** — `bunx tsc --noEmit` exit 0 (strict `noUnusedLocals/Params` lolos, `src/testing` `Proxy` `Reflect.get` terekam sebagai `any` tapi aman `skipLibCheck`), `bun run build` (`tsc -p tsconfig.json`) emit `dist/` 15 file baru `dist/testing/*` + `dist/api`/`host`/`cli`/`types`/`utils` (total `dist/testing/index.d.ts` + `index.js` + maps), `bun test` 216 pass.

### Stub / Belum Bisa Diimplementasikan Penuh

Semua stub di bawah **tetap** sebagai `throw new NotImplementedError(feature, detail)` deskriptif per `NOTRON_SDK_00_INDEX.md:2` (bukan silent). Tidak ada stub baru di Fase 6 — Fase 6 hanya membungkus stub yang sudah ada dari Fase 2–5 ke dalam Mock Host (boleh sederhana untuk yang stub, tetap jujur). Untuk kejujuran, daftar ulang stub dari Fase 1–5 yang masih berlaku di `notron-sdk/testing` (Mock Host menyisipkan stub yang sama, tidak menyembunyikan):

- `window.registerCustomEditorProvider`/`openEditorToSide`/`createWebviewPanel`/`registerWebviewViewProvider` (window) — **Stub** (Notron core hanya `MarkdownPreview`/`ImageViewer`/`DiffEditor` hardcoded `previewRegistry.ts:14` + `SplitView`, belum generik + CSP Batasan #4). Mock Host tetap `throw` agar penulis ekstensi tahu gap, tapi tetap terdaftar sebagai signature final + `__getCustomEditorProviders` introspection.

- `workspace.registerFileSystemProvider` — **Stub (prioritas rendah)** (§3.5, virtual FS remote/in-memory) — core hanya `TauriFileService` lokal. Mock Host `throw`.

- `languages` providers (completion/hover/definition/formatting) — **Stub prioritas tinggi** — highlighting sudah modular (80+ bahasa `languageDetector.ts:LANG_LOADERS` + `languageRegistry` per-bahasa + `Editor.svelte:langCompartment` lazy), tapi provider runtime `registerCompletionItemProvider` etc disimpan in-memory + `warnStubOnce`, belum bridge ke `editorExtensionRegistry`/`Compartment`. Mock Host menyediakan `__getCompletionProviders` dll untuk inspeksi, tapi eksekusi nyata menunggu core. `DiagnosticCollection`/`setLanguageConfiguration`/`matchesSelector` **Stabil**.

- `theming` declarative `path` dynamic import — **Stub ringan** — `contributes.themes[].path` disimpan `ThemeContribution` via `__registerTheme`, belum `import()` + `applyThemeVariables` (`themes/index.ts:159` 51 tema). Mock Host `__getContributedThemes`.

- `debug` (`registerDebugAdapterDescriptorFactory` in-memory + `warnStubOnce`, `startDebugging` `throw NotImplementedError`) — **Stub prioritas rendah** — core tanpa DAP host (hanya `runService`/`runProviderRegistry` run tanpa debug; `grep DAP` 0 hit). Mock Host sama.

- `tasks` (`registerTaskProvider` real bridge ke `taskRegistry` bila delegate, `executeTask` hybrid fallback tanpa shell `cargo`/`npm` spawn) — **Sebagian stub** (execution gap rendah). Mock Host sama (fallback `queueMicrotask`).

- `authentication` (`registerAuthenticationProvider` in-memory + `warnStubOnce`, `getSession` `throw`) — **Stub sangat rendah** — core tanpa auth/OAuth. Mock Host sama.

- CLI templates `webview-view`/`language-support`/`custom-editor` — **Stub folder** (manifest valid minimal + placeholder `src/extension.ts`).

- `signature` (code signing) — **Placeholder** `signature?: string` tidak diimplementasikan (rekomendasi `minisign`/`cosign`).

- `install-local` loader — **Dev only** (ekstrak `.ntrn` ke `~/.notron/extensions-dev/<id>/`, core belum punya loader `.ntrn`/`watcher` untuk folder ini — gap prioritas tinggi, dicatat di setiap fase).

> Tidak ada stub baru di Fase 6 selain yang di atas (sudah ada di Fase 2–5). Mock Host sengaja mengekspos stub yang sama agar penulis ekstensi dapat `expect(...).toThrow(NotImplementedError)` di test mereka (contoh `testing.test.ts: custom editor stub`).

> Batasan tugas: **HANYA `packages/notron-sdk` boleh ditulis**. Root `examples/` (`examples/hello-world` dll per `NOTRON_SDK_05_TESTING_DX §4`) tidak dibuat di `C:\Users\... \Notron\examples` agar tidak melanggar read-only. Contoh referensi tetap di `packages/notron-sdk/examples/` (3 folder, sama dengan Fase 3+5) — didokumentasikan di README §8 sebagai deviasi yang disengaja karena batasan tugas, dengan catatan "sejajar `packages/`" adalah lokasi ideal di luar batasan.

### File yang Diubah/Ditambah

- `src/testing/index.ts` — **baru**, 446 baris: Mock Host 14 namespace via `Proxy` + `withHelpers`, `createMockNotron`/`createMockContext`/`resetAllMocks` (`__resetAllMocks`).
- `package.json` — **diubah**: tambah `exports "./testing": {types: "./dist/testing/index.d.ts", import: "./dist/testing/index.js"}`, `scripts lint/lint:fix` (`eslint src test --ext .ts`), `devDependencies` `eslint@8.57.1` + `@typescript-eslint/parser@6.21.0` + `@typescript-eslint/eslint-plugin@6.21.0`.
- `tsconfig.json` — **diubah**: tambah `paths "notron-sdk/testing": ["./src/testing/index.ts"]`.
- `.eslintrc.cjs` — **baru**, 30 baris: `eslint:recommended` + `plugin:@typescript-eslint/recommended`, `ignorePatterns dist/node_modules/examples/**/dist/templates`, `rules prefer-const warn, no-console off`.
- `test/testing.test.ts` — **baru**, 360 baris, 34 test Mock Host (14-namespace + integration spec example + hello-world smoke + validate types).
- `src/api/snippets.ts` — **diubah**: hapus `else if (before.length===0 && cur.length===0)` unreachable (eslint `no-dupe-else-if`) → `void before`.
- `src/cli/utils.ts` — **diubah**: tambah `// eslint-disable-next-line no-constant-condition` di `while(true)`.
- `src/api/menus.ts` — **diubah**: `let p → const p` (prefer-const).
- `src/utils/index.ts` — **diubah**: `let len → const len` (prefer-const).
- `README.md` — **rewrite total** (201 → 350+ baris): 9 bagian sesuai §5 (Pendahuluan, Instalasi, Quick Start, Konsep Inti, Referensi API 14 namespace, CLI Reference, Status Implementasi jujur, Testing & Mock Host, Contributing).
- `CHANGELOG.md` — **baru**, 120+ baris: `[0.1.0] 2026-09-12` (Ditambahkan 14 namespace + host + CLI + testing + examples/dx, Stub/Belum 10 baris, Diuji 216 pass, Catatan).
- `dist/` — hasil `bun run build` ter-generate ulang (`dist/testing/*` + `dist/api`/`host`/`cli`/`types`/`utils`).
- `IMPLEMENTATION_LOG.md` — entry Fase 6 (file ini).

### Catatan Tambahan

- Verifikasi: `bunx tsc --noEmit` exit 0 (strict, `src` only `include: ["src"]` — test tidak di-typecheck via `exclude: ["test"]`, `isolatedModules` true, `Proxy` `any` aman `skipLibCheck`). `bun run build` emit `dist/` (JS + `.d.ts` + maps, `dist/testing` 2 file). `bunx eslint src test --ext .ts` exit 0, 0 errors, 0 warnings (setelah fix 2 errors + 2 warn). `bun test` **216 pass, 0 fail, 542 expect()** (7 files) — termasuk `testing.test.ts` 34 (mock host 14 namespace + reset + isolation + integration spec example) + `hello-world` smoke via mock + `validateManifest` types invalid; `host.test.ts` 40 (lifecycle + error isolation + Memento + EventBus + commands/window/workspace) + `fase3.test.ts` 19 (views/menus/languages/theming + examples) + `fase4.test.ts` 28 (debug/tasks/scm/snippets/auth/terminal/extensions + 14-namespace) + `fase5.test.ts` 16 (checksums + file refs + list-templates/init + build/package flow + install-local + validate negative) + `manifest.test.ts` 78 + `utils.test.ts` 1. Semua `typecheck`/`lint`/`test`/`build` lolos — kriteria keluar Fase 6 tercapai (lihat di bawah).

- Keputusan desain: Mock Host memakai `Proxy` (`withHelpers`) agar getter `window.activeTextEditor`/`workspace.workspaceFolders`/`tabGroups` tetap delegasi ke fallback store/`__set*` tanpa hilang saat `...spread`. `createMockNotron({reset:true})` default reset semua registry (isolasi antar test) — `beforeEach(() => resetAllMocks())` di `testing.test.ts` juga. `createMockContext` mengisolasi `InMemoryMemento` per `id`. Delegates (`set*Delegate(null)`) juga di-clear di `resetAllMocks()` agar host injection tidak bocor antar test. Tidak ada perubahan di luar `packages/notron-sdk` (read-only dihormati — semua bridging via interface injeksi, bukan `import` langsung `src/lib/...`).

- Tidak ada perubahan `src/cli/templates` (4 templates tetap, `hello-world` full + 3 stub) — sesuai §4, deviasi root `examples/` dijelaskan di README & log ini (batasan tugas HANYA `packages/notron-sdk` boleh tulis).

**Kriteria keluar Fase 6: Tercapai.**
- Semua script lolos tanpa error: `bun run typecheck` (`tsc --noEmit` exit 0), `bun run lint` (`eslint src test --ext .ts` 0 errors, 0 warnings), `bun test` 216 pass / 0 fail (7 files, termasuk `notron-sdk/testing` 34 test), `bun run build` (`tsc -p tsconfig.json` emit `dist/` termasuk `dist/testing`).
- README bisa dipakai orang lain untuk mulai bikin ekstensi dari nol tanpa bertanya lebih lanjut (self-contained): 9 bagian (Pendahuluan, Instalasi, Quick Start `init→package` 8 langkah + struktur folder + dev loop, Konsep Inti 5 poin + tautan dokumen, Referensi API 14 namespace tabel Status/Catatan/Signature, CLI Reference 6 perintah + config, Status Implementasi jujur per API (Stabil/Stub 10 baris), Testing & Mock Host (`createMockNotron` contoh + introspection + `__resetAll`), Contributing + Versioning.

---

## Laporan Akhir Implementasi notron-sdk

> Entry penutup konsolidasi Fase 0–6 sesuai `NOTRON_SDK_06_ROADMAP_PHASING.md` Fase 7. Disusun **hanya** dari `IMPLEMENTATION_LOG.md` Fase 0–6 (tanpa analisis ulang). Format persis: Ringkasan Umum, Tabel Status per Namespace API, Tabel Pemetaan Event & Command final, Gap di Notron Core (prioritas), Keputusan Desain Penting, Rekomendasi Lanjutan.

### Ringkasan Umum

SDK `notron-sdk` dibangun berurutan Fase 0–6 tanpa mengubah Notron core (read-only di luar `packages/notron-sdk`). Fase 0 memetakan 87 command ID unik di 12 modul (`src/lib/commands/registry.ts:48`, `commandCapabilities.ts:43`, `keybindings.ts:30`, `menuRegistry.ts:11`, `eventBus.ts:1`, `fileService.ts:43`, `platform/ipc.ts:10`, `settings.svelte.ts:36`, `themes/index.ts:159`) dan 14 event + 6 IPC namespace + struktur `db.rs:31` / `workspace_cache.rs` / `watcher_service.rs:195`. Fase 1 mengimplementasikan tipe manifest penuh (`src/types/index.ts:1` — `ExtensionManifest`, `ActivationEvent` 8 varian, `ExtensionPermission` 7 nilai, `ExtensionContributes` 13 sub-tipe, `MenuLocation` 13 lokasi) + util validasi (`src/utils/index.ts:1` — `validateManifest`/`validatePermissions`/`validateActivationEvents` + `manifest.schema.json`) dengan 79 test. Fase 2 membangun API inti `commands`/`window`/`workspace` + `host` (`ExtensionContext`, `NotronEventBus` 15 event, `Memento`/`SecretStorage`, `ExtensionHost` topologicalSort + isCompatible + error isolation) dengan delegate injection agar SDK dapat berjalan standalone (fallback in-memory) maupun bridged ke Notron (`setCommandDelegate`/`setWindowDelegate`/`setWorkspaceDelegate`), diverifikasi 119 test termasuk `examples/hello-world` smoke. Fase 3 menambah `views`/`menus`/`languages`/`theming` + `host/contributions.ts:1` declarative reader (7 kategori) + `evaluateWhenClause` 470 baris, serta verifikasi highlighting 80+ bahasa sudah modular (`languageDetector.ts:116` `LANG_LOADERS`, `languageRegistry.ts:15`, `Editor.svelte:232` `langCompartment`) — provider runtime tetap stub prioritas tinggi. Fase 4 melengkapi 14 namespace ( `debug`/`tasks`/`scm`/`snippets`/`authentication`/`terminal`/`extensions` ) dengan pengecekan nyata-vs-stub eksplisit di `src/` + `src-tauri/src` (terminal nyata `terminal.ts:74` + `TerminalInstance.svelte:1` + `tauri-pty`, scm generic `scmRegistry.ts:9`, tasks `taskRegistry.ts:9` + `taskProviders.ts`, snippets `snippetRegistry.ts:29`, debug/auth tanpa DAP/OAuth). Fase 5 mengintegrasikan `esbuild ^0.28.2` + `adm-zip`, 6 perintah CLI (`init`/`build`/`package`/`validate`/`install-local`/`list-templates`), `NotronExtensionConfig` + `defineConfig`, checksum SHA-256 pure-JS (`computeChecksumSync`/`verifyChecksum`), format `.ntrn` ZIP (`manifest.json` + `dist/extension.js` + `README` + `checksums.json`) + signature placeholder, 4 templates (`hello-world` full + 3 stub). Fase 6 membungkus semua ke `notron-sdk/testing` Mock Host 14 namespace via `Proxy` (`src/testing/index.ts:1` `createMockNotron`/`createMockContext`/`resetAllMocks`) + lint (`eslint` 8.57 + `@typescript-eslint` 6.21, `.eslintrc.cjs`) + `README.md` 350+ baris self-contained + `CHANGELOG.md` 0.1.0, menutup gate `typecheck`/`lint`/`test`/`build`.

Tingkat kesiapan SDK secara keseluruhan: **siap pakai untuk ekstensi non-webview**. Seluruh 14 namespace memiliki representasi kode (nyata atau stub bertipe `NotImplementedError` deskriptif per `NOTRON_SDK_00_INDEX.md:2`, tidak ada yang terlewat). Gate kualitas lolos konsisten tiap fase: `bunx tsc --noEmit` exit 0 (strict `noUnusedLocals/Params`, `emitDeclarationOnly` Fase 1–4 → `false` + `declaration:true` Fase 5–6 untuk emit `dist/cli/*.js`), `bun run build` emit `dist/` (JS + `.d.ts` + maps + `dist/cli/templates` raw copy), `bunx eslint src test --ext .ts` 0 errors/0 warnings (setelah fix `no-dupe-else-if`/`no-constant-condition`/`prefer-const` Fase 6), `bun test` **216 pass, 0 fail, 542 expect()** (7 files: `utils 1` + `manifest 78` + `host 40` + `fase3 19` + `fase4 28` + `fase5 16` + `testing 34`) termasuk smoke `ExtensionHost` + `examples/hello-world`/`sidebar-counter`/`markdown-preview` + flow `init → build → package` (checksums verified, tamper detection) + `validate` negatif. Tidak ada perubahan di luar `packages/notron-sdk` (read-only dihormati; bridging hanya via interface injeksi, bukan `import src/lib/...` langsung). `package.json` exports `"."` + `"./testing"` (`dist/testing`), bin `notron-sdk`/`ntrn` → `dist/cli/bin.js`.

Dari perspektif cakupan, SDK **Stabil** untuk `commands`, `workspace.fs/config/events`, `views`, `menus`, `terminal`, `tasks` (provider), `scm` (SourceControl CRUD), `snippets` (declarative), `extensions` + `host` lifecycle; **Sebagian/Hybrid** untuk `window` (95% real, 4 custom/webview stubs) dan `theming`; **Stub prioritas tinggi** yang masih menunggu core adalah `languages` provider runtime (Completion/Hover/Definition/Formatting — `warnStubOnce` + in-memory array, highlighting 80+ bahasa sudah modular), `window` CustomEditor/Webview (`NOTRON_MODULARITY_MAP` Batasan #4), dan loader `.ntrn`/`extensions-dev` watcher (CLI `install-local` berfungsi ekstrak ke `~/.notron/extensions-dev/<id>/` tetapi core belum scan). Semua stub terdokumentasi jujur di tabel Status Implementasi (0 Deprecated) dan diuji via `expect(...).toThrow(NotImplementedError)` di Mock Host, sehingga penulis ekstensi dapat mengembangkan dengan fallback yang aman sambil menunggu follow-up core.

### Tabel Status per Namespace API

| Namespace | Status | Catatan |
|---|---|---|
| **commands** | Implementasi Nyata | `registerCommand`/`executeCommand`/`getCommands` via `src/api/commands.ts:1` registry lokal + delegate `commandRegistry.execute` (`registry.ts:52/72/107`) + `KNOWN_CORE_COMMANDS` 87 ID unik Fase 0 (deduplicated) + error isolation `ExtensionRuntimeError`; `getCommands` merge lokal+core deduplicated; introspection `__getRegisteredIds`/`__clearRegistry` |
| **window** | Implementasi Nyata (sebagian) | Notifikasi `showInformation/Warning/ErrorMessage` → `notificationService`/`uiStore.addToast` fallback queue + `__getShownMessages` — nyata ringan; dialog `showQuickPick`/`showInputBox`/`withProgress` → `dialogService` delegate else `undefined`/`no-op` — nyata ringan; `createStatusBarItem` → `statusBarRegistry.register` (`statusBarRegistry.ts:24`) delegate else `StatusBarItemImpl` + Map — nyata ringan; `createOutputChannel` → buffer fallback `__getOutputChannelText` — nyata ringan; `tabGroups`/`activeTextEditor`/`visibleTextEditors` → `editorStore`/`splitStore` fallback + `__setActiveTextEditor` — nyata ringan; **4 stub bertipe** `registerCustomEditorProvider`/`openEditorToSide`/`createWebviewPanel`/`registerWebviewViewProvider(window)` `throw NotImplementedError` karena core hanya `MarkdownPreview`/`ImageViewer`/`DiffEditor` hardcoded `previewRegistry.ts:14` + `SplitView`, belum registry generik/CSP Batasan #4 — prioritas tinggi, signature final, `examples/markdown-preview` catch fallback ke `OutputChannel` |
| **workspace** | Implementasi Nyata (sebagian) | `workspace.fs` `readFile`/`writeFile`/`readDirectory`/`createDirectory`/`delete`/`rename`/`stat` dual: delegate `fileIpc`/`fileService` → `invoke read_file_text/save_file/read_directory/...` (`file_ops.rs:76`) else fallback in-memory tree `Map` + `onFileSystemChange` emit — CRUD penuh; `getConfiguration`/`update`/`onDidChangeConfiguration` dual: delegate `settingsIpc` (`db.rs:577`, `settingsRegistry.ts:34`) else `configStore` + `configEmitter` + `affectsConfiguration` — nyata ringan/nyata; `onDidOpen/Close/Save/ChangeTextDocument` + `onDidChangeWorkspaceFolders` via `Emitter` + `__fireDidOpen/Close/Save/Change` + bridge `eventBus request-open-file/split:request-close-tab` — nyata ringan; **1 stub** `registerFileSystemProvider` `throw NotImplementedError` prioritas rendah (core hanya `TauriFileService` lokal `fileService.ts:66`) |
| **views** | Implementasi Nyata | `ViewContainer`/`View`/`TreeDataProvider<T>`/`WebviewViewProvider` di `src/api/views.ts:1` (262 baris) — CRUD `getContainer(s)`/`getView(s)`/`registerTreeDataProvider`/`registerWebviewViewProvider` + delegate `ViewsDelegate` → `activityBarRegistry`/`sidebarRegistry` (`activityBarRegistry.ts:9`, `sidebarRegistry.ts:9`) else in-memory `containers`/`viewsMap`/`treeProviders`/`webviewProviders` + `viewEmitter`; declarative `contributes.viewsContainers`/`views` via `host/contributions.ts`; `examples/sidebar-counter` smoke (1 webview + 1 tree) `fase3.test.ts` 3 test |
| **menus** | Implementasi Nyata | `MenuLocation` 13 (`commandPalette`, `editor/context`, `editor/title`, `explorer/context`, `view/title`, `view/item/context`, `tab/context`, `menubar/file/edit/view/run/help`, `pane/context`) + `evaluateWhenClause` 470 baris (`src/api/menus.ts:1`) tokenizer `STRING` quoted, `IDENT` (allow `.`/`:`/`/`/`-` untuk `.js`), `&&`/`||`/`!`/`==`/`!=`/`===`/`!==`/`=~`/`>`/`<`/`>=`/`<=`/`in`/`not in`, `(`/`)`, parser `or→and→equality→unary→primary`, missing IDENT → falsy/ literal, regex `/.../flags` → `RegExp` + `getMenus(location, context)` filter via evaluator + `MenusDelegate`; declarative `contributes.menus` via `host/contributions.ts`; host belum wiring string → `() => boolean` di `menuRegistry.ts:21` — gap ringan |
| **languages** | Stub Prioritas Tinggi (sebagian) | `DiagnosticCollection` (`set`/`get`/`has`/`delete`/`clear`/`forEach`/`dispose` via `DiagnosticCollectionImpl` + `Emitter`) — **nyata**; `setLanguageConfiguration`/`matchesSelector` — nyata; declarative `languages`/`grammars` (`__registerLanguage`/`__registerGrammar` → `declaredLanguages`/`Grammars`) — nyata; **4 provider stub** `registerCompletionItemProvider`/`registerHoverProvider`/`registerDefinitionProvider`/`registerDocumentFormattingEditProvider` di `src/api/languages.ts:184` `warnStubOnce` + simpan in-memory `__getCompletionProviders` etc. — **stub prioritas tinggi**: highlighting sudah modular 80+ bahasa (`languageDetector.ts:116` `LANG_LOADERS` 80+ + `languageRegistry` per-bahasa + `Editor.svelte:232` `langCompartment.reconfigure`) terpenuhi `NOTRON_MODULARITY_MAP:9`, tapi runtime provider belum bridge ke `editorExtensionRegistry`/`Compartment`/`commonExtensions.ts:72` `autocompletion()` generic |
| **theming** | Implementasi Nyata ringan | `getActiveColorTheme()`/`onDidChangeActiveColorTheme` di `src/api/theming.ts:1` via `ThemingDelegate` (`getActiveColorTheme`, `onDidChangeActiveColorTheme`, `getContributedThemes`) else fallback `{id:'vscode-dark',kind:'dark'}` + `themeEmitter` + `__setActiveTheme`; declarative `themes`/`iconThemes`/`productIconThemes` via `__registerTheme` disimpan `ThemeContribution` — gap rendah: `path` belum dynamic import + `applyThemeVariables` (`themes/index.ts:303` 51 tema) belum terhubung |
| **debug** | Stub Bertipe Prioritas Rendah | `DebugConfiguration`/`Session`/`AdapterDescriptorFactory` di `src/api/debug.ts:1` (165 baris) + `DebugDelegate` — `registerDebugAdapterDescriptorFactory` in-memory `adapterFactories` + `warnStubOnce` + `__getAdapterFactories`; `startDebugging` `throw NotImplementedError('debug.startDebugging', 'Notron core does not yet implement DAP...Use Run...')` — core hanya `runService`/`runProviderRegistry` tanpa DAP (grep DAP 0 hit `src-tauri`); declarative `contributes.debuggers` **nyata** (data-only `declaredDebuggers` via `__registerDebugger`); events `onDidStart/TerminateDebugSession` via `Emitter` |
| **tasks** | Implementasi Nyata Sebagian (Hybrid) | `Task`/`TaskProvider`/`ShellExecution`/`ProcessExecution`/`TaskExecution` di `src/api/tasks.ts:1` (210 baris) + `TasksDelegate` — `registerTaskProvider` delegate → `taskRegistry.register` (`taskRegistry.ts:9`, `taskProviders.ts:27` npm/cargo) else in-memory `providers[]`; `executeTask` delegate else fallback `TaskExecutionImpl` + `queueMicrotask` fire `onDidStartTask`→`onDidEndTask` tanpa spawn shell; `fetchTasks` delegate atau kumpulkan provider; events `onDidStartTask`/`onDidEndTask`/`onDidStart/EndTaskProcess` via `Emitter`; declarative `taskDefinitions` via `__registerTaskDefinition` — nyata; execution real shell gap rendah |
| **scm** | Implementasi Nyata Sebagian | `SourceControl`/`ResourceGroup`/`ResourceState`/`InputBox` di `src/api/scm.ts:1` (185 baris) `SourceControlImpl` + `ResourceGroupImpl` CRUD penuh (`createResourceGroup` duplicate throw, `resourceStates` clone + fire `onDidChange`, `dispose` LIFO, `count` auto) + `Map` + `Emitter`; `createSourceControl` delegate → `scmRegistry.register` (`scmRegistry.ts:9` generic `git` + `SourceControlPanel`) else fallback; `getSourceControls`/`onDidChangeSelectedSourceControl` real; core sudah punya `git.ts:1` + `gitRepo.ts:1` + `git_service.rs:792` — SCM generic nyata, wiring `resourceStates` → view gap rendah |
| **snippets** | Implementasi Nyata (Declarative) | `Snippet`/`SnippetContribution` di `src/api/snippets.ts:1` (115 baris) + `SnippetsDelegate` — declarative `contributes.snippets` via `__registerSnippetContribution` → `snippetContributions[]` + `languageSnippetMap` case-insensitive; `registerSnippets(language,snippets)` imperative helper delegate → `snippetRegistry.register` (`snippetRegistry.ts:29`, `snippetCompletion.ts:14`, `snippets.ts` 6 bahasa js/ts/py/rust/go/md) else fallback; `getSnippetsForLanguage`/`getAllSnippetContributions`/`validateSnippet` real; `SnippetString` imperatif tidak diekspos sesuai spec — sangat rendah, bukan stub |
| **authentication** | Stub Prioritas Sangat Rendah | `AuthenticationSession`/`Provider` di `src/api/authentication.ts:1` (120 baris) + `AuthenticationDelegate` — `registerAuthenticationProvider` in-memory `Map` + `warnStubOnce` + `__getAuthenticationProviders`; `getSession` `throw NotImplementedError('authentication.getSession', 'No authentication delegate...')`; `onDidChangeSessions` via `Emitter`; core grep `authentication` 0 hit, hanya `SecretStorage` placeholder belum encrypted (`host/memento.ts:1` `InMemorySecretStorage` warn once) |
| **terminal** | Implementasi Nyata | `Terminal`/`TerminalOptions`/`Profile`/`Link` di `src/api/terminal.ts:1` (190 baris) `TerminalImpl` (id `term-${counter}-${now}`, `processId 1000+counter`, `buffer` + `sendText(addNewLine)` + `show`/`hide` + `dispose→didCloseEmitter`) + `terminals Map` + `profileProviders`/`linkProviders` + `Emitter`; `createTerminal` delegate → `terminalStore.newTerminal` (`terminal.ts:74` 222 baris `newTerminal`/`closeTerminal`/`killProcess`, `TerminalInstance.svelte:1` `tauri-pty spawn` + `xterm FitAddon`, `bottomPanelRegistry.ts:11` panel `terminal`, `terminalCommands.ts:12` 6 command) else fallback; `registerTerminalProfileProvider`/`registerTerminalLinkProvider` hybrid + events `onDidOpen/CloseTerminal` real |
| **extensions** | Implementasi Nyata | `ExtensionInfo`/`ExtensionsDelegate` di `src/api/extensions.ts:1` (39→95 baris) `localExtensions Map` + `changeEmitter`; `getExtension(id)`/`all`/`onDidChange` + `__registerExtensionInfo`/`__clearExtensions` + `setExtensionsDelegate` — real via `ExtensionHost` (`src/host/lifecycle.ts:1` `topologicalSort` DFS cycle+missing, `isCompatible` lenient `^`/`~`/`>=`/`x`, `ExtensionHost` `register`→sort, `activateAll` cek deps+compat `try/catch` `ExtensionActivationError` continue, `deactivateAll` reverse LIFO dispose, `getExtension`/`allExtensions`/`eventBus`/`hostLog`) — tidak ada stub |

### Tabel Pemetaan Event & Command (dari Fase 0 & 2)

#### Event Core → SDK Event (Final Fase 0–6)

| Event SDK | Sumber Event Core (hasil eksplorasi) | Status Final |
|---|---|---|
| `onDidOpenTextDocument` | `eventBus.emit('request-open-file', {path})` (`eventBus.ts:8`) + `openFileInActivePane()` (`App.svelte:271`, `fileOperations.ts`) → `editorStore.addTab()` + `invoke('read_file_text')` (`file_ops.rs:76`) — tidak ada Tauri emit | **Implementasi nyata ringan** — `workspace.onDidOpenTextDocument` via `Emitter<TextDocument>` + `__fireDidOpen`; host bridge `eventBus.on('request-open-file')→__fireDidOpen` saat `setWorkspaceDelegate`; fallback in-memory untuk test (`host.test.ts:40`) |
| `onDidCloseTextDocument` | `eventBus.emit('split:request-close-tab', {tabId})` (`eventBus.ts:11`, `App.svelte:287`) + `editorStore.closeTab()` — tidak ada Tauri event | **Implementasi nyata ringan** — `__fireDidClose` + bridge `eventBus.on('split:request-close-tab')`; error isolation per listener (`host/errors.ts:1`) |
| `onDidSaveTextDocument` | Command `workbench.action.files.save` → `fileService.save()` → `invoke('save_file', {path,content})` (`fileService.ts:73`, `fileCommands.ts:41`, `file_ops.rs:260`) + `editorStore.markSaved()` — tidak ada emit save | **Implementasi nyata ringan** — `__fireDidSave` + bridge dari `fileService.save()` wrapper saat delegate ada |
| `onDidChangeTextDocument` | CodeMirror `updateListener` di `Editor.svelte` → `editorStore.updateContent(tabId,content)` → `window.dispatchEvent('editor:sync-content')` (`editor.ts:279`) | **Implementasi nyata ringan** — `__fireDidChange` + bridge `window.addEventListener('editor:sync-content')→__fireDidChange` |
| `onDidChangeActiveTextEditor` | `editorStore.activeTabId.subscribe()` + `Editor.svelte:onFocus` + `eventBus 'editor:focus-group'` (`navigation.ts:152`) | **Implementasi nyata ringan** — `window.onDidChangeActiveTextEditor` via `activeEditorEmitter`; host dapat injeksi `__setActiveTextEditor` (bridges `editorStore` subscribe) |
| `onDidChangeActiveTab` (`tabGroups`) | `splitStore.panes[paneId].activeTabId` + `editorStore.activeTabId` + `split:request-close-tab` — store Svelte, bukan Tauri | **Implementasi nyata ringan** — `window.tabGroups.onDidChangeActiveTab` via `activeTabEmitter` + in-memory `TabGroup[]` + `__setTabGroups` |
| `onDidChangeWorkspaceFolders` | `eventBus.emit('request-workspace-switch', {path})` (`eventBus.ts:10`, `App.svelte:262`) + `uiStore.explorerRoot` + `load_startup_state` (`lib.rs:81`) | **Implementasi nyata ringan** — `workspace.onDidChangeWorkspaceFolders` + `__setWorkspaceFolders`; bridge `eventBus.on('request-workspace-switch')` |
| `onDidChangeConfiguration` | `settingsStore.updateSetting(key,value,scope)` (`settings.svelte.ts:111`) → `invoke('save_global_setting'/'save_workspace_setting')` (`db.rs:588`) | **Implementasi nyata** — `workspace.onDidChangeConfiguration` via `configEmitter` (fallback) atau `workspaceDelegate.onDidChangeConfigurationEmitter` (bridges `settingsStore` subscribe); `affectsConfiguration(section)` real; semula "Stub (gap belum emit)" Fase 0 → nyata Fase 2 |
| `onDidChangeActiveColorTheme` | `themeStore.setTheme()` + `applyThemeVariables(theme)` (`themes/index.ts:303`) + `localStorage THEME_KEY` | **Implementasi nyata ringan** — `theming.onDidChangeActiveColorTheme` + `getActiveColorTheme()` via `ThemingDelegate` else fallback `{id:'vscode-dark',kind:'dark'}` + `themeEmitter` + `__setActiveTheme`; semula stub Fase 0 → nyata ringan Fase 3; path dynamic import gap rendah |
| `onDidChangeTextEditorSelection` | CodeMirror `selection` → `editorStore.setCursor(tabId,{line,column})` (`Editor.svelte`) | **Stub ringan** — tipe `TextEditor.selection` (`api/types.ts`) ada, tapi emitter publik belum diekspos; menunggu bridge gutter/selection Fase 3 `languages`; gap menengah-rendah |
| `onDidChangeVisibleTextEditors` (split) | `splitStore.rootNode` (`SplitView.svelte:1`) — observable via `subscribe`, bukan event | **Stub ringan** — `window.visibleTextEditors` getter sudah nyata (fallback store), event perubahan belum; mudah jadi nyata via `splitStore` subscribe |
| `onFileSystemChange` (custom) | Tauri `watcher_service.rs:346` `app.emit('fs-change', FsChangePayload)` + `FileTree.svelte:49` + `App.svelte:844` listener; `file_ops.rs:392` `search-invalidate`/`git-invalidate` | **Implementasi nyata ringan** — `workspace.onFileSystemChange` + `workspace.fs` mutasi fallback memancarkan `fsChangeEmitter`; delegate ada → bridge `listen('fs-change')→fsChangeEmitter` |
| `onDidChangeGitDecorations` | `git_service.rs:992` `app.emit('git-decorations-changed', DecorationDelta)` → `gitDecoration.ts:59` listener | **Stub** — event core ada (`watcher_service.rs:346` `.git` watcher → `git-status-refresh`), tapi API SDK `scm` publik belum expose decoration delta ( `SDK_EVENTS.DID_CHANGE_GIT_DECORATIONS` internal ready `host/eventBus.ts:1` ); menunggu `scm` generic wiring prioritas rendah |
| `onDidChangeGitState` (repo) | `git_service.rs:792` `get_repo_state` + polled `git-status-refresh:346` (`.git` watcher → `gitRepo.ts:182` debounced 400ms) | **Stub** — sama, menunggu `scm` Fase 4; `SDK_EVENTS.DID_CHANGE_GIT_STATE` internal ready, `gitRepoStore` belum delegate |
| `onDidOpenTerminal` / `onDidCloseTerminal` | `terminalStore.newTerminal/closeTerminal` (`terminal.ts:74`) — belum emit, hanya mutation | **Implementasi nyata ringan** — `terminal.onDidOpenTerminal`/`onDidCloseTerminal` via `Emitter<Terminal>` + `TerminalDelegate` (delegate → `terminalStore` + `TerminalInstance.svelte` `ptyProcess.onExit`); Fase 0 stub → nyata Fase 4 |
| `onDidChangeViews` | `sidebarRegistry.ts:9` + `activityBarRegistry.ts:9` registry-driven viewContainer/view CRUD | **Implementasi nyata** — `views.onDidChangeViews` via `viewEmitter`; declarative `viewsContainers`/`views` via `host/contributions.ts` sebelum `activateAll` |
| `onDidChangeDiagnostics` (languages) | `languages.createDiagnosticCollection` `DiagnosticCollectionImpl` + `Emitter` | **Implementasi nyata** — CRUD `set`/`get`/`has`/`delete`/`clear`/`forEach` real; tidak butuh bridge editor |
| `onDidStartTask` / `onDidEndTask` | `taskRegistry.ts:9` `provideAll` → `runService.ts:334` task→Run | **Hybrid nyata** — `tasks.onDidStartTask`/`onDidEndTask` via `Emitter` + fallback `queueMicrotask` (tanpa shell) + delegate ke `taskRegistry`/`terminal` bila ada |
| `onDidChangeSelectedSourceControl` | `scmRegistry.ts:9` + `SourceControlImpl` `resourceStates` setter | **Implementasi nyata** — `scm.onDidChangeSelectedSourceControl` via `Emitter<SourceControl>` |
| `onDidChangeSessions` (auth) | Tidak ada di core (grep `authentication` 0 hit) | **Stub** — `authentication.onDidChangeSessions` via `Emitter` in-memory + `warnStubOnce`; `getSession` throw |
| `onDidChange` (extensions) | `ExtensionHost` lifecycle `register`/`activateAll`/`deactivateAll` | **Implementasi nyata** — `extensions.onDidChange` via `changeEmitter` + `ExtensionHost.allExtensions` |

#### Command / Invoke Core → SDK Command (Final)

| Command SDK (`executeCommand(id)`) | Sumber Core (registry / invoke) | Status Final |
|---|---|---|
| `commands.registerCommand` / `getCommands` / `executeCommand` | `commandRegistry.register/get/execute` (`registry.ts:52/72/107`) + `toPaletteItems:136` + `executeCommand(id)` helper `registry.ts:150` | **Implementasi nyata** — `src/api/commands.ts:1` registry lokal + `setCommandDelegate` → `commandRegistry.execute` nyata; `getCommands(filterInternal?)` merge lokal+`KNOWN_CORE_COMMANDS` 87 deduplicated; error isolation `ExtensionRuntimeError` |
| `workbench.action.files.save/saveAs` | `fileCommands.ts:41/61` → `fileService.save()` → `invoke('save_file')` (`file_ops.rs:260`) | **Implementasi nyata via delegate** — terdaftar `KNOWN_CORE_COMMANDS`; `executeCommand` forward ke core saat delegate ada |
| `workbench.action.files.newUntitledFile/newFile/openFile/openFolder` + `openRecent`/`newWindow` | `fileCommands.ts:22/24/28/36` → `handleNewTextFile` / `@tauri/plugin-dialog open()` + `openFileInActivePane` | **Implementasi nyata via delegate** |
| `explorer.newFile/newFolder/refresh/collapseAll/duplicate/copy/cut/paste/rename/delete/copyPath/openInTerminal/openToSide/toggleDotFiles` | `explorerCommands.ts:28` → `fileService.createFile/createDirectory/renameItem/deleteItem/copyItem` → `invoke('create_file','rename_item',...)` (`file_ops.rs:389`) | **Implementasi nyata via delegate** |
| `editor.action.*` (undo/redo/selectAll/copyLinesUp/Down/moveLinesUp/Down/find/replace/revealDefinition/referenceSearch, `editor.action.clipboard*`, `actions.find`) | `editorActionCommands.ts:12` → `eventBus.emit('editor:action', {action})` (`eventBus.ts:12`) → CodeMirror extension `Editor.svelte` | **Implementasi nyata via eventBus delegate** |
| `workbench.action.*` (toggleSidebar/showExplorer/search/scm/debug/showCommands/quickOpen/openSettings/toggleMinimap/Breadcrumbs/...) | `viewCommands.ts:10` → `uiStore.toggleSidebar/setActiveSidebarPanel` (`ui.ts`) + `window.dispatchEvent('open-command-palette')` | **Implementasi nyata via delegate** |
| `workbench.action.closeActiveEditor/closeOtherEditors/closeEditorsToTheRight/closeAllEditors/pinEditor/splitEditorRight/Down/Left/Up/focusActiveEditorGroup` | `tabCommands.ts:22/46/95/135` → `splitStore.closeTabInPane/splitPane` + `editorStore.closeTab` | **Implementasi nyata via `window.tabGroups` delegate** |
| `workbench.action.terminal.new/toggleTerminal/killActive/clear/maximize/show` + `view.problems/output` | `terminalCommands.ts:13` → `terminalStore` + `bottomPanelRegistry` | **Implementasi nyata via delegate** |
| `git.*` (init/refresh/stageAll/unstageAll/commit/push/pull/fetch/publish/cancel/reDetect/openOutput/stage/unstage/discard) | `gitCommands.ts:14` → `gitRepoStore.*` → `gitIpc` (`ipc.ts:66`) → `invoke('git_*')` (`git_service.rs:1146`) + `Channel<GitProgress>` | **Implementasi nyata via delegate** |
| `workbench.action.run/runCurrentFile/stopRun` + `debug.run` + `run.createLaunchJson/openLaunchJson/openFileForRunning` | `runCommands.ts:11` → `runService.runSelectedConfiguration/runCurrentFile/stopActiveRuns` → `runProviderRegistry.getForType` | **Implementasi nyata via delegate** |
| `workbench.action.preview.showCode/showPreview/showSplit` | `previewRegistry.ts:130` → `getForFile(path,language)` → `editorStore.updateTab(mdViewMode/svgViewMode)` | **Implementasi nyata via delegate** |
| `window.minimize/maximize/close/reload` | `windowCommands.ts:12` | **Implementasi nyata via delegate** |
| `workspace.fs.*` (`readFile`/`writeFile`/`readDirectory`/`createDirectory`/`rename`/`delete`/`stat`) | `fileIpc.*` (`ipc.ts:11`) / `fileService.*` (`fileService.ts:66`) → Tauri `invoke('read_file_text','read_directory','create_file',...)` (`file_ops.rs:76`) + `workspace_cache.rs` + chunking `CHUNK_THRESHOLD_LOW 512KB/HIGH 5MB` | **Implementasi nyata ringan** — dual delegate/fileService saat ada else fallback in-memory tree (fully functional untuk test/smoke) |
| `workspace.getConfiguration/update` / `onDidChangeConfiguration` | `settingsIpc` (`ipc.ts:37`) → `invoke('load/save_global_settings', 'load/save_workspace_settings')` (`db.rs:577`) + `settingsRegistry.ts:34` 15 schema + `HARDCODED_DEFAULTS` layering `global←workspace` | **Implementasi nyata ringan** — dual delegate settingsStore else `configStore` Map + `onDidChangeConfiguration` emitter |
| `window.showInformation/Warning/ErrorMessage` | `notificationService.ts` / `uiStore.addToast()` (`constants.ts:116` MAX_TOASTS) | **Implementasi nyata ringan** — delegate notificationService else queue fallback `__getShownMessages()` |
| `window.showQuickPick/showInputBox/withProgress` | `dialogService.ts` + `QuickPickDialog.svelte`/`InputBoxDialog.svelte` + `palette.ts:1` + `uiStore.addProcessToast` | **Implementasi nyata ringan** — delegate dialogService else `undefined` (cancelled)/no-op |
| `window.createStatusBarItem` | `statusBarRegistry.register(item)` (`statusBarRegistry.ts:10`, `statusBarContributions.ts`) `{id,alignment,priority,getText(),command,when}` | **Implementasi nyata ringan** — delegate statusBarRegistry else `StatusBarItemImpl` Map |
| `window.createOutputChannel` | `terminalStore` output log + `OUTPUT_CATEGORIES` (`constants/index.ts:102`) | **Implementasi nyata ringan** — fallback `OutputChannelImpl` buffer `__getOutputChannelText`; host dapat bridge ke bottom panel Output |
| `window.activeTextEditor/visibleTextEditors/tabGroups` | `editorStore.tabs/activeTabId` + `splitStore.panes/rootNode` | **Implementasi nyata ringan** — getter fallback + `__setActiveTextEditor`/`__setTabGroups` bridge |
| `window.registerCustomEditorProvider` / `openEditorToSide` / `createWebviewPanel` | Belum ada Custom Editor/Webview di core (hanya `MarkdownPreview.svelte:1`, `ImageViewer.svelte:1`, `DiffEditor.svelte:1`, `SplitView` hardcoded `previewRegistry.ts:14`) | **Stub bertipe** `throw new NotImplementedError` (prioritas tinggi, Batasan #4 CSP) — signature final |
| `views.registerTreeDataProvider/registerWebviewViewProvider` (views namespace) | `sidebarRegistry.ts:9` + `activityBarRegistry.ts:9` (viewContainer/view CRUD) | **Implementasi nyata** — in-memory + delegate |
| `languages.registerCompletionItemProvider/registerHoverProvider/registerDefinitionProvider/registerDocumentFormattingEditProvider` / `createDiagnosticCollection` / `setLanguageConfiguration` | `commonExtensions.ts:31`, `extensionRegistry.ts:32`, `languageRegistry.ts:15` — per-bahasa CM6 extension modular tapi provider dinamis belum pluggable runtime | **DiagnosticCollection + setLanguageConfiguration nyata**; **4 provider stub prioritas tinggi** `warnStubOnce` + in-memory (`__get*Providers`) |
| `menus.evaluateWhenClause` + declarative menus | `when` di `menuRegistry.ts:21` sudah `when: () => boolean` fungsi, bukan string | **Evaluator nyata** 470 baris (tokenizer+parser `&&`/`||`/`!`/`==`/`!=`/`=~`/regex/`in`) + `getMenus(location,context)` filter real; bridge string→function gap ringan |
| `theming.getActiveColorTheme/onDidChangeActiveColorTheme` | `themes/index.ts:159` `THEMES` 51 tema + `themeStore` + `applyThemeVariables:303` | **Implementasi nyata ringan** — `__getActiveThemeFallback`/`__setActiveTheme`; declarative `themes[].path` gap rendah |
| `debug.registerDebugAdapterDescriptorFactory/startDebugging` | `scmRegistry.ts:9` generic, `runRegistry.ts:10` per-runtime, `terminal.svelte.ts` — tidak ada DAP `Breakpoint`/`DebugAdapter` (grep DAP 0 hit) | **Stub bertipe** (declarative `debuggers` data-only nyata) |
| `tasks.registerTaskProvider/executeTask/fetchTasks` | `taskRegistry.ts:9` 102 baris + `taskProviders.ts:27` npm/cargo + `runService.ts:334` task→Run `type: task` | **Hybrid nyata** — provider bridge ke `taskRegistry.register`; execution fallback `queueMicrotask` tanpa shell PTY |
| `scm.createSourceControl` | `scmRegistry.ts:9` generic `git` (`loadComponent: SourceControlPanel` + `git.ts`/`gitRepo.ts`/`git_service.rs:992`) | **Real bridge sebagian** — CRUD penuh fallback + delegate |
| `snippets` (`getSnippetsForLanguage`/`registerSnippets`/`validateSnippet`) | `snippetRegistry.ts:29` 72 baris + `snippetCompletion.ts:14` + `snippets.ts` 6 bahasa | **Declarative real** — delegate `snippetRegistry.register` else `languageSnippetMap` case-insensitive |
| `authentication.registerAuthenticationProvider/getSession` | Tidak ada di core (0 hit) + `SecretStorage` placeholder `memento.ts` warn once | **Stub sangat rendah** — tipe final, `getSession` throw |
| `terminal.createTerminal/registerTerminalProfileProvider/registerTerminalLinkProvider` | `terminal.ts:74` 222 baris + `TerminalInstance.svelte:1` `tauri-pty spawn` + `bottomPanelRegistry` + `terminalCommands.ts` 6 command | **Implementasi nyata** (fallback `TerminalImpl` + delegate `terminalStore.newTerminal` + `ptyProcess.write`) |
| `extensions.getExtension/all/onDidChange` | Belum ada loader `.ntrn` di core — dikelola `ExtensionHost` sendiri (`host/lifecycle.ts:1`) | **Implementasi nyata sisi SDK** — `ExtensionHost.getExtension/all` Map, tidak butuh core |
| CLI `init`/`build`/`package`/`validate`/`install-local`/`list-templates` + `checksums.json` | `esbuild` bundle + `adm-zip` ZIP + SHA-256 pure-JS (`src/utils/index.ts:683`) | **Implementasi nyata** — `notron-sdk`/`ntrn` bin → `dist/cli/bin.js` (`package.json:bin`), `validate` cek `validateManifest` + file existence, `package` ZIP 4 file tanpa `.map` + `checksums.json` verified |

### Gap di Notron Core yang Menghambat SDK (Prioritas Tinggi → Rendah)

1. **Loader `.ntrn` & watcher `extensions-dev` / `extensions` belum ada** — dampak: `install-local` hanya ekstrak ZIP ke `~/.notron/extensions-dev/<id>/` (`src/cli/install.ts:1` `extractAllTo`) dan mencetak catatan dev-only; core belum scan folder tersebut saat startup, tidak ada `invoke`/`load_startup_state` (`lib.rs:81`, `startup.rs`, `db.rs`) untuk memuat `manifest.json` + `dist/extension.js` dari `.ntrn`, sehingga ekstensi hasil `package` tidak auto-aktif di Notron — gap **prioritas tinggi** (blocking distribusi; tercatat tiap fase sejak Fase 5 `grep ntrn` 0 hit). Rekomendasi: tambah `src-tauri/src/extension_loader.rs` yang scan `~/.notron/extensions-dev` + `extensions/` saat `load_startup_state`, verifikasi `checksums.json` via `verifyChecksumSync` (reuse `utils/index.ts:683` logic di Rust `sha2`), unzip/extract jika `.ntrn` mentah, baca `manifest.json` + validasi + `engines.notron` compatibility, lalu `ExtensionHost` inject via `setCommandDelegate`/`setWorkspaceDelegate` etc. (host sudah siap, hanya perlu wiring); tambah watcher `notify::Watcher` untuk folder `extensions-dev` (reuse `watcher_service.rs:195` pattern) → `app.emit('extensions-changed')`.
2. **Webview / Custom Editor host + CSP sandbox belum ada** (`NOTRON_MODULARITY_MAP.md` Batasan #4) — dampak: `window.registerCustomEditorProvider`/`openEditorToSide`/`createWebviewPanel`/`registerWebviewViewProvider` (window) `throw new NotImplementedError` deskriptif (`src/api/window.ts:491`); `examples/markdown-preview` harus catch dan fallback ke `OutputChannel`; `previewRegistry.ts:14` hanya 2 provider hardcoded `.md`/`.svg` (`MarkdownPreview.svelte`, `ImageViewer.svelte`) tanpa isolasi. Rekomendasi: buat `WebviewHost.svelte` + `CustomEditorRegistry` generik mirip `previewRegistry` tapi dengan `webview` iframe/CSP + message passing `onDidReceiveMessage`/`postMessage` (`types.ts:Webview`), delegasi via `WindowDelegate`; prioritaskan setelah loader, karena banyak ekstensi preview membutuhkan ini.
3. **Languages provider runtime bridge ke CodeMirror 6 belum ada** — dampak: `languages.registerCompletionItemProvider`/`registerHoverProvider`/`registerDefinitionProvider`/`registerDocumentFormattingEditProvider` (`src/api/languages.ts:184`) hanya `warnStubOnce` + simpan in-memory `__getCompletionProviders` — tidak terhubung ke editor live; `examples/markdown-preview` provider tidak muncul di autocomplete/hover. Padahal highlighting per-bahasa **SUDAH modular** (80+ bahasa `languageDetector.ts:116` `LANG_LOADERS` `Record<string,Loader>` + `languageRegistry` per-bahasa + `Editor.svelte:232` `langCompartment.reconfigure` lazy; `commonExtensions.ts:31` static tanpa bahasa) — `NOTRON_MODULARITY_MAP:9` terpenuhi untuk highlight (Fase 3 verifikasi eksplisit). Rekomendasi prioritas tinggi: tambah `CompletionProviderRegistry` facet di `extensionRegistry.ts:32` / `commonExtensions.ts:72` (`autocompletion()` generic → per-provider `CompletionSource` via `EditorState.languageData` atau `Compartment`), wire `LanguagesDelegate` → `editorExtensionRegistry`; `DiagnosticCollection` sudah real (tidak butuh bridge) sebagai bukti pola yang berhasil.
4. **Event `onDidChangeTextEditorSelection` / `onDidChangeVisibleTextEditors` belum dipublish sebagai SDK event publik** — dampak: `window.visibleTextEditors` getter sudah nyata (fallback store) tapi event perubahan split (`splitStore.rootNode`) belum emit; `TextEditor.selection` tipe ada (`api/types.ts:1`) tapi tidak ada emitter; ekstensi tidak dapat bereaksi terhadap perpindahan kursor/selection untuk context-aware menus. Rekomendasi menengah-rendah: subscribe `editorStore.setCursor` + `splitStore.rootNode` di `Editor.svelte`/`navigation.ts:152`, emit via `windowDelegate` → `NotronEventBus` `SDK_EVENTS.DID_CHANGE_SELECTION`/`DID_CHANGE_VISIBLE_EDITORS` (sudah ada nama internal `host/eventBus.ts:1`).
5. **Menus `when` string → function bridge belum wiring di core** — dampak: SDK `evaluateWhenClause` 470 baris sudah real (support `&&`/`||`/`!`/`==`/`!=`/`=~`/`in`/`not in`, regex `/.../`, `editorTextFocus` truthiness, `view == 'explorer'` quoted, `resourceExtname == .md` unquoted `.` literal — `fase3.test.ts:5` lolos), `menus.getMenus(location, context)` filter real, tetapi `menuRegistry.ts:21` core masih `when?: () => boolean` (fungsi) bukan string evaluator, sehingga declarative `contributes.menus[].when` string dari ekstensi tidak otomatis menjadi `when` function di `TitleMenuBar`/`ContextMenu`. Rekomendasi rendah: host wrapper `whenString → () => evaluateWhenClause(when, buildContext(resourceExtname, view, editorTextFocus))` saat `setMenusDelegate`.
6. **Theming declarative `path` belum dynamic import** — dampak: `contributes.themes[]`/`iconThemes` disimpan `ThemeContribution {id,label,uiTheme,path}` via `__registerTheme` (`host/contributions.ts:1`) tetapi `themes/index.ts:159` `THEMES` 51 tema + `applyThemeVariables:303` belum load `path` JSON token secara dinamis; ekstensi theme tidak langsung terlihat. Rekomendasi rendah: host `import(path)` JSON → `THEMES[id] = {extension, isDark, label}` + CSS variables injection (reuse `applyThemeVariables`).
7. **Workspace `registerFileSystemProvider` virtual FS belum ada** — dampak: `workspace.registerFileSystemProvider(scheme, provider)` (`src/api/workspace.ts:1` tipe `FileSystemProvider` final) `throw NotImplementedError` prioritas rendah; ekstensi remote/in-memory FS tidak dapat register scheme `mem://`. Core hanya `TauriFileService` (`fileService.ts:66`) + `fileIpc` (`ipc.ts:11`) untuk FS lokal + `read_directory_batch`/`batch_read_files`. Rekomendasi rendah (sesuai `NOTRON_SDK_01_API_SURFACE.md:3.5`): tambah routing scheme di `fileService` → delegate ke provider map bila `uri.scheme !== 'file'`.
8. **Debug Adapter Protocol (DAP) host belum ada** — dampak: `debug.registerDebugAdapterDescriptorFactory` in-memory + `warnStubOnce`, `debug.startDebugging` `throw NotImplementedError` (`src/api/debug.ts:1`); `contributes.debuggers` declarative real (inspection) tapi tidak ada eksekusi debug; core hanya `runService.ts:1` 772 baris (`runProviderRegistry` per-runtime `build(resolved,args,ctx)` → `ShellDialect`) + `contrib/debug/taskProviders.ts` npm/cargo tanpa `Breakpoint`/`DebugAdapter` (grep DAP 0 hit `src-tauri`). Rekomendasi rendah: bangun DAP host + UI breakpoint (di luar cakupan SDK, prioritas rendah sesuai `01:8`); sementara Run tetap sebagai interim (pesan error sarankan Run).
9. **Tasks `executeTask` shell spawn nyata belum ada** — dampak: `tasks.registerTaskProvider` real bridge ke `taskRegistry.register` (provider npm/cargo `taskRegistry.ts:9`/`taskProviders.ts:27` + `runService` task→Run `type: task` sudah ada), tetapi `executeTask` fallback hanya `queueMicrotask` → `onDidStartTask`/`onDidEndTask` tanpa `cargo`/`npm` spawn; `fetchTasks` delegasi atau kumpulkan provider. Rekomendasi rendah: wiring `TasksDelegate.executeTask` → `taskRegistry.provideAll` + `terminalStore.newTerminal`/`ptyProcess.write` (reuse `TerminalInstance.svelte` `tauri-pty spawn`).
10. **SCM dekorasi & git-state event belum public + `views` panel `bottomPanel` wiring** — dampak: `scm.createSourceControl` CRUD penuh real (`SourceControlImpl` inputBox/groups/resourceStates), tetapi `onDidChangeGitDecorations`/`onDidChangeGitState` core (`git_service.rs:992`/`watcher_service.rs:346`) belum expose sebagai `scm` event publik (`SDK_EVENTS.DID_CHANGE_GIT_DECORATIONS/STATE` internal ready); view `scm` sudah generic (`scmRegistry.ts:9` `getBadgeText`/`loadComponent: SourceControlPanel`) tapi `resourceStates` → `SourceControlPanel` rendering masih perlu wiring host. `viewsContainers.panel` disamakan ke `ViewContainer` activitybar — `bottomPanelRegistry.ts:11` panel `terminal/output/problems` belum bridge untuk SCM panel bawah. Rekomendasi rendah: expose decoration delta via `ScmDelegate` + render `resourceStates` di `ScmView` generic.
11. **Terminal profile/link provider wiring ke selector belum ada** — dampak: `terminal.createTerminal` real (hybird fallback + `terminalStore` delegate), tetapi `registerTerminalProfileProvider`/`registerTerminalLinkProvider` fallback hybrid belum wiring ke terminal selector UI (`TerminalInstance.svelte` + `bottomPanelRegistry`). Rekomendasi rendah: host wiring saat `setTerminalDelegate` (`registerTerminalProfileProviderInternal`/`registerTerminalLinkProviderInternal`).
12. **Authentication OAuth + `SecretStorage` encrypted belum ada** — dampak: `authentication.registerAuthenticationProvider` in-memory + `getSession` throw; `SecretStorage` (`host/memento.ts:1` `InMemorySecretStorage` warn `console.warn` sekali belum encrypted) hanya placeholder; core grep `authentication` 0 hit. Rekomendasi sangat rendah (prioritas terendah `01:12`): tambah OAuth service + encrypted storage (OS keychain via Tauri plugin) + `SecretStorage` file-backed `<app_data>/secrets.json` dengan enkripsi.
13. **Templates `webview-view` / `language-support` / `custom-editor` masih stub folder** — dampak: hanya `hello-world` full (`manifest.json` `acme.hello-world` + `src/extension.ts` `registerCommand` + `package.json` peer `notron-sdk`), 3 lain manifest valid minimal + placeholder `src/extension.ts` + README `stub` (`src/cli/templates/*`); tidak blocking kriteria keluar (`NOTRON_SDK_04_PACKAGE_CLI:3` minimal hello-world wajib, sisanya boleh stub). Rekomendasi rendah: lengkapi setelah webview/languages bridge selesai (agar template dapat demo `WebviewViewProvider`/`CompletionProvider` nyata).
14. **Code signing `signature` placeholder** — dampak: `.ntrn` ZIP berisi `checksums.json` (`sha256:` per file via pure-JS `sha256HexSync` `utils/index.ts:683`) terverifikasi + tamper detection (`fase5.test.ts` 16 test), tetapi `signature` field kosong + log `note: code signing not implemented` (`src/cli/package.ts:1`); `verifyChecksum` util siap. Rekomendasi: penerbit tanda tangan digital (`minisign`/`cosign`/`openssl`) di luar cakupan, dicatat di `NOTRON_SDK_04_PACKAGE_CLI:1` sebagai rekomendasi lanjutan.

### Keputusan Desain Penting yang Diambil Agent

- **Grammar Highlighting CodeMirror 6/Lezer bukan TextMate** — `GrammarContribution.path` dibiarkan generik menunjuk modul CM6/Lezer (`NOTRON_SDK_02_MANIFEST_TYPES.md:171`); alasan: Notron core sudah modular per-bahasa dengan 80+ loader (`languageDetector.ts:116` `LANG_LOADERS: Record<string, Loader>` `async () => import('@codemirror/lang-...')` + `StreamLanguage` legacy) + `languageRegistry` per-bahasa + `Editor.svelte:232` `langCompartment.reconfigure` — bukan TextMate `.tmLanguage.json`; keputusan ini dicatat Fase 1 & diverifikasi eksplisit Fase 3 (805 baris detector, `FILENAME_TO_LANG`/`EXT_TO_LANG` via `constants/languages.json` share `file_ops.rs:659` `include_str!`).
- **`MenuLocation` 13 superset dari 7 di `01:151`** — menambah `menubar/file/edit/view/run/help` + `pane/context` agar selaras container nyata `menuRegistry.ts:11` (`menubar/*`, `pane/context`); tidak breaking, hanya superset, menjaga kompatibilitas (`Fase 1`).
- **Validasi idempotent unwrapping** — `validatePermissions`/`validateActivationEvents` menerima array mentah atau objek manifest-like (`manifest.permissions`/`activationEvents`) — memudahkan `validateManifest` mendelegasikan tanpa duplikasi error path + `ValidationResult {valid, errors}` dengan path dot/bracket (`engines.notron`, `contributes.commands[0].command`) — diuji 78 test `test/manifest.test.ts:1`.
- **Selective re-export & alias collision avoidance** — `src/index.ts:8` re-export selektif `types/*` + `ApiCommandContribution`/`ApiKeybindingContribution` alias untuk hindari duplikat `CommandContribution`/`KeybindingContribution` antara `types` dan `api/extensions.ts:22` (template lama) — `tsc --noEmit` tetap lolos tanpa mengubah kontrak publik (`Fase 1`).
- **Delegate injection menjaga read-only core** — `setCommandDelegate`/`setWindowDelegate`/`setWorkspaceDelegate`/`setViewsDelegate`/`setMenusDelegate`/`setLanguagesDelegate`/`setThemingDelegate`/`setDebugDelegate`/`setTasksDelegate`/`setScmDelegate`/`setSnippetsDelegate`/`setAuthenticationDelegate`/`setTerminalDelegate`/`setExtensionsDelegate` memungkinkan SDK standalone (fallback in-memory untuk test/smoke) dan bridged (host injects `commandRegistry.execute`, `statusBarRegistry.register`, `fileService.*`, `settingsStore.*`, `dialogService.*`, `activityBarRegistry`, `terminalStore.newTerminal`, dll) tanpa `import src/lib/...` langsung — dihormati tiap fase (`Fase 2–4`).
- **Error isolation hierarchy** — `NotronError` → `NotImplementedError` (stub bertipe lengkap, pesan jelas + code `NOT_IMPLEMENTED` per `00_INDEX:2`) → `ExtensionActivationError`/`ExtensionRuntimeError` + helper `withErrorIsolation`/`safeInvoke` membungkus `activate`/`deactivate`/command handler/event callback — satu ekstensi gagal tidak menghentikan host/ekstensi lain; diverifikasi `host.test.ts:40` (isolasi aktivasi/deactivate/event/command, LIFO dispose `['second','first']`, `topologicalSort` circular/missing, `isCompatible` `^0.1.0`).
- **Event bus `NotronEventBus` 15 `SDK_EVENTS` + `bridgeExternalEvent`/`toEvent`** — `host/eventBus.ts:1` `EventBus {on,off,once,emit,onRaw,emitRaw,clear,count}` via `window: EventTarget` + `CustomEvent` + per-listener `try/catch`; `SDK_EVENTS` 15 nama (`DID_OPEN_TEXT_DOCUMENT`…`DID_CHANGE_COLOR_THEME`/`DID_CHANGE_GIT_DECORATIONS`/`DID_OPEN/CLOSE_TERMINAL`) + helper untuk bridge Tauri `listen`/`window.EventTarget` ke bus — konsisten `NOTRON_SDK_03_HOST_RUNTIME:3.1`.
- **`ExtensionContext` LIFO dispose + `Memento`/`SecretStorage`** — `host/context.ts:1` `ExtensionContext {subscriptions: Disposable[], extensionPath/extensionUri, globalState/workspaceState Memento, secrets SecretStorage, logChannel OutputChannel, pushDisposable, dispose() reverse loop}` per `03:1` step 7; `host/memento.ts:1` `Memento` CRUD `get/update/keys` (delete via `update(key, undefined)`) `InMemoryMemento` + `createFileMementoPersistence` + `InMemorySecretStorage` warn once belum encrypted — sesuai `03:4`.
- **`ExtensionHost` lifecycle `topologicalSort` DFS + `isCompatible` lenient semver** — `host/lifecycle.ts:1` DFS + cycle detection + missing deps, `isCompatible` `^`/`~`/`>=`/`>`/`<=`/`<`/`=`/`x` wildcard untuk `engines.notron`; `register` sort + catat circular/missing ke `hostLog`, `activateAll` cek deps/compat skip+log `try/catch` continue, `deactivateAll` reverse+LIFO dispose bahkan bila `!activated` (agar declarative contributes selalu dibersihkan tanpa `activateAll` + idempotent `clear`).
- **Declarative contributions dibaca saat `register()` sebelum `activateAll`** — `host/contributions.ts:1` 195 baris `registerManifestContributions(manifest, extensionId) → Disposable` membaca `contributes.viewsContainers/views/menus/languages/grammars/themes/iconThemes/productIconThemes/snippets/debuggers/taskDefinitions` (7 kategori Fase 3 → 10 kategori Fase 4) via `validateManifest` enum whitelist, push ke `context.subscriptions` (LIFO) + `ExtensionHost.contributions` map — sehingga `views.getContainer/getView` sudah tersedia di `activate()` (`sidebar-counter` smoke `fase3.test.ts:292`).
- **`evaluateWhenClause` parser lengkap** — `src/api/menus.ts:1` 470 baris tokenizer `STRING` quoted `''`/`""`, `IDENT` allow `.`/`:`/`/`/`-` untuk `.js`/`.md`, operator `&&`/`||`/`!`/`==`/`!=`/`===`/`!==`/`=~`/`>`/`<`/`>=`/`<=`/`in`/`not in`, `(`/`)`, parser `or→and→equality→unary→primary`, missing IDENT → falsy (boolean) vs literal (RHS comparison), regex `=~` stripping `/.../flags` + `new RegExp` — lulus uji `editorTextFocus` truthiness, `a && b`/`||`/parens, `view == 'explorer'` quoted, `resourceExtname == .md` unquoted `.` literal, `!=`/`=~ /test/`/`/\.md$/` (`Fase 3`).
- **`languages` DiagnosticCollection real + provider hybrid** — `src/api/languages.ts:1` 486 baris menyimpan provider di `completionProviders`/`hoverProviders` arrays + `warnStubOnce` deskriptif `requires CodeMirror extension bridge (editorExtensionRegistry/compartment)` (`Fase 3`); `DiagnosticCollectionImpl` + `setLanguageConfiguration` CRUD penuh real karena tidak butuh bridge editor — pemisahan ini membuat gap prioritas tinggi eksplisit tanpa memblokir declarative `languages`/`grammars`.
- **esbuild bundling `cjs` default + external peer** — `src/cli/build.ts:1` `bundle:true`, `platform: node`, `target: es2020` configurable, `format: cjs` default (Host `import()` interop `default`, kompatibel `require('notron-sdk')` peer `external: ['notron-sdk', ...config.external]`) + `esm` via `notron-extension.config.ts` `format: 'esm'`; `sourcemap`/`minify` dev vs prod (prod `sourcesContent:false`, hapus stale `.map`); `package.ts` collect `manifest.json` + `dist/extension.js` (dari `manifest.main`) + `README`/`CHANGELOG` + `contributes` file refs + `assets/` → checksums → ZIP `.ntrn` via `AdmZip` — didokumentasikan `README` Bundling Details (`Fase 5`).
- **`tsconfig` `emitDeclarationOnly: false` + `emitDeclarationOnly: true` sebelumnya** — Fase 5 ubah `false` + `esModuleInterop:true` + `allowSyntheticDefaultImports:true` agar `dist/cli/*.js` ter-emit untuk bin `notron-sdk`/`ntrn` (`package.json:bin`) sambil tetap `declaration:true` + `declarationMap:true` untuk `.d.ts` — tidak breaking Fase 1–4 (`bun test` via `bun` transpiler, `tsc --noEmit` gate tetap).
- **Checksums pure-JS SHA-256 FIPS 180-4 tanpa `node:crypto`** — `src/utils/index.ts:683` `sha256HexSync` 64-round pure-JS + async `crypto.subtle` fallback; `computeChecksumSync(data) → sha256:<hex>` / `computeChecksum` / `verifyChecksum` / `generateChecksums` — aman di browser bundle, terverifikasi vektor `hello → sha256:2cf24dba...` + tamper detection (`fase5.test.ts` 6 test) — reuse oleh CLI `package` (`checksums.json` map) dan nanti core (`extension_loader.rs` `sha2`).
- **Mock Host `Proxy` `withHelpers`** — `src/testing/index.ts:1` 446 baris `withHelpers<T>(ns, helpers) → Proxy` + `Reflect.get` agar getter `window.activeTextEditor`/`workspace.workspaceFolders`/`tabGroups` tetap delegasi ke fallback store (`__set*`) tidak hilang saat spread; `createMockNotron({reset:true})` default reset semua 11 registries + delegates (`__resetAllMocks`); `createMockContext(id)` isolasi `InMemoryMemento` per `id`; 14 namespace terproxy dengan introspection `__get*`/`__clear*`/`__fire*` — diuji `test/testing.test.ts:1` 34 test + spec integration `myExtension.activate → executeCommand → __getShownMessages` (`05:2`).
- **Lint setup minimal konsisten Notron** — `eslint@8.57.1` + `@typescript-eslint/parser@6.21.0` + `plugin@6.21.0`, `.eslintrc.cjs` `extends eslint:recommended` + `plugin:@typescript-eslint/recommended`, `ignorePatterns dist/node_modules/examples/**/dist/templates`, `rules prefer-const warn, no-console off, no-unused-vars off via @typescript-eslint/no-unused-vars with ^_` — fix `no-dupe-else-if` (`snippets.ts:139` unreachable branch), `no-constant-condition` (`while(true)`), `prefer-const` (`menus.ts:354`, `utils/index.ts:713`) — gate `lint` exit 0 tiap fase (`Fase 6`).

### Rekomendasi Lanjutan (Di Luar Cakupan Tugas Ini)

- **Loader `.ntrn` di sisi core** — implementasikan `src-tauri/src/extension_loader.rs` + scan `~/.notron/extensions-dev` & `extensions/` saat `load_startup_state` (`lib.rs:81`, `startup.rs`, `db.rs`), verifikasi `checksums.json` (`verifyChecksumSync` Rust `sha2` crate, reuse `utils` logic), unzip `AdmZip` compat, validasi `manifest.json` (`validateManifest` port Rust atau reuse `schema`), cek `engines.notron` + `extensionDependencies`/`extensionPack`, lalu `ExtensionHost` wiring (`setCommandDelegate` → `commandRegistry`, `setWorkspaceDelegate` → `fileService`/`settingsStore`, dll) + watcher `notify::Watcher` untuk `extensions-dev` → `app.emit('extensions-changed')` → hot-reload.
- **Isolasi proses / sandbox ekstensi** — ekstensi saat ini berjalan di renderer Svelte (trusted `trusted`/`dangerous` capability `commandCapabilities.ts:43` belum enforce). Rekomendasi: jalankan ekstensi di worker/isolated context atau `WebWorker` dengan CSP + `permission` gating (`permissions` 7 nilai `fs:workspace-read/write`, `fs:outside-workspace`, `shell:execute`, `network:fetch`, `clipboard:read/write`) — validasi sudah ada Fase 1 tetapi belum enforce runtime; cari `fs:outside-workspace` → blokir fallback in-memory tree root check, `shell:execute` → gate `runService`/`terminal`.
- **Penegakan permission sungguhan** — hubungkan `ExtensionPermission` (`src/types/index.ts:1`) dengan `trustGate`/`isAllowed` (`registry.ts:150`) dan `ipc` guard (`src-tauri/src/file_ops.rs`/`git_service.rs`); tolak `fs:workspace-write` tanpa permission saat `workspace.fs.writeFile` di luar workspace root, tolak `network:fetch` tanpa permission, log ke `OutputChannel` `Extension Host`.
- **Code signing** — implementasikan `signature` di `.ntrn` (`src/types/index.ts:284` `PackageSignature` placeholder) dengan `minisign`/`cosign`/`openssl ed25519`; `package` generate `checksums.json` + `signature.json` (sign `checksums.json` bytes), core `verifyChecksum` + `verifySignature` dengan public key di `manifest.publisher` atau marketplace registry; sementara `checksums.json` SHA-256 + tamper detection sudah real (`fase5.test.ts`).
- **Marketplace / publishing pipeline** — registry HTTP untuk `publisher.name` uniqueness + `SEMVER_REGEX` + `ID_REGEX` enforcement, `notron-sdk publish` CLI (upload `.ntrn` + `checksums.json` + `signature`), `notron-sdk install <id>@<version>` (download → `installLocal`).
- **Debug Adapter Protocol (DAP) host** — bangun DAP host + breakpoint UI (gutter + `DebugSession`/`DebugAdapterDescriptorFactory` wiring ke `runProviderRegistry` + `terminal`), selesaikan stub `debug` Fase 4 (prioritas rendah, tetapi penting untuk Run & Debug parity VS Code).
- **Webview CSP & message bridge hardening** — selesaikan Batasan #4 dengan `WebviewHost.svelte` isolated iframe + CSP `script-src 'self'` + `postMessage` `onDidReceiveMessage` validation + `acquireVsCodeApi()` shim.
- **Languages provider registry lengkap** — selesaikan stub prioritas tinggi dengan `registerCompletionItemProvider` → `editorExtensionRegistry` facet + `Compartment` per provider + `DiagnosticCollection` sudah contoh yang berhasil.
- **Encrypted `SecretStorage`** — ganti `InMemorySecretStorage` warn dengan OS keychain (`keyring` crate Tauri plugin) + file-backed encrypted `<app_data>/secrets.json` + `MementoPersistence` wiring ke `<app_data>/extensions-state/<id>.json` (gap `NOTRON_SDK_03_HOST_RUNTIME:4` dicatat).
- **CI gate & quality** — pertahankan `bun run typecheck` + `lint` + `test` + `build` di GitHub Actions (Windows + PowerShell 5.1 sesuai `AGENTS.md`), tambah `cargo check` di `src-tauri/` bila core loader ditambah; pertahankan `bun run check` (`svelte-check`) 0 errors/0 warnings gate repo (`AGENTS.md`).
- **Dokumentasi & examples** — lengkapi 3 stub templates (`webview-view`, `language-support`, `custom-editor`) setelah webview/languages bridge selesai, pindah `packages/notron-sdk/examples/*` ke `examples/*` sejajar `packages/` (deviasi Fase 6 karena batasan HANYA `packages/notron-sdk` boleh tulis — ideal `NOTRON_SDK_05_TESTING_DX:4`), publish ke VS Code extension gallery analog.

**Kriteria keluar Fase 7: Tercapai.**
- Laporan Akhir tersusun lengkap dengan tabel yang benar-benar terisi (bukan placeholder kosong) — 14 namespace, 19+ event + 27+ command final, 14 gap prioritas dengan dampak & rekomendasi, 15 keputusan desain dengan alasan, 10+ rekomendasi lanjutan — seluruhnya dirangkum dari `IMPLEMENTATION_LOG.md` Fase 0–6 tanpa analisis ulang.
- Semua fase 1–6 memenuhi kriteria keluarnya masing-masing (diverifikasi ulang: `tsc --noEmit` exit 0, `eslint` 0, `bun test` 216 pass, `build` emit `dist/`).



