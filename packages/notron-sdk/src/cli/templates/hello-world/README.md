# hello-world

Minimal Notron extension generated via `notron-sdk init`.

## Build

```bash
npm install
notron-sdk build          # dev (with sourcemap)
notron-sdk package        # prod + .ntrn
notron-sdk validate       # lint manifest
```
